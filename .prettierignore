# Dependency / build output
node_modules
dist
.turbo
coverage
.next
build
out
pnpm-lock.yaml

# Generated code — machine output validated by its own stale-check (openapi-typescript,
# packages/api-client/src/schema.gen.ts). Not prettier-managed (would fight the generator).
**/*.gen.ts

# Pre-existing handcrafted content — NOT prettier-managed (authored by hand, source of truth).
# Prettier only formats the code we own (root config + packages/**).
# NOTE: leading slash anchors to repo root — a bare `tokens/` also matches `packages/tokens/`
# (gitignore semantics), which would silently drop an owned package from format:check.
/designs/
/docs/
/tokens/
/.claude/
/.github/
/tests/harness/
# Native services (Go/Rust) — formatted by gofmt/rustfmt via `make verify-go|verify-rs`, not prettier.
/services/
spec.md
design-system.md
CLAUDE.md
AGENTS.md
Cat Peek - Behavior Spec.md
.gitignore

# Design-reference prototype (handoff HTML/JS/assets) — not code we format.
/design_handoff_wedding_invitation/

# Draco decoder vendored verbatim from three (self-hosted, PDPL) — third-party emscripten
# output, not ours to format (mirrors the eslint ignore).
/apps/storefront/public/draco/
