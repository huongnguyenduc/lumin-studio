# Keep the build context to the crate sources cargo needs. target/ is huge (compiled
# artifacts) and would bust the COPY layer cache; the rest is docs/build-meta the binary
# never embeds.
/target
/testdata
Dockerfile
.dockerignore
.gitignore
README.md
