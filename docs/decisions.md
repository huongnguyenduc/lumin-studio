# Decisions log (ADR) — Lumin Studio

> **Mục đích:** ghi **vì sao** mỗi lựa chọn được chốt, gồm cả phương án đã loại. **Đọc trước khi định đổi bất cứ thứ gì.**
> **Quy ước:** muốn thay một quyết định → không xoá ADR cũ; đặt `Status: Superseded by ADR-NNN` và viết ADR mới.
> Tất cả chốt 2026-06-22 sau 3 vòng thảo luận; vòng 2–3 có nghiên cứu + fact-check (run wf_dde709c1, wf_8a7b6f32).

---

### ADR-001 — Monorepo pnpm + Turborepo · Accepted
4 bề mặt + 1 state machine + 1 design system ⇒ cần lõi chia sẻ (`packages/core`, `tokens`, `ui`, `api-client`). Turborepo cache + build affected-only. **Loại:** 4 repo rời (đồng bộ state machine khổ).

### ADR-002 — Frontend Next.js (storefront, admin) + MV3 (extension) · Accepted
Next.js: SSR/SEO cho catalog, mobile-first, hệ sinh thái lớn, dễ tích hợp model-viewer. Admin Mobile = **cùng app admin responsive**, không tách codebase. **Loại:** Vue/Nuxt (đội không có lợi thế rõ).

### ADR-003 — Core API: Go + Chi v5 (+ hợp đồng OpenAPI) · Accepted
Chi là pure `net/http` → toàn hệ sinh thái dùng được; binary tĩnh idle ~15–40MB, hợp box nhà (tải thật nằm ở Postgres/NATS/Garage/Blender). Toolchain: **chi v5 + pgx v5 + sqlc + go-chi/jwtauth + RBAC nhỏ**. **Bắt buộc lớp hợp đồng** vì Chi không cho type TS: **OpenAPI → oapi-codegen (Go) + openapi-typescript/openapi-fetch (3 client TS)**. **Loại:** Node/NestJS (idle ~80–150MB+, đã rời bỏ); Fiber (fasthttp, mất tương thích net/http); Gin/Echo (context riêng). Connect-RPC để dành cho surface streaming/admin nếu sau này cần.

### ADR-004 — PostgreSQL (app) + Postgres DB riêng cho Umami · Accepted
JSON column cho statusHistory/address/personalization. Truy cập qua sqlc+pgx (type-safe, nhanh hơn GORM). Umami để **DB riêng** tránh query nặng khoá bảng app.

### ADR-005 — Object storage: Garage, all-home (KHÔNG MinIO, KHÔNG R2) · Accepted
**MinIO CE đã archive (2/2026)**, gỡ admin console, ngừng publish Docker image từ 10/2025 → không xây mới trên đó. Garage: S3-compatible, ~1GB RAM, single-node, còn maintain. User chọn **all-home** (không R2). **Hệ quả bắt buộc:** Garage **không có object versioning** → backup ngoài là lưới an toàn duy nhất; serve .glb/sprite qua **Cloudflare cache** với URL **content-hash bất biến + TTL dài** (giữ asset nhỏ); upload model qua **S3 multipart presigned-PUT, part <100MB** (Cloudflare Tunnel chặn body >100MB). Lối thoát nếu vướng ToS file lớn của CF: bật bucket R2 cho asset public (cùng API S3, không sửa code).

### ADR-006 — Queue: NATS JetStream + outbox (KHÔNG River) · Accepted
Worker là **Rust** (ADR-007) → River (Go-only, crate Rust mới 0.0.0) không dùng được. NATS JetStream: broker language-agnostic nhẹ nhất cho 1 box, ack-wait dài + heartbeat cho render lâu, MaxDeliver + stream DLQ. **Bắt buộc outbox / publish-on-commit** (publish job chỉ sau khi row AssetJob commit) để tránh mất job do dual-write. **Lưu ý:** nếu sau này worker chuyển sang Go, cân nhắc River để bỏ hẳn broker.

### ADR-007 — Asset Worker: Rust + Blender, GIỮ render 360° server-side · Accepted (user override)
Nghiên cứu khuyến nghị worker Go + bỏ turntable (model-viewer client-side đã lo xoay 360°), nhưng **user chọn giữ** để fidelity cao + làm fallback no-WebGL. **Ràng buộc bắt buộc:** render bằng **Cycles + CUDA** (KHÔNG OptiX — Pascal không RT core, phí VRAM; KHÔNG EEVEE — chết headless); OpenImageDenoise chạy **CPU** trên CC 6.1; **subprocess** Blender (không bpy) để crash-isolation + retry; **concurrency=1**, off-peak; decimate + ≤1080p + sample vừa để vừa 6GB VRAM. WSL2 GPU: chỉ cài driver NVIDIA trên Windows + cuda-toolkit trong WSL2, **không** cài driver Linux trong WSL2; validate Blender thấy GPU trong container (Blender #126014) trước khi coi là xong. ⇒ **32GB RAM gần như bắt buộc**. **Worker KHÔNG render poster** — ảnh đại diện/list của SP dùng **ảnh shop chụp** (`Product.images[0]`); 360° sprite chỉ dùng cho hover (PC)/dừng-2s (mobile) trên card + fallback no-WebGL của viewer 3D.

### ADR-008 — Realtime: SSE từ Core API · Accepted
Tiến độ là server→browser thuần → SSE (EventSource auto-reconnect). NATS **không** lộ ra browser. **Bắt buộc** cấu hình chống buffer của cloudflared (no-transform, Content-Encoding: identity, X-Accel-Buffering: no, http.Flusher mỗi event, heartbeat) + **smoke-test qua named tunnel**; fallback = polling ngắn (admin ít người, thừa sức). **Loại:** NATS-over-WebSocket ra browser (lộ broker).

### ADR-009 — Hosting: all-home WSL2 + Cloudflare Tunnel, chấp nhận downtime · Accepted (user)
GPU nhà = compute miễn phí cho Blender; cloudflared = không mở port, ẩn IP, TLS/WAF ở edge. User **chấp nhận downtime** (không VPS edge). Khuyến nghị gần-free để bù: UPS + tắt auto-reboot Windows + cache catalog mạnh + Uptime Kuma ping từ ngoài.

### ADR-010 — Thanh toán: đối soát VietQR **thủ công** (SePay/Casso bị từ chối) · Accepted (user)
User giữ thủ công. **Luồng web (pay-then-confirm):** checkout **chưa tạo đơn** → màn **QR tĩnh** (render server-side từ STK đã lưu; **nội dung/memo CK không bắt buộc**) → khách **gửi ảnh chụp biên lai + bấm xác nhận** thì frontend mới `POST /orders` tạo đơn ở `PENDING_CONFIRM` (kèm ảnh CK). Vì khoảng chờ "đã CK → xác nhận" là rủi ro bỏ giỏ lớn nhất, các bù trừ thành **bắt buộc**: màn "chờ xác nhận" (khách vào qua **link tra cứu đơn**) có timeline + thời gian dự kiến + **auto-poll**; shop **đối soát bằng ảnh biên lai** khách gửi (đối chiếu số tiền) → **nút 1-chạm → PAID** (owner-only) trên Admin + Admin Mobile; email link+mã đơn. SePay/Casso (webhook real-time) là option để dành — nếu bật sau này cần thêm memo/định-danh-đơn để auto-match (QR tĩnh hiện không mang). **Loại (đừng làm):** tự scrape ngân hàng/parse email; cổng VNPay/MoMo/ZaloPay (giai đoạn 1 chỉ bank_transfer).

### ADR-011 — Extension: assistive-only (KHÔNG đụng DOM Meta) · Accepted (user)
Extension MV3 scrape/tự động hoá messenger.com/instagram.com **vi phạm Meta Platform Terms** → nguy cơ **khoá vĩnh viễn tài khoản bán hàng** (existential). Thu nhỏ: panel chỉ gọi BFF (tra đơn, form tạo đơn, copy mẫu trả lời, quét mã qua paste/camera); **người tự bấm** trong Meta. Giải phóng phần lớn phase 5. Fallback nếu volume buộc tự động: Zalo OA / Messenger API chính thức.

### ADR-012 — Hàng cá nhân hoá: cọc + trả trước + no-COD + huỷ/hoàn-tiền · Accepted (user)
Hàng khắc không bán lại được → COD bị bom là mất trắng. **Đặt cọc (Điều 328 BLDS) + mặc định trả đủ trước qua VietQR + không COD hàng khắc + tickbox "không đổi trả hàng cá nhân hoá" trước thanh toán** (vô hiệu hoá quyền huỷ 30 ngày của Luật BVNTD 19/2023; vẫn giữ nghĩa vụ in lại nếu lỗi/sai). Bước **echo lại nội dung khắc** trước khi trả tiền.

**Cập nhật 2026-06-24 (user) — mô hình huỷ/hoàn tiền (đồng bộ `spec.md §04`):**
- **Bỏ `RETURNED`.** Trả đủ 100% trước + hàng custom không tái sử dụng ⇒ không nhận hàng trả về. Mở lại chỉ khi sau này có mặt hàng không-cá-nhân-hoá có thể trả ("chuyện rất lâu").
- **Thêm state `REFUNDED`** thay vì gắn metadata refund vào `CANCELLED`. `CANCELLED` = đóng đơn **không** hoàn tiền (tiền chưa về, hoặc lỗi khách + đã in → giữ tiền); `REFUNDED` = **đã chuyển hoàn** (thường lỗi shop), kèm `refundProofUrl`. "Fault do shop/khách" biểu đạt bằng chính lựa chọn `CANCELLED` vs `REFUNDED` — không cần field riêng. Cả hai terminal; không có đường ra khỏi `COMPLETED`.
- **Inbox vào thẳng `PAID`:** nhân viên tự kiểm tra thấy tiền về rồi mới tạo đơn ⇒ không qua `PENDING_CONFIRM` (state này chỉ còn ở kênh web, nơi chủ đối soát ảnh biên lai).
- **RBAC tiền-vào vs tiền-ra:** tạo đơn inbox-đã-`PAID` cho phép **nhân viên** (họ là người verify tiền, rủi ro thấp); nhưng `→ REFUNDED` và sửa `bankAccount`/STK **owner-only** (tiền-ra + config khoá cho chủ). Bổ sung cho ADR-010 (`PENDING_CONFIRM → PAID` web vẫn owner-only).
- **Kế toán:** doanh thu ròng = đã thu − `REFUNDED`; `CANCELLED` sau `PAID` không hoàn = shop giữ tiền (vẫn là doanh thu). Hàng custom không nhập lại kho; chỉ ghi filament đã tiêu thành phế phẩm (Spoolman) nếu huỷ sau PRINTING.
- Copy trấn an checkout bỏ "đổi trả miễn phí 30 ngày" → "in lại miễn phí nếu lỗi do shop"; `Setting.returnPolicy` → `refundPolicy`.

### ADR-013 — Thông báo: email-first, Zalo để sau · Accepted (user)
Bắt đầu email (miễn phí) cho mọi mốc trạng thái. Zalo OA + ZNS/ZBS (mở tin 60–90%, ~200–400đ/tin) **để sau** — khi thêm chỉ việc gắn template vào transition `statusHistory` (idempotent).

### ADR-014 — Observability: OTel Collector + OpenObserve/Victoria + Uptime Kuma + GlitchTip · Accepted
1 OTel Collector làm phễu (app chỉ biết nó → backend swap được). **Đánh giá OpenObserve trước** (1 binary Rust, lưu vào Garage). Uptime Kuma (ping từ ngoài) + **GlitchTip** (KHÔNG Sentry self-host — 16GB+/40 container). Trace để sau. Ràng buộc #1 là **CPU contention**, không phải RAM → `cpus=` limit mọi container.

### ADR-015 — Analytics: Umami v3.1 self-host (KHÔNG PostHog) · Accepted
Umami v3.1 OSS đã có funnel/retention/replay; ~0.2–0.5GB, dùng DB Postgres riêng. PostHog self-host quá nặng (sàn 16GB/7 service). Cắm event spec §08 qua `umami.track()` từ `core/api-client`. **Session replay TẮT mặc định** (PDPL), opt-in + che input + tắt ở /personalize + checkout.

### ADR-016 — Search: PostgreSQL FTS + unaccent · Accepted
Đủ cho catalog nhỏ tiếng Việt (không dấu). **Loại:** Meilisearch (thêm service + đồng bộ index) cho tới khi thực sự cần search-as-you-type chịu lỗi gõ.

### ADR-017 — Mô hình địa chỉ: tỉnh → phường → đường (BỎ quận/huyện) · Accepted
VN bỏ cấp huyện từ 2025-07-01. Schema dùng `province → ward → street`, **không** có trường district. Quyết định schema rẻ bây giờ, migration đau nếu ship sai.

### ADR-018 — Backup là điều kiện launch (WAL-G + restic, offsite, test restore) · Accepted
SSD đơn + 1 máy + Garage không versioning = mất trắng nếu hỏng ổ. WAL-G (hoặc pgBackRest đã hồi sinh 5/2026) cho Postgres + restic cho object/secrets → đẩy offsite (off-peak). **TEST restore trước launch** — không thương lượng.

### ADR-019 — Glyph tiền: một formatter trong `core` xuất `390.000₫` · Accepted
`Intl('vi-VN')` ra `390.000 ₫` (có khoảng trắng); spec/brand muốn `390.000₫` (không space, U+20AB). Dùng **một** formatter trong `core`, post-process Intl để ra đúng spec. Không format tiền rải rác.

### ADR-020 — Stack lint/test/format: ESLint 10 + Prettier + Vitest/fast-check + golangci-lint/sqlc-vet + clippy · Accepted
Yêu cầu cứng (i18n không hard-code chuỗi, a11y WCAG, tiền qua một formatter) **chỉ ESLint enforce được** — Biome `noJsxLiterals` còn nursery/info và plugin GritQL không biểu đạt nổi các rule contextual/type-aware này; oxlint chưa hỗ trợ custom type-aware rule (alpha). ⇒ **TS:** ESLint 10 (flat, v9 EOL 8/2026) + typescript-eslint + `eslint-config-next` + `eslint-plugin-jsx-a11y` + `eslint-plugin-i18next` (`no-literal-string`) + rule in-house `no-restricted-syntax/properties` cấm `Intl.NumberFormat`/`.toLocaleString()` ngoài `packages/core`; **Prettier 3** + `prettier-plugin-tailwindcss` (tách khỏi ESLint); **typecheck** `tsc -b` project references qua Turborepo; **test** Vitest 3 + `@fast-check/vitest` (bất biến tiền/state) + Playwright (e2e). **Go:** `golangci-lint` v2 + `sqlc vet` (rule `sqlc/db-prepare` chạy với testcontainers Postgres) + `go test -race` + testcontainers-go. **Rust:** `cargo fmt` + `clippy -D warnings` + `cargo test` (tuỳ chọn nextest). Next.js 16 bỏ `next lint` ⇒ wire ESLint trực tiếp. **Loại:** Biome-only (mất enforcement i18n/a11y/tiền), Jest (chậm hơn Vitest), eslint formatting rules (dùng Prettier). **Lưu ý:** sentence-case + `prefers-reduced-motion` KHÔNG lint được → kiểm bằng test trên message catalog / Stylelint. Script chuẩn để hook gọi: root `pnpm verify/lint/typecheck/test/lint:file`; `make verify-go/verify-rs`. Chi tiết: `agent-harness.md`.

### ADR-021 — Kiến trúc điều khiển Claude Code (harness): advisory→deterministic · Accepted
Để hạ tỉ lệ sai/lệch: CLAUDE.md/rules là *advisory* (không đảm bảo tuân thủ — model có thể bỏ qua), nên mọi luật "luôn-phải" được đẩy xuống tầng *deterministic*. Triển khai trong `.claude/` (commit vào repo): **hooks** — `guard-bash` (chặn rm -rf, `docker compose down -v`, git push --force, mkfs/dd, DROP/TRUNCATE), `guard-files` (chặn cứng secrets; **ask** khi sửa file hợp đồng), `format-and-lint` (PostToolUse Prettier+ESLint per-file), `verify-before-stop` (Stop hook chặn dừng tới khi typecheck/lint/test xanh, check `stop_hook_active`, van xả `.claude/.skip-verify`); **`.claude/rules/*.md`** path-scoped (load theo bề mặt đang chạm, giảm context rot); **`agents/spec-guardian`** (reviewer ngữ-cảnh-mới soi diff theo ADR/conventions). Mọi hook **tự no-op** tới khi tool/script tồn tại (an toàn trước Phase 0) và gọi script trừu tượng nên độc lập version. Kỷ luật quy trình: plan-to-file cho việc nhiều-file, `/clear` sau 2 lần sửa sai, single-thread + subagent kiểm chứng cho code (multi-agent chỉ cho nghiên cứu đọc rộng). **Loại:** chỉ dựa CLAUDE.md (advisory, dễ bị bỏ qua — xem issue #21119); over-fragment nhiều agent song song cho việc viết code (Cognition "Don't Build Multi-Agents"). Chi tiết: `agent-harness.md`. **(Mệnh đề "ask khi sửa file hợp đồng" được ADR-022 siết lên hard-block cho `decisions.md`+`conventions.md`; phần còn lại giữ nguyên. Mở rộng harness vòng 2: ADR-023.)**

### ADR-022 — Siết file hợp đồng lõi (decisions/conventions) từ ASK lên hard-block · Accepted (supersedes một phần ADR-021)
Từ vòng nghiên cứu harness (2026-06-23, run wf_1a2ff429; chi tiết `agent-harness-learnings.md`). ADR-021 đặt `guard-files` ở mức **ask** cho mọi file hợp đồng. ask là gate **mềm** — một "yes" lỡ tay giữa phiên có thể đổi luật binding mà cả harness đang ép theo (Aider tách `/read` chỉ-đọc khỏi `/add`; thứ tự quyền của Claude Code: **deny > ask**). **Quyết định:** `guard-files.sh` **hard-block (exit 2)** với `docs/decisions.md` + `docs/conventions.md`; **van xả** là file `.claude/.allow-contract-edit` (mirror `.skip-verify`) — khi tồn tại thì rơi xuống mức ask cũ. **Giữ nguyên ask** cho `tokens/*.css` + `CLAUDE.md` + `AGENTS.md` (Phase 0 sửa thường xuyên). **Đường amend chính thức** cho mọi thay đổi ADR/luật (kể cả viết ADR mới): `touch .claude/.allow-contract-edit` → sửa (chỉ THÊM ADR / đánh dấu Superseded) → `rm .claude/.allow-contract-edit`. **Phạm vi supersede:** chỉ mệnh đề "ask file hợp đồng" của ADR-021 cho 2 file lõi. **Loại:** hard-block luôn cả tokens/CLAUDE.md (ma sát Phase 0); chỉ dựa prompt/spec-guardian (advisory, không đảm bảo).

### ADR-023 — Mở rộng harness vòng 2: orient / anti-reward-hacking / anti-loop / skills / oracle / self-test · Accepted
Từ nghiên cứu các harness coding-agent phổ biến (Claude Code lifecycle hooks, Cline/Roo Memory Bank, OpenHands StuckDetector, Amp Oracle, SWE-agent ACI, spec-kit/Kiro, AGENTS.md) — bảng đối chiếu "đã có vs còn thiếu" + lý do từng khuyến nghị ở **`agent-harness-learnings.md`**. **Giữ nguyên** nguyên lý advisory→deterministic (ADR-021) và **không** đổi quyết định cũ; chỉ bổ sung: (1) **SessionStart hook** `session-start.sh` — orient (nhánh + index plan + `active-context.md`) + reset state loop/retry. (2) **Anti-reward-hacking** — `guard-files` ask khi test bị làm yếu (thêm `.skip`/`t.Skip`, giảm assertion) + spec-guardian coi xoá test/bỏ assertion trên invariant lõi là **BLOCKER**. (3) **Anti-loop** — retry budget trong `verify-before-stop` (4 lần fail cùng target → surface cho người, không loop vô hạn — vd test GPU không pass nổi trên GTX 1060) + loop-detector lệnh y hệt (≥4×) trong `guard-bash`. (4) **Parse-gate Go** (`gofmt -e`) sớm trong `format-and-lint`. (5) **Tier `.claude/skills/`** (vn-compliance, render-worker-gpu, event-outbox — pointer mỏng, **defer** về doc nguồn). (6) **Agent `oracle`** (design second-opinion, read-only, gọi tay, **không bao giờ** là gate). (7) **Harness self-test** `tests/harness/guard.test.sh` chạy ở CI khi `.claude/**` đổi (gate no-op = rủi ro vô hình). (8) **`AGENTS.md`** pointer cross-tool mỏng. (9) **Acceptance ledger** `docs/acceptance.md` (EARS, test-linked) + **compaction contract** (keep_first) trong `agent-harness.md`. spec-guardian thành **read-only thật** (bỏ Bash; caller paste diff). **Loại:** multi-agent song song cho viết code; LLM-judge làm gate cứng (giữ **test là gate**; oracle/spec-guardian chỉ advisory). Chi tiết cơ chế: `agent-harness.md`.

### ADR-024 — Mở rộng harness từ nghiên cứu vòng 2 (chứng minh test ràng buộc · truy vết acceptance · bồi đầu trái · sàn sandbox) · Accepted (user 2026-06-23)
Từ vòng nghiên cứu harness thứ 2 (2026-06-23, run wf_57240dd3) soi **methodology-level** + framework mới hơn vòng 1 (spec-driven: Spec Kit/Kiro/Tessl · agentic-SDLC: BMAD/Agent OS/APM/Vibe Kanban · essay engineering Anthropic · eval-driven: ImpossibleBench/Meta-ACH mutation/SWE-bench · memory: Beads/mem0/Cline · parallel-orchestration · Claude Code/SDK 2026 · 12-factor/Ralph). Bảng "đã có vs còn thiếu" + lý do từng REC ở **`agent-harness-learnings-r2.md`** (31 ứng viên → verify đối kháng từng cái với file thật → 26 giữ, 5 bỏ vì trùng/đụng ADR/sai sự thật). **Giữ nguyên** advisory→deterministic (ADR-021) và **không** relitigate single-thread-cho-code / LLM-judge-không-bao-giờ-gate; chỉ bồi 4 seam còn hở:
- **(1) Green-gate chứng minh test RÀNG BUỘC, không chỉ PASS** — mutation kill-gate `tests/harness/osm-mutation.test.sh` (REC-15: mutant cố định allow-all/swap/drop-history/drop-reason → assert OSM-01..03 chuyển đỏ; em deterministic của `guard.test.sh`, **off** inner-loop `verify-before-stop`) + anti-overfit/special-casing trong `guard-files.sh` (REC-16: ask khi SOURCE ngoài `core` hardcode literal output khớp fixture test đang sửa) + dòng WARN special-casing trong `spec-guardian` + §Test integrity trong `conventions.md`.
- **(2) Truy vết acceptance hai chiều** — coverage-map test requirement↔task↔test (REC-17, cần thêm 1 dòng convention task-id) + EARS-grammar lint trên `acceptance.md` (REC-18).
- **(3) Bồi đầu trái** — PreCompact hook `pre-compact.sh` làm keep_first **deterministic** (REC-19: snapshot plan-path · 4 luật · changed-files · verify-cmd · ADR-chạm vào `.claude/.precompact-state`; `session-start` phát lại + xoá one-shot) + reasoning-digest/archive (REC-20) + eval regression hành vi agent (REC-21, off green-gate, người curate) + turn/cost budget (REC-33) + repair-event artifact (REC-34).
- **(4) Sàn sandbox (tier ADR-021 đặt tên chưa dựng)** — env-scrub subprocess `env.CLAUDE_CODE_SUBPROCESS_ENV_SCRUB=1` (REC-22) + OS Bash sandbox render-worker/ops (REC-23) + container throwaway experiment-loop (REC-24).
Cùng: story-file ephemeral (REC-25) · risk-axis pre-code (REC-26) · insight store non-binding (REC-28) · issue-graph additive (REC-27) · context-rot budget (REC-30) · fan-out heuristic (REC-31) · worktree hygiene serial (REC-32) · SubagentStop advisory (REC-36) · harness plugin version-stamp (REC-37). Forward-looking chờ inbox-assistant (ADR-013): convention tool/API agent-facing (REC-29). Opt-in cần scope riêng: Ralph batch-lane cơ học (REC-35). **Loại (đã verify đối kháng, bỏ):** spec-task lifecycle event (Claude Code không có transition-event như Kiro) · sequential role-hats (thuần relabel cơ chế đã có) · eval-driven tool-description cho inbox-assistant (panel REST typed, không LLM tool — ADR-011) · metamorphic 4-surface (MỘT core/OpenAPI client, không có impl thứ 2 để relate — ADR-001) · split `progress.md` riêng (đã là `plan.md`). **Mọi REC giữ TEST làm gate**; LLM (spec-guardian/oracle) chỉ WARN/NOTE. Lộ trình P0→P3 + cơ chế từng cái: `agent-harness-learnings-r2.md` + `agent-harness.md`. **Trạng thái:** REC-15/16/19/22 Implemented; REC-17/18/20/21/23–37 chờ implement.

### ADR-025 — Học chọn lọc từ obra/Superpowers: front-load luật/skill + harvest process craft · Accepted (user 2026-06-23)
Từ nghiên cứu framework **Superpowers** (vòng harness thứ 3, run wf_cb144264; chi tiết `agent-harness-learnings-superpowers.md`) — *methodology-as-mandatory-skills* đa nền, enforce gần như **toàn bộ bằng advisory prose**. **Kết luận: tầng enforcement của Lumin đã MẠNH HƠN** (verify-before-stop green-gate / mutation kill-gate / anti-reward-hacking là bản *deterministic* của các skill advisory của nó); Superpowers còn **tự tái khẳng định** nguyên lý ADR-021 ("mechanical constraints → automate; documentation chỉ cho judgment calls"). **Giữ nguyên** advisory→deterministic + single-thread-cho-code + LLM-judge-không-bao-giờ-gate; chỉ **adopt phần process/authoring craft**, đều ở tầng advisory/context-injection (KHÔNG thêm gate mới):
- **(A1·REC-SP-01·Implemented)** `session-start.sh` front-load **4 luật always-must** lúc startup/clear/resume (không chỉ post-compact) — vá lỗ phiên sửa path không auto-load `domain-core.md` (OSM xuyên 4 surface) khởi động mù. Gate `[ -f .precompact-state ]` tránh trùng sau /compact; đồng bộ literal `pre-compact.sh` dòng 17. +assert `guard.test.sh`.
- **(A2·REC-SP-04·Implemented)** thêm vế **"Đọc trước khi <action>"** vào `description` 3 skill (consult sớm nơi cost-of-miss cao nhất: mất NATS job / OOM render / ship consent sai).
- **(A3·REC-SP-10·Implemented)** surface **skill-index** trong orient (glob động, pointer thuần, **không** auto-invoke). guard.test.sh khi đó **44 assertion** (audit 2026-06-23 mở rộng đáng kể — số hiện hành xem chính `tests/harness/guard.test.sh`, đừng cite số cứng ở đây để khỏi drift).
**Lộ trình kế (chờ, scope sau):** B1 plan-template kiểu writing-plans (Global Constraints verbatim + Interfaces Consumes/Produces + no-placeholder + self-review-vs-spec) · B2 two-stage review verdict (spec-compliance + code-quality) + hand-off diff bằng **FILE** không paste · B3 ledger `active-context.md` git-anchored · B4 mục authoring + **fix drift** (`when_to_use` vs `description`; chuẩn "description = WHEN không phải WHAT") · B5 mục tie-break precedence · gộp **REC-SP-08→REC-37** (version runtime đọc `plugin.json`) · thu REC-SP-02/05/06. **Loại (verify đối kháng vs file thật):** **REC-SP-11** mandatory-skill apparatus ("1%"/"not negotiable" — nâng tier yếu nhất thành law, đảo ADR-021) · **REC-SP-07** subagent carve-out (canh injection path không tồn tại — Task-subagent không nhận SessionStart; lo ngại thật = REC-36) · mandatory-worktree-mọi-task (đụng REC-32) · multi-agent-cho-code-writing (ADR-021/023) · plugin/marketplace polyglot đa-harness (Lumin chỉ Claude Code) · nhét full methodology vào SKILL.md body (phá pointer-not-restate). **Mọi thay đổi giữ TEST là gate; không LLM-judge mới.** Chi tiết: `agent-harness.md` + `agent-harness-learnings-superpowers.md`. **Trạng thái:** A1/A2/A3 Implemented; B1–B5 + gộp C chờ.

### ADR-026 — Đánh giá MemPalace: xác nhận no-vector + khoá lộ trình memory B→A→C→D · Accepted (user 2026-06-23)
Từ đánh giá framework **MemPalace** (AI-memory open-source — vector retrieval trên log hội thoại verbatim, cấu trúc wings/rooms/drawers, embeddings on-device; run wf_4adbe0c0, 10 reader song song → 16 ý → verify đối kháng từng cái vs file thật; chi tiết memory `lumin-agent-harness` + `agent-harness-learnings-r2.md` §C/§F). **Kết luận: ~0% hạ tầng transfer** — Lumin không có similarity-retrieval, memory là markdown thủ công đọc-nguyên-khối scope bằng path-glob; thêm vector DB đảo ADR-021 (minimal-infra) + chất tải GTX 1060 (ADR-007/009). MemPalace tự hạ uy tín headline: issue #39 nhận "170-token wake-up" thực ~810, và `--mode raw` (bỏ lâu đài) là cấu hình mạnh nhất ⇒ lift 60.9→94.8% là category-mismatch, không phải bằng chứng cho Lumin. **Giá trị thật = (a) đối chứng độc lập xác nhận hướng grep-first/no-vector của Lumin đúng, (b) chỉ thẳng các chỗ Lumin phát biểu nguyên lý "route-to-scope-rồi-mới-đọc / pointer-not-restate" nhưng tự vi phạm.** Giữ nguyên advisory→deterministic + single-thread-cho-code + LLM-judge-không-bao-giờ-gate + minimal-infra; **không** thêm gate mới, **không** đổi kiến trúc. Chỉ khoá **thứ tự thực thi 4 lane advisory/doc/test** (đều đã ở registry learnings-r2):
- **B (REC-20 · giá trị cao nhất) — pre-compact bắt "reasoning-at-discard", không chỉ skeleton git.** `pre-compact.sh` (REC-19) hiện snapshot plan/4-luật/changed-files; thiếu phần GIỮA giàu tin (why-X-rejected, dead-ends, constraint vừa phát hiện) — rơi âm thầm ở `/compact`. *Làm:* append block timestamp (changed-files + ADR-id chạm + free-text "why/dead-ends") vào `docs/active-context.history.md` **append-only · cold storage · session-start KHÔNG đọc** (giữ single-volatile-scratchpad); +assert append-only (line-count không giảm vs HEAD). Phần git-anchored inert tới Phase-0 init.
- **A (REC-38 · rẻ nhất, không phụ thuộc) — budget tier always-on bằng số thật + guard cliff.** MemPalace 170→810 token là cùng bệnh "giả định budget chưa đo": `session-start.sh` cap `head -c 3000` **đo byte chưa đo token**; `active-context.md` đang **sát cliff 40 dòng** `sed -n '1,40p'` — một edit nữa tràn câm. *Làm:* assertion `guard.test.sh` chặn `active-context.md ≤ 40 dòng` + đảm bảo 4-luật-literal + dòng Focus **không bao giờ là phần bị cắt** khi active-context chạm trần. Deterministic-tier (test), không hook token-count.
- **C (REC-28) — insight store, format "dòng diary".** Tier giữa (durable-nhưng-non-binding) chưa có chỗ sống. *Làm:* `## Insights (non-binding)` vào 3 SKILL.md, 1 dòng/insight `date | trigger | finding | mitigation | nguồn`, surface qua description-match sẵn có (~0 token khi không chạm). **Không** hook/gate (ADR-025); muốn binding vẫn lên ADR; cap entry tránh thành MEMORY.md thứ hai.
- **D (REC-39) — luật memory-scoping: auto-memory + learnings topic-slice & pointer-surface, KHÔNG nuôi thành monolith load-nguyên-khối.** Lõi đã-deflate của scope-then-search/thin-index. Kẻ vi phạm: `MEMORY.md` + `lumin-agent-harness.md` (9.3KB) bơm mỗi phiên, ~52KB learnings kéo nguyên file khi trích một REC. *Làm:* `session-start.sh` dùng index 4-dòng `MEMORY.md` làm **scope-router**; tách `lumin-agent-harness.md` thành "current state" gọn + đuôi provenance on-demand. Convention-only, không infra.

**Thứ tự khoá: B→A→C→D** (theo giá trị); A không phụ thuộc gì nên có thể làm mechanically trước nếu cần win nhanh. **Sản phẩm (watch, KHÔNG build):** customer-memory inbox-assistant = scope-then-search **keyword-first** (socialHandle/SĐT/mã-đơn = exact id; LongMemEval của chính họ cho thấy keyword thắng embeddings cho id chính xác; quy mô 1 shop ⇒ Postgres exact+trigram+recency là cả sản phẩm) — ghi design-note REC-29, hard-gate ADR-011 (assistive-only, data từ BFF, **không bao giờ** scrape Meta DOM) + PDPL (vn-compliance: consent/retention); embeddings nếu có phải tính/lưu **local/in-country** mặc định. **Đừng build trước khi tool inbox-assistant tồn tại.** **Loại (verify đối kháng vs file thật — false analogy):** (1) vector DB/ChromaDB/embeddings/taxonomy wings-rooms-drawers — không có similarity-retrieval để gắn. (2) Pluggable storage-backend contract — trừu tượng hoá vector store không tồn tại; ethos "local-first + 1 escape hatch" đã xong ở ADR-005/014/016. (3) Namespace isolation + marker file — giải multi-tenant; Lumin single-dev/single-tenant ⇒ thêm failure-mode cho mối đe doạ bất khả; xoá PDPL bằng FK scoping `Customer.id`, không namespace. (4) Verbatim-by-default phía sản phẩm — ngược PDPL data-minimization + scrape/lưu Meta DOM phạm ADR-011 (rủi ro ban sống còn). (5) On-device-embedding-for-privacy phía harness — không có retrieval để "làm local"; posture all-home đã vượt mặc định MemPalace. **Meta-risk:** bẫy lớn nhất là nhập *từ vựng* MemPalace (wings/drawers/vector/diary) vào harness đã giải cùng bài toán deterministic — mọi lane trên **bóc hạ tầng, tái dùng cơ chế Lumin** (path-scoped rules, skill description-match, pre-compact, guard.test.sh, `MEMORY.md` index). **Trạng thái:** REC-20/28 cập nhật + REC-38/39 thêm mới trong `agent-harness-learnings-r2.md`; cả 4 chờ implement theo thứ tự B→A→C→D. Chi tiết: `agent-harness-learnings-r2.md` §C/§F + memory `lumin-agent-harness`.

### ADR-027 — Vòng cải tiến workflow giao-PR (visual-fidelity · risk-routing · spec-sync · plan-drift) · Accepted (user 2026-06-24)
Từ vòng nghiên cứu workflow human↔Claude **giao một PR trọn vẹn** (run wf_00bf70da): 5 reader best-practice ngoài — Anthropic *explore→plan→code→commit* + "give Claude a check it can run" + **verify-UI-visually** + adversarial-review · spec-driven (Spec Kit/Kiro/Thoughtworks: *spec KHÔNG executable*) · TDD/property/mutation với AI · PR hygiene · context-engineering — rồi **3 lăng kính phản biện** (over-engineer / redundancy / solo-feasibility) đối chiếu file thật. **Kết luận: tầng harness Lumin đã CHÍN hơn phần lớn best-practice ngoài** (explore→plan→code→commit = plan mode; test-as-gate = `verify-before-stop`; adversarial-review = `spec-guardian`; "/clear sau 2 lần" = ADR-021; dead-ends pre-compact = ADR-026 lane B; GPU dead-ends = `conventions §3D-upload`; mutation kill-gate OSM = ADR-024). Lỗ hổng thật **nổi bật duy nhất:** có nửa "check hành vi" (test) nhưng **thiếu nửa "check thị giác"** — shop design-heavy (hi-fi `.dc.html` + tokens) mà không gì đóng vòng design-fidelity. **Giữ nguyên** advisory→deterministic (ADR-021) · single-thread-cho-code · LLM-judge-không-bao-giờ-gate · minimal-infra; **KHÔNG thêm gate mới** (mọi thứ ở tầng advisory/doc/test).

**Adopt:**
- **Visual-fidelity (lỗ hổng #1):** convention — màn UI, khi app chạy được (skill `verify`), chụp 1 screenshot mobile+desktop rồi **đối chiếu mắt thường** với `.dc.html` mở cạnh, liệt kê lệch spacing/màu/type vào evidence/PR. Advisory; **KHÔNG** baseline-PNG/pixel-diff-tooling/Stop-hook pixel-gate (brittle, solo bỏ ngay). `conventions §Visual-fidelity`.
- **Risk-routing + evidence:** `verify-before-stop` phát additionalContext **non-blocking** (gộp một JSON với REC-02, như REC-33) khi diff chạm path tiền/state/auth/STK/migration/outbox → "review từng dòng" + nhắc diff-size. Là **bộ định-tuyến chú-ý-người**, KHÔNG auto-approve (owner luôn merge). Self-no-op trước Phase-0. PR body dán evidence (test cmd+output, lint, screenshot) — `.github/PULL_REQUEST_TEMPLATE.md`.
- **plansDirectory + plan-drift:** `settings.json.plansDirectory=./docs/plans` (plan-mode artifact sống sót `/clear`+compaction); `spec-guardian` thêm soi **diff vs plan đã duyệt** (mục chưa làm + file ngoài scope).
- **spec-sync trước merge:** hành vi lệch `spec.md`/`acceptance.md` → sửa **cùng PR**; `spec-guardian` WARN khi diff chạm order-state/money/checkout mà spec/acceptance không đổi.
- **EARS-per-feature:** lúc duyệt plan, hành vi có invariant thật → append 1-3 dòng EARS + test-id vào `acceptance.md` (feed ledger sẵn có).
- **Scope & branch:** 1 PR = 1 trục (feature XOR refactor), target <~400 dòng (>800 tách tuần tự); trunk-based + nhánh ngắn (merge ~một ngày) + squash-merge; cite anchor (ADR/§/acceptance-id) trong commit backbone (norm doc, KHÔNG lint).
- **Surface sẵn có:** "/clear sau 2 lần sửa sai" (ADR-021) đưa vào `conventions §Session hygiene`; interview-up-front + `/clear` tuỳ-chọn là **trigger phán đoán** khi blast-radius chưa rõ (KHÔNG ép mọi lần chạm tiền).

**Hoãn về Phase-0 ARM (`plan.md`) — là CODE chưa dựng được (`packages/core` money chưa tồn tại):**
- **REC-M mutation→money:** mở `osm-mutation.test.sh` sang money (formatter + `sum==total`): mutant đổi±/off-by-one/bỏ-grouping **phải** làm `money.*` đỏ.
- **REC-E property-test backbone:** money round-trip `parse(format(n))==n` + khớp regex ₫ · `sum(parts)==total` ngẫu nhiên · replay `statusHistory`→status qua **chỉ** transition hợp lệ (fast-check/gopter). Cap đúng vài cái.

**Loại (verify đối kháng vs file thật + chống over-engineer solo):** spec-as-executable / auto-regen-từ-spec (Thoughtworks: verification ở test) · reviewer SaaS ngoài CodeRabbit/Graphite (`spec-guardian` + `/code-review[ ultra]` đã phủ; bolt-on thêm cost + nhiễu false-positive) · auto-approve/auto-merge "low-risk" (reviewer LÀ owner; risk-tag chỉ định-tuyến) · folder OpenSpec `specs/`+`changes/`+archive (constitution = decisions/conventions đã có; cross-cutting dùng block spec-delta 3 dòng trong plan) · mutation toàn-repo / full test-pyramid (scope `packages/core` + differential) · pixel-diff làm Stop-hook gate (brittle) · **confirm-RED auto-stash** (trùng mutation kill-gate + template RED→GREEN §4; stash-giữa-Stop-hook mong manh → dạy thêm `.skip-verify`; REC-M deterministic hơn). Chi tiết: memory `lumin-agent-harness` + script run wf_00bf70da. **Trạng thái:** adopt Implemented; 2 ARM-item chờ Phase-0 code.

### ADR-028 — Data layer: golang-migrate · native ENUM · sqlc/pgx (no-DB gate) · Accepted (user 2026-06-26)
Quyết định cho Core slice 2 (data layer). Plan đầy đủ + sub-PR: [`docs/plans/core-data-layer.md`](plans/core-data-layer.md) (run wf_0952e60c-e3d).
- **Migration tool = golang-migrate** (flat `NNNNNN_name.up.sql`/`.down.sql`). Lý do: driver pgx/v5 native, một static binary, plain SQL **kiêm luôn** schema-source cho sqlc, khớp gated one-shot deploy job (`operations.md §4`). **Loại:** goose (thêm Go dep), Atlas (declarative/HCL lệch "plain-SQL = schema"), GORM auto-migrate (đã loại ADR-004). `sqlc.schema` trỏ `db/migrations/*.up.sql` (**up-only** — glob cả `.down.sql` sẽ nuốt DROP, vỡ codegen + gate).
- **Enum đóng = native Postgres ENUM** (order_status/channel/role/payment_method/product_status/option_type/review_status/print_stage/consent_scope/consent_channel) — **byte-identical** với `internal/order` + `packages/core` (hợp đồng OpenAPI, ADR-003). **EXCEPTION:** `product_material` mở (`PLA·PETG·recycled-PLA…`, spec §02) → **TEXT+CHECK** (né ALTER-TYPE friction). sqlc `overrides` map cột enum về **type `internal/order` sẵn có** (một type Go, không sinh trùng) — áp dụng khi cột enum land (PR-2c+).
- **Gate = `sqlc vet` + `sqlc diff`** trong `make verify-go` (**no-DB**: vet = query↔schema compile; diff = code-gen không stale). `sqlc/db-prepare` (prepare vs Postgres thật) **HOÃN** tới khi CI cấp Postgres. **testcontainers** (ADR-020) **HOÃN sang PR-2b** (test integration đầu tiên có DB thật; máy nhà chưa có Docker daemon → giữ PR-2a verify được offline). Pin: **pgx v5.7.5** (giữ `go 1.23`; v5.10 ép go 1.25), **sqlc CLI v1.30.0**.
- Khoá theo ADR cũ (không relitigate): statusHistory = jsonb-on-orders (ADR-004), money int8 VND server-authoritative (ADR-019), address province→ward→street (ADR-017), PDPL consent append-then-mark, outbox tx-insert helper `pgx.Tx` first-arg (ADR-006). **ARM:** `guard.test.sh §ARM-GUARD` soi `'sqlc vet'` trong THÂN recipe verify-go + testcontainers real-check (arm khi land PR-2b).

### ADR-029 — Relay outbox→NATS: goroutine in-process · quét-tập-pending theo seq · publish→PubAck→mark · Accepted (user 2026-06-26)
Quyết định cho Core slice 3 (HTTP + relay). Plan đầy đủ + sub-PR: [`docs/plans/core-http-relay.md`](plans/core-http-relay.md) (run wf_48252601). Slice-3 dựng relay phát dòng `outbox` `status='pending'` (slice-2 tích) lên NATS JetStream. **Quyết định:** relay là **một goroutine in-process trong core-api** (KHÔNG binary `cmd/relay` riêng — durability nằm ở dòng đã-commit; tách binary không thêm an toàn mà thêm deployable/conn/lifecycle; ADR-009 minimal + `architecture.md` "core-api phát job qua outbox→NATS"). **Quét cả TẬP pending `ORDER BY seq`** mỗi tick (~1s) — **KHÔNG watermark `seq>cursor`, KHÔNG `SKIP LOCKED`**: `bigserial seq` gán lúc INSERT chứ không lúc COMMIT → tx seq-thấp có thể hiện ra SAU tx seq-cao → watermark bỏ sót vĩnh viễn dòng seq-thấp commit-muộn = **mất event tiền câm** (hazard lớn nhất). Single instance (ADR-009) ⇒ KHÔNG advisory-lock/leader-election. **`publish → await PubAck → mark-published`** (không mark-rồi-publish); `Nats-Msg-Id = outbox.id` (dedup server-side trong duplicate-window); **subject = `event_type` nguyên văn** (relay không tra cứu). **Transient-vs-poison:** lỗi connection → để `pending`, **không đốt `attempts`**; PubAck reject → `attempts++` → `failed` sau `RelayMaxAttempts` (cô lập poison, không chặn head-of-line). Provision 2 stream `ORDERS`(`order.>`,Limits)+`ASSET_JOBS`(`asset_job.>`,WorkQueue); **worker tự sở hữu durable consumer** (AckWait/MaxDeliver→DLQ là knob worker, ADR-007). Crash-recovery: dòng pending bền → restart re-scan; re-publish trùng bị dedup hấp thụ. Relay chỉ ĐỌC dòng đã-commit (`EnqueueOutbox(tx,…)` vẫn là writer duy nhất). **Loại:** binary riêng · watermark/SKIP-LOCKED · advisory-lock · relay hard-code consumer knob.

### ADR-030 — Auth: JWT tự-phát (core-api own login) thay vì Cloudflare Access + RBAC owner/staff · Accepted (user 2026-06-26)
ADR-003 đặt tên `go-chi/jwtauth` nhưng chưa chốt cơ chế; `users` chưa có cột mật khẩu. **Quyết định (user chọn self-issued, KHÔNG dựa Cloudflare Access):** core-api **tự xác thực** — `POST /auth/login` (email+password) → verify `bcrypt` (`golang.org/x/crypto/bcrypt`; x/crypto promote indirect→direct) → phát JWT ký (`go-chi/jwtauth`, *nhất quán* ADR-003) → cookie **httpOnly+Secure+SameSite** (apps/admin là Next SPA → token ngoài tầm JS, chặn XSS-theft); `POST /auth/logout` xoá cookie. `users` thêm `password_hash` (migration `000009_user_credentials`). Middleware verify JWT trên `/admin/*` map `sub`(users.id)→`users.role`→`order.Role`, inject actor `{ByUser=users.id, Role, At=server-clock}`. RBAC middleware owner/staff + `requireOwner` cho cạnh owner-only (reconcile→PAID, →REFUNDED, `PATCH STK`) — **phòng-thủ-tầng** trên guard `internal/order` (guard vẫn authoritative; KHÔNG re-implement RBAC math). **Cổng channel `POST /orders`:** `web` public; `channel=inbox` đòi actor staff/owner (optional-auth mw — inbox đẻ đơn born-PAID không chứng từ; conventions §17/ADR-012 §49). **Guest web genesis** `StatusEvent.ByUser="customer"` (sentinel; ngoại lệ duy nhất của ByUser=users.id). **Chốt trong PR-3e:** seed owner đầu (bootstrap-hash-phải-xoay vs `make seed-owner` CLI), TTL/refresh token, JWT secret qua env, màn login mỏng apps/admin. **Ngoài scope:** auth khách storefront + Pet Tag `Account` (Phase-1, identity khác). **Mở (không chặn):** handshake auth extension MV3. **Loại:** Cloudflare-Access edge-trust (chọn tự-chủ, không phụ thuộc cấu hình CF Access) · session server-side state (chọn JWT stateless cookie).

### ADR-031 — OpenAPI: yaml viết tay = single-source → oapi-codegen `strict-server` + openapi-typescript · Accepted (user 2026-06-26)
ADR-003 bắt OpenAPI là hợp đồng Go↔TS nhưng chưa có spec/`packages/api-client`; `packages/core` Zod là hợp đồng de-facto. **Quyết định:** **một `services/core-api/openapi.yaml` viết tay = nguồn chân lý** → `oapi-codegen` (Go, **mode `strict-server`** — conformance handler↔contract lúc-compile) + `openapi-typescript`/`openapi-fetch` (TS, gói mới `packages/api-client`). **Test parity 4 chiều** (OpenAPI == `internal/order` == `packages/core` Zod == PG enum) chặn nguồn-thứ-hai trôi. Codegen chạy trong verify/CI; `*.gen.go` commit + **path-exclude trong `.golangci.yml`** (output oapi-codegen đụng golangci v2 0-issue). Đổi Review `text`→`body` (+ sync `spec.md §02`). `Order` DTO công khai dạng **nested** (không phải sqlc row phẳng); prop camelCase; tiền `int64`. **Loại:** types-only (chọn strict-server cho drift-protection mạnh hơn) · sinh spec từ code · giữ Zod làm nguồn riêng.

### ADR-032 — HTTP error envelope `{code, messageKey, fields?}` + bảng domain-error→status · Accepted (user 2026-06-26)
Hôm nay chỉ `writeJSON` + `{"error":"internal"}` tạm. **Quyết định:** một envelope chuẩn `{code, messageKey, fields?}` + **bảng ánh xạ tường minh** domain-error→HTTP-status: `db.ErrNotFound`→404, `ErrNoItems`→422, `ErrInvalid*`/`money.ErrInvalidAmount`→400/422, `order.TransitionError.Code`: `INVALID_EDGE`→409 / `RBAC`→403 / `REASON_REQUIRED`|`REFUND_PROOF_REQUIRED`|`PROOF_REQUIRED`→422 / `INVALID_ACTOR`|`INVALID_TIMESTAMP`→400. `TransitionError.Message` tiếng Việt **server-internal** — KHÔNG bao giờ qua wire (always-must #3 i18n: client map `messageKey`→chuỗi next-intl). Ghi thành quyết định (không improvise per-handler) vì cả 3 client TS tiêu thụ giống nhau.

### ADR-033 — POST /orders idempotency: HOÃN sang slice sau · Accepted (user 2026-06-26)
Order id sinh per-request → `POST /orders` retry đẻ **đơn trùng + `order.created` trùng** (lỗ thật đường tiền). **Quyết định (user chọn HOÃN):** slice-3 KHÔNG dựng `Idempotency-Key`/bảng dedupe; chấp nhận rủi ro retry-dup tới slice sau (revisit khi storefront-checkout đẻ retry thật; ghi rõ ở plan §Out-of-scope). **Vẫn làm slice-3 (độc lập idempotency):** mã `#LMN-xxxx` = Postgres sequence (3f, migration `000010`) + biên host-trust `paymentProofUrl` (3g). **Caveat end-to-end:** `POST /orders` (web) **nhận** `paymentProofUrl` đã-validate nhưng endpoint presigned-PUT đẻ URL đó HOÃN (ADR-005) → luồng browser→upload→order chưa chạy trọn slice này.

### ADR-034 — Guest order-lookup rate-limit: token-bucket in-process keyed by order CODE (không per-IP, không failure-lockout) · Accepted (user 2026-07-03)
conventions §Bảo mật đòi "token-bucket trong Go + lockout chống dò" cho `/orders/lookup` (PR-P1-n) nhưng không chốt CÁCH. **Quyết định (user 2026-07-03):** limiter **in-process in-memory** (`golang.org/x/time/rate` per-code bucket + lazy TTL-sweep giữ map bounded; reset khi restart — chấp nhận theo ADR-009/014), **KEY theo mã đơn (`code`), KHÔNG per-IP** — vì slice này core-api **chưa có client-IP tin-cậy** (CF-Connecting-IP hoãn tới edge-integration; chi `RealIP` giả-mạo-được). Phân vai defense-in-depth: **CF WAF** chặn sweep per-IP xuyên-mã; **token-bucket per-code** chặn brute-force SĐT của một mã đã biết (0.5/s → không gian SĐT 9-chữ-số bất-khả-thi). **KHÔNG failure-lockout theo mã:** mã đơn tuần-tự/đoán-được (`#LMN-1000,1001,…` từ sequence 000010) + lockout kiểm-tra TRƯỚC khi so SĐT → attacker gửi N-SĐT-sai khoá mã → **chủ đơn thật (đúng mã+SĐT) bị 429 khỏi đơn của chính họ** (DoS; adversarial review wf_4ef2b511). Token-bucket đơn thuần đã đủ chống brute-force nên lockout chỉ thêm rủi ro. **Env-knobs (rate/burst/ttl) HOÃN sang P1-o** (cadence auto-poll chưa chốt, plan §6.6) — hằng-số package tạm, redeploy re-tune. **Loại:** per-IP keying (không có IP tin-cậy in-process) · failure-lockout theo mã (DoS chủ đơn) · store DB-backed (thừa cho single-instance accept-downtime) · Retry-After trên 429 (rò cửa-sổ).

### ADR-035 — Payment-proof upload: presigned POST to dedicated Garage bucket + host-pinned URL + 90-day terminal retention · Accepted (user 2026-07-05)
Quyết định cho Phase 2 P2-c (`docs/plans/phase-2-checkout.md` D-P2-2): ảnh biên lai CK là dữ liệu cá nhân/tài chính phục vụ đối soát thủ công, nên **không proxy qua core-api/Cloudflare Tunnel**. Storefront gọi `POST /checkout/payment-proof-upload` để lấy **presigned POST** S3/Garage ngắn hạn (TTL mặc định 5 phút), rồi browser upload trực tiếp vào bucket riêng `lumin-payment-proofs`. **POST, không PUT:** policy POST mới chặn được `content-length-range` (≤10MB) + exact `Content-Type` (`image/jpeg|png|webp`) ở phía S3; PUT chỉ tin header client khai. Core-api tự sinh key ngẫu nhiên không chứa PII, trả `finalUrl` **host-pinned** từ cấu hình bucket public/CDN; browser chỉ được gửi `finalUrl` đó vào `POST /orders.paymentProofUrl`. Bucket cần CORS cho storefront origin (POST + form headers) và credentials S3 bucket-scoped. **Retention:** giữ ảnh khoảng 90 ngày sau khi đơn vào terminal để đủ xử lý tranh chấp/đối soát, rồi xoá bằng lifecycle/GC bucket (PDPL data-minimization); orphan upload GC là follow-up nếu tích tụ thật. **Loại:** upload qua core-api (tốn tunnel/body, tăng rủi ro log/proxy) · presigned PUT (không enforce size policy) · bucket chung asset public (retention/PII lẫn với model/sprite) · client tự dựng URL cuối (mất host-pin).

### ADR-036 — Model upload: single presigned POST ≤100MB (KHÔNG S3 multipart) → refine ADR-005 · Accepted (user 2026-07-11)
Quyết định cho Phase 3 P3-j-b (`docs/plans/phase-3-admin.md` D-P3-5): admin editor tải model nguồn (.glb/.stl/.3mf) lên Garage bằng **một presigned POST** (cùng cơ chế `minio-go` `PresignedPostPolicy` như ADR-035 ảnh biên lai), **cap ≤100MB** — KHÔNG dùng S3 multipart. Đây là **refine** ADR-005 (vốn ghi "multipart presigned-PUT, part <100MB") vì ba lý do phát hiện khi implement: (1) `minio-go` v7 **không** presign được từng part multipart (high-level API không expose → phải thêm dep `aws-sdk-go-v2` + 3 endpoint initiate/parts/complete + client ETag orchestration); (2) 100MB là trần body của Cloudflare Tunnel cho **một** request, mà một model web-ready (.glb đã decimate) nằm thoải mái dưới ngưỡng; (3) POST-policy enforce `content-length-range` + exact `Content-Type` phía S3 (PUT chỉ tin header client khai). Server tự sinh key ngẫu nhiên PII-free `models/YYYY/MM/DD/<uuid>.<ext>` trong bucket **công khai riêng** `lumin-assets` (tách khỏi bucket biên lai riêng-tư/PDPL của ADR-035 — model là asset **vĩnh viễn**, world-readable qua CDN, **KHÔNG** có retention sweeper), trả `finalUrl` **host-pinned**; browser upload thẳng browser→Garage (core-api không proxy body qua tunnel). `POST /admin/products/{id}/asset-jobs` **host-pin** `sourceModelUrl` (chỉ URL do store này phát — foreign/spoofed → 400) trước khi enqueue `CreateAssetJobTx` (job row + outbox `asset_job.created` cùng tx — publish-on-commit ADR-006). Cả hai owner-only (spec §08 catalog = quyền owner). **Trần đã biết + lối nâng cấp (`ponytail:`):** model nguồn >100MB (STL high-poly thô) không upload được → chủ decimate trước, hoặc nâng lên S3 multipart (khi đó thêm `aws-sdk-go-v2`; transport browser giữ nguyên), hoặc bật bucket R2 nếu vướng ToS file lớn của CF (lối thoát ADR-005). **Loại:** multipart-ngay (minio-go không presign part; thêm dep + 3 endpoint + orchestration cho một class kích thước có thể không bao giờ xảy ra) · presigned-PUT (không enforce size/type phía S3) · content-hash key ngay (client phải hash trước khi biết key; uuid + bucket write-once đã cho bất biến per-object — hash chỉ để dedup, chưa cần) · bucket chung với biên lai (lẫn retention/visibility).

### ADR-037 — Product configurator: màu theo bộ-phận + lựa-chọn theo option (capture jsonb bồi thêm) · Accepted (user 2026-07-11)
Quyết định cho epic mở rộng editor sản phẩm (`docs/plans/p3l-product-editor.md`, design screen 4): sản phẩm có **bộ phận (part) có tên** (Chao đèn/Đế/Nút bấm), mỗi bộ phận có **bộ màu riêng** — khách chọn **một màu mỗi bộ phận**; và option `choice` mang **lựa chọn liệt kê** (S/M/L, mỗi lựa chọn có mô tả + priceDelta riêng) — khách chọn **một lựa chọn mỗi option**. Trước đây `Color`/`Option` phẳng theo product, `order_items` chỉ giữ một `color_id` + `option_ids` phẳng. **Chốt (bồi thêm, KHÔNG phá):** (1) **Catalog:** bảng mới `parts(id,product_id,name,display_order)` + cột `colors.part_id` **nullable** (NULL = màu phẳng/legacy, đi đúng đường pricing cũ; set = thuộc một bộ phận — theo oracle, nullable rẻ hơn "uniform-parts" vì KHÔNG cần migrate/backfill product cũ); bảng mới `option_choices(id,option_id,label,description,price_delta,display_order)`. Option `choice` KHÔNG có `option_choices` → giữ toggle+`price_delta` như cũ; CÓ → giá lấy từ lựa chọn đã chọn (option base bỏ qua — **một nguồn giá**). (2) **Capture (lineage ADR-004 "jsonb, không child-table"):** `order_items` thêm `part_colors` + `option_choices` jsonb, **denormalized** (nhét sẵn `colorName`/`hex`/`choiceLabel` lúc capture → admin/print đọc không cần join mới); GIỮ `color_id`/`option_ids` cho đơn phẳng + **đơn lịch sử bất biến** (reader dual-path theo sự-hiện-diện; KHÔNG bao giờ ghi đè snapshot). (3) **Pricing (đường tiền — server, 422 nếu vi phạm):** đúng **một màu mỗi bộ phận**, đúng **một lựa chọn mỗi choice-option**, và **màu ∈ đúng bộ phận nó khai** (chặn cross-charge); `unit = base + Σ màu-delta + Σ choice-delta + text-delta`, overflow-checked; sentinel `Err*` mới per-group. (4) **Print:** GIỮ **một job / order_item** (ADR-007 concurrency=1 — đèn 3 màu là **một** lượt in vật lý, không phải 3 job); schema `print_jobs` **không đổi**; `ListPrintQueue` thêm `oi.part_colors` vào SELECT (join sẵn tới order_items). (5) **Admin `ListOrderItems`** đọc jsonb off `oi.*` (join màu-đơn cũ giữ cho row legacy). (6) **Hợp đồng chung:** `@lumin/core` `OrderItemSchema` + openapi (`Product.parts`/`Color.partId`/`Option.choices`/`OrderItem.partColors|optionChoices`) + regen — client/server validate cùng shape. (7) **Timeline công khai KHÔNG đổi** (ADR-032 — không lộ item/màu ra guest). **Hệ quả:** migration mới (>000014, cấp số lúc land — monotonic, memory `lumin-migration-numbering-monotonic`); `pricing.PriceItem`/`Selection` mở rộng per-group membership; storefront config-UI + `cart.ts` cartLineKey (canonical-sort 2 trục mới, chống dòng-giỏ ma) + admin order-detail cập nhật. **KHÔNG backfill catalog · đơn cũ + product phẳng chạy nguyên.** Đảo được: bỏ `parts`/`option_choices` → về phẳng (cột jsonb nullable). **YAGNI:** "chọn part → sáng trên model 3D" chỉ là state viewer keyed theo `part_id` — KHÔNG kéo dữ liệu geometry/zone vào catalog ở ADR này. **Loại:** child-table cho capture (phá grain ADR-004, nổ surface pricing/admin/print); uniform-parts (thêm migration + synthetic default-part + heuristic "gộp khi 1 part" = dual-mode trá hình); fan-out print theo part (phá 1:1 order_item↔card, sai vật lý, vi phạm ADR-007).

### ADR-038 — Model 3D: góc xem mặc định (camera pose) shop lưu — jsonb bồi thêm, worker không đổi · Accepted (user 2026-07-11)
Stage 3 của epic editor (`docs/plans/p3l-product-editor.md`): design (Admin Hi-fi screen 5, khối "Model 3D — căn chỉnh hiển thị") cho shop **xoay / chọn mặt mặc định / chỉnh kích thước hiển thị** rồi **"Lưu góc mặc định" = đúng góc khách thấy khi mở app**. **Bối cảnh nền (đã kiểm):** worker `model_ingest` ĐÃ **chuẩn-hoá geometry** (recenter/rescale/reorient về canonical pose — comment `000006_jobs.up.sql:9` + `architecture.md`), và storefront `model-3d-viewer.tsx` ĐÃ **auto-frame** (chỉ đặt `camera-controls`, KHÔNG hardcode `camera-orbit`) → **NULL = auto-frame vẫn đúng**. Nên góc-shop-lưu là **curation bồi thêm** (chọn ấn tượng đầu), KHÔNG phải sửa lỗi hiển thị. **Chốt (user chọn "full pose", additive, KHÔNG phá):** (1) **Persist:** cột mới `products.model3d_view` **jsonb NULLABLE** = một **camera pose đầy đủ** `{orbitTheta, orbitPhi, orbitRadius, targetX, targetY, targetZ}` (deg · deg · % + 3 toạ độ mét), map **1:1** sang model-viewer `camera-orbit="θdeg φdeg radius%"` + `camera-target="x y z m"`. Pose **atomic** (đặt cùng nhau qua "Lưu góc mặc định") → **một cột jsonb**, không tách 6 cột (không query lẻ — đúng grain ADR-004). **NULL = chưa lưu → auto-frame** (bất biến). (2) **KHÔNG phải đường tiền:** metadata hiển thị → **float hợp lệ** (góc/khoảng-cách), KHÁC luật int-VND; không đụng pricing/CalcTotals. (3) **Write — endpoint owner-only riêng** `PATCH /admin/products/{id}/model-view` (khớp nút lưu-riêng của design + nếp P3-i single-col PATCH không clobber; **KHÔNG** nhét vào `ProductInput` — `UpdateProduct` đã cố tình loại field không-thuộc-form-lõi). Validate range **server = tường** → 400 field-map: `orbitTheta ∈ [-360,360]`, `orbitPhi ∈ [0,180]` (model-viewer clamp cực), `orbitRadius ∈ (0,1000]%`, `targetX/Y/Z ∈ [-100,100]m` hữu hạn (reject NaN/Inf). (4) **Read:** `Product.model3dView` **optional** trên `GET /admin/products/{id}` (editor prefill knobs) + storefront `GET /products/{slug}` (viewer đặt camera ban đầu) — additive, absent = auto-frame. (5) **Worker KHÔNG đổi:** pose do shop tạo, **tương đối canonical pose** worker; re-ingest canonical-deterministic → pose giữ hiệu lực; upload model MỚI → pose vẫn áp (camera pose độc lập geometry) — shop lưu lại nếu khung lệch. **KHÔNG** DELETE/clear ở stage này (`ponytail:` thêm "reset về auto-frame" nếu shop cần). (6) **Part-highlight TÁI KHẲNG ĐỊNH ADR-037 YAGNI:** "chọn part → sáng trên model" vẫn là **viewer-state keyed `part_id`**, KHÔNG persist geometry/zone. Stage 3 = **camera pose THÔI**. **Hệ quả:** migration mới (>000016, cấp số lúc land — monotonic, memory `lumin-migration-numbering-monotonic`); openapi +`Model3dView` schema + `Product.model3dView` + 1 path `PATCH .../model-view`; regen (api.gen.go + schema.gen.ts); storefront viewer đọc pose → set `camera-orbit`/`camera-target`; admin editor dựng knobs (Stage 5, tiêu thụ endpoint này). **Đảo được:** bỏ cột → về auto-frame. **KHÔNG backfill · SP cũ = NULL = auto-frame · SP phẳng chạy nguyên.** **Loại:** preset+scale-only (bỏ xoay tinh X/Y/Z design cho — user loại); nhét view vào `ProductInput` (design lưu-riêng + tránh gửi cả ProductInput chỉ để lưu góc); 6 cột rời (pose atomic → jsonb); worker tự đoán góc (worker chuẩn-hoá pose, KHÔNG đoán ý-đồ-shop — góc là curation của người).

### ADR-039 — Vật tư & chi phí: costing engine (filament bình quân gia quyền + giờ máy khấu hao + chi phí phụ + hao hụt) + COGS snapshot chốt-lúc-IN · Accepted (user 2026-07-11)
Stage 4 của epic editor (`docs/plans/p3l-product-editor.md`; design Admin Hi-fi screen 8 — 4 tab Filament/Giờ máy/Chi phí phụ/Hao hụt + snapshot giá vốn theo đơn). Greenfield; **user chọn FULL costing engine** (plan §7 cảnh báo giữ nhỏ, nhưng user chốt full → ADR định nghĩa toàn bộ). Oracle-reviewed (money + state-machine + outbox). **Chốt (bồi thêm, KHÔNG phá):** (1) **Filament tồn + bình quân gia quyền (batches, KHÔNG moving-avg 2 cột):** `filament_materials` (palette shop-wide, mỗi màu-có-tên: `material` text-CHECK PLA/PETG/Resin/…, `unit` gram|ml, `hex?` guard CSS-injection như colors, `low_stock_threshold`, `archived`) — **tồn + ₫/đơn-vị DERIVE từ batches, không lưu**. `filament_batches` (mỗi lô "nhập cuộn": `qty_original>0`, `qty_remaining`, `total_cost_vnd` — ₫/đơn-vị-lô = total/original **derive**). Bình quân = `Σ(qty_remaining × total_cost/qty_original) ÷ Σ(qty_remaining)` tính **một biểu thức, numeric rộng, làm tròn int-VND CHỈ khi đóng băng** (không pre-round mỗi lô → tránh trôi số). Batches vì design VẼ panel breakdown per-lô — moving-avg 2 cột không dựng lại được. (2) **Ledger tiêu hao (print + scrap chung một bảng):** `filament_consumption` {`at`,`material_id`,`qty>0`,`kind`(print|scrap),`cost_vnd` (FIFO thực tiêu, đóng băng),`reason?`,`order_item_id?`,`product_name?`,`note?`}. Mỗi lần trừ → FIFO-decrement lô cũ nhất + ghi MỘT row. **Ledger là nguồn chân lý; `qty_remaining` = cache (original−Σconsumed) dựng-lại-được** (audit rebuild). Hao hụt tab = row kind=scrap; KHÔNG bảng scrap riêng. (3) **Định mức per-part (theo grain ADR-037, KHÔNG per-item):** `parts.est_filament_qty` (gram/part) + `products.est_filament_qty` (SP phẳng) + `products.est_print_hours` (giờ máy — per-item: một job = một lượt in vật lý, ADR-007 1:1) + `colors.filament_material_id` nullable FK (nối màu-SP → cuộn shop). Trừ-khi-in lặp `part_colors` (SP có part) / màu đơn (phẳng). **Ceiling (`ponytail:`):** một vật liệu/part; est-gram KHÔNG scale theo size/option ở v1 (nâng khi two-tone/size-costing cần). (4) **Trừ-khi-in = `AdvancePrintStageTx`:** refactor `AdvancePrintStage` (UPDATE autocommit) → Tx (nếp `CreateAssetJobTx`). Khi →PRINTING: **claim atomic** `UPDATE print_jobs SET stage='PRINTING', filament_deducted_at=now() WHERE id=$1 AND filament_deducted_at IS NULL RETURNING …` (0 rows + job tồn = đã trừ → chỉ set stage; row-lock serialize double-move) → helper **chung** `decrementFilament(material,qty,kind)`: FIFO-decrement + **clamp sàn 0, ghi qty THỰC + shortfall** (thiếu ≠ chặn — board staff lái ADR-012; ghi thiếu để không đầu độc mẫu số bình quân) + consumption row `cost_vnd`=Σ(gram-lô × ₫-lô) thực. 1:1 order_item↔job (ADR-007) giữ trừ sạch 1:1. (5) **COGS snapshot = chốt-lúc-IN, best-effort SAU commit, TUYỆT ĐỐI KHÔNG trên tx tạo đơn (oracle R1/R2):** made-to-order → lúc bán (PENDING_CONFIRM, trước cả khi CK về) chưa in gì → chốt-lúc-bán sai (bình quân cuộn CHƯA tiêu). Chốt lúc first →PRINTING (biết gram thực + ₫/lô thực). `order_items.cost_snapshot` **jsonb NULLABLE** {filamentVnd(=consumption.cost_vnd), machineVnd, wasteVnd, auxVnd, totalVnd, +inputs, at}. Filament portion **authoritative in-tx** (consumption.cost_vnd); rollup ghi **best-effort SAU commit** (lỗi→log+để NULL="chưa chốt", backfill được) → **bug costing KHÔNG bao giờ fail đơn đã trả tiền / chặn board / cuốn theo outbox order.created**. KHÔNG outbox (đó NATS cross-process ADR-006; đây same-DB không consumer — nếp best-effort như `printHub.broadcast`). Biên (read) = giá bán − totalVnd. "Đổi giá sau không sửa đơn cũ" (design) VẪN đúng — đóng băng lúc in. (6) **Giờ máy (khấu hao):** `machines` {name,`purchase_price_vnd`,`depreciation_months>0`,`expected_hours_per_month>0`,`is_primary`,`active`}. ₫/h **derive**=purchase/(months×hours). Snapshot dùng máy `is_primary` (printer-API auto-attribute máy-thực = Phase-5, ceiling). (7) **Chi phí phụ (phân bổ):** `aux_costs` {`label`,`kind`(per_order|per_month),`amount_vnd`}. Phân bổ/đơn=`Σper_order + Σper_month ÷ max(1, đơn-thực-30d)` (COUNT derive; **guard chia-0**: 0 đơn → phần per_month=0). Hệ số hao hụt(30d)=`Σqty[scrap] ÷ Σqty[print]` (scrap trên good-output, khớp cách design cộng %; **guard print=0→0**); COGS áp hệ số **cùng mẫu số** KPI (biên khớp dashboard). (8) **Money int-VND (ADR-019):** cột tiền `bigint NOT NULL CHECK(>=0)`; ₫/đơn-vị & ₫/h là **rate DERIVE** (không lưu → tránh làm tròn); đóng băng int CHỈ ở snapshot. Đường tiền **nội bộ** (giá vốn/biên) **TÁCH khỏi giá khách** (PriceItem server-authoritative không đổi — COGS không cộng vào cái khách trả). (9) **RBAC:** mutation Vật tư + định-mức trên catalog = **owner-only** (classify authOwnerOnly + assertOwner re-assert); đọc /vat-tu + snapshot = **owner+staff** (nếp settings/STK). **Hệ quả:** 3 migration (000018 materials+batches; 000019 consumption + cột định-mức/snapshot/deducted; 000020 machines+aux — >000017, cấp lúc land, monotonic memory `lumin-migration-numbering-monotonic`); refactor AdvancePrintStage→Tx; openapi + regen (api.gen.go + schema.gen.ts); badge "thiếu nhựa" P3-h thành THẬT (derive est vs tồn); FE 4-tab + snapshot card. **Đảo được:** drop bảng/cột → về không-costing (badge tĩnh cũ, order không snapshot). **KHÔNG backfill · đơn cũ cost_snapshot NULL · SP phẳng/không-định-mức bỏ qua trừ (skip sạch).** **Slicing (user-merge-gate mỗi PR):** 4a filament BE (materials+batches CRUD+nhập-cuộn+weighted-avg+low-stock) · 4b trừ-khi-in + định-mức-catalog + snapshot-lúc-in + badge (ops/tiền — oracle pass riêng) · 4c máy+phụ+hao-hụt BE + KPI/margin read · 4d FE /vat-tu 4-tab + snapshot card + dialogs. **Loại:** (a) snapshot chốt-lúc-bán (sai physics made-to-order — oracle R2, two divergent truths); (b) COGS trong tx tạo đơn (bug costing cuốn theo đơn đã trả — oracle R1); (c) moving-avg 2 cột (không dựng panel per-lô); (d) filament qua outbox/async (race board-PRINTING-nhưng-stock-chưa-trừ; đó same-DB không consumer); (e) một-gram-per-item (sai giá two-tone ADR-037 → per-part thay); (f) bảng scrap riêng (consumption kind=scrap đủ); (g) printer-API auto-giờ + forecast tồn (Phase-5 / §Đừng-làm ERP).

### ADR-040 — Pet Tag NFC: tách feature-plan + data model (3 bảng, tách khỏi OrderStatus, money-free) + 3 quyết định owner · Accepted (user 2026-07-13)
Feature NFC hoàn chỉnh (`docs/plans/pet-tag.md`; spec §10; design `Lumin Pet Tag - Hi-fi.dc.html`). **P3-t tách khỏi Phase-3** (open-Q #5 resolved): P3-t = feature-plan + cross-ref; build = slices **t-1..t-5 user-merge-gate riêng** (đừng để phình Phase-3, `plan.md` §7). **3 quyết định owner (2026-07-12):** **D1** trang pet phục vụ trên **app storefront path `/t/{shortId}`** (reuse Next app + tunnel; `lumin.pet` để dành → edge-rewrite sau, KHÔNG deploy riêng); **D2** ghi NFC qua **admin-mobile Web NFC** (Chrome-Android) + **fallback nhập tay `chip_uid`** (KHÔNG companion app/USB reader); **D3** kích hoạt **email-only** (reuse customer session P1-r; Google OAuth hoãn — tag auto-attach vào session nào mint, thêm provider sau không phá luồng); **D4** báo-lúc-lạc = **email + in-app** (ADR-013 email-first), `lost_events` lưu raw `{lat,lng}` + link OSM, reverse-geocode "{khu vực}"+static-map **hoãn** (`ponytail:` thêm khi cần). **Data model (t-1 BUILT — migration `000023_pet_tag`):** 3 bảng + 1 cột + 3 enum. (1) `product_type` enum(`standard`|`nfc_tag`) + `products.product_type` **DEFAULT 'standard'** (additive, mọi SP cũ = standard; đánh dấu SP cần cấp tag). (2) `pet_tags` {id, code#LMN-Txxxx uq, short_id uq=routing-key, order_item_id→order_items (KHÔNG unique — line qty=N đẻ N tag), status `pet_tag_status`(UNENCODED/ENCODED/ACTIVATED), chip_uid?, owner_account_id?→customers SET NULL, encoded_at?, activated_at?}. (3) `pet_profiles` {id, tag_id→pet_tags uq CASCADE, owner_account_id→customers CASCADE, handle uq, pet_name(1..40), species `pet_species`(dog/cat/other), breed?/age?/weight? text, gallery/favorites/medical/owner_contact/socials/theme/**blocks** jsonb, lost_mode default false, updated_at}. (4) `lost_events` {id, tag_id→pet_tags CASCADE, scanned_at, finder_location jsonb?, owner_notified_at?}. **Tách khỏi OrderStatus §04:** vòng đời tag (UNENCODED→ENCODED→ACTIVATED) **song song nhưng tách hẳn** state machine đơn (đơn vẫn PENDING_CONFIRM→…→COMPLETED) — **KHÔNG** đi qua guard `packages/core`, **KHÔNG** `statusHistory` (đây là DB-only lifecycle, không cross-surface → không nhét vào core). **MONEY-FREE:** tag bán qua Order/PriceItem thường; không cột tiền nào ở axis này. **Chuẩn hoá vs spec §10** (spec khái niệm, schema chuẩn hoá — nếp 000004 `social_handle` singular vs prose): (a) `ProfileBlock[]` = cột `blocks` **jsonb** (không bảng — blocks luôn tải cùng cả profile, y như gallery/socials/medical); (b) **KHÔNG** `pet_tags.profile_id` back-pointer — link 1-1 = `pet_profiles.tag_id UNIQUE` (né circular FK); (c) URL chip **derive** từ short_id (không lưu 2 lần; chỉ `chip_uid` = hardware UID phải lưu). **`owner_account_id` ở CẢ tags (set lúc login 2a, trước khi có profile) lẫn profiles (chủ trang)** — set khác thời điểm, owner bất biến nên không lệch. **PDPL (skill vn-compliance):** trang công khai → SĐT che phần công khai/ở-nhà, đầy-đủ khi lostMode/chủ; vị trí người-nhặt consent-1-lần + retention (t-6); consent log 2 điểm (t-3/t-4). Enum `consent_scope` bổ sung **khi code consent land** (t-3/t-4), không thêm suy đoán. **Chặng "Ghi chip NFC"** (giữa PRINTING↔PACKING, chỉ nfc_tag) = `ALTER TYPE print_stage ADD VALUE` ở **t-2** (đã tiên liệu `000006_jobs.up.sql:19`), KHÔNG ở migration này. **RBAC:** admin pet-tag (roster read + encode) = **owner+staff** (fulfillment, nếp hàng-đợi-in); trang pet = **chủ storefront tự sửa** (auth khách P1-r), không phải admin. **Hệ quả:** t-1 xong (migration 000023 + sqlc models + verify-go green up/down/up); t-2 thêm print_stage value + encode; t-3 reuse auth P1-r; endpoints/openapi/FE theo từng slice; KHÔNG money/statusHistory/outbox/dep ở t-1. **Đảo được:** drop 3 bảng + cột + 3 enum → về không-Pet-Tag. **Loại:** (a) deploy lumin.pet riêng (edge-rewrite đủ); (b) Google OAuth v1 (reuse email); (c) bảng profile_blocks (jsonb đủ); (d) circular FK profile_id (derive); (e) lưu url (derive); (f) pet_tag_status vào state machine `packages/core` (lifecycle DB-only, không có impl thứ 2 để share — khác OrderStatus xuyên-4-surface).

### ADR-041 — Pet Tag NFC: chặng "Ghi chip NFC" (print_stage NFC_ENCODE) + mint tag lúc encode + endpoint 2-pha · Accepted (user 2026-07-13)
Slice **t-2** hiện thực hoá chặng encode đã tiên liệu (ADR-040 · `000006_jobs.up.sql:19`). **BE + Admin, KHÔNG money/statusHistory/outbox/dep** — vòng đời tag vẫn tách hẳn OrderStatus. **(1) print_stage +`NFC_ENCODE`** giữa `PRINTING`↔`PACKING` (migration **000024** `ALTER TYPE … ADD VALUE 'NFC_ENCODE' BEFORE 'PACKING'`, **file cô lập 1-statement** vì ADD VALUE pre-PG12 không chạy trong tx-block + PG12+ không dùng-value-cùng-tx; **down = NO-OP** vì Postgres không DROP được enum value, mà `000001_enums.down` drop cả type → test `TestMigrationsReversible` (0 enum sau downs) vẫn xanh; up dùng `IF NOT EXISTS` cho idempotent). **Routing chỉ-`nfc_tag`** cưỡng chế ở **FE product-aware `nextStage`** (standard: PRINTING→PACKING bỏ qua; nfc_tag: PRINTING→NFC_ENCODE→PACKING) + **guard endpoint**, **KHÔNG** ràng buộc DB — một SP standard *về lý thuyết* kéo tay vào cột NFC_ENCODE được, nhưng board snap-back (drop guard) + không nút encode (gate `productType==nfc_tag`) → vô hại, không đẻ tag (`ponytail:` thêm guard PATCH nếu mis-drag xảy ra). **(2) Tag mint LÚC ENCODE** (get-or-create theo `order_item_id`) — vì **chưa có wiring order→print_job** (`CreatePrintJob` 0 caller production), encode là nơi tag ra đời: mint `code` `#LMN-Txxxx` (sequence **000025** `pet_tag_code_seq` START 231, mirror order_code_seq) + `short_id` (`crypto/rand` 8B → base64url 11 ký tự, UNIQUE = backstop, `ponytail:` không retry-loop). Khi order→print_job land sau này, mint dời lên upstream, path này thành get/backfill. **qty>1 → 1 tag/dòng bây giờ** (`GetPetTagByOrderItem LIMIT 1`; `ponytail:` vòng lặp N-tag/đơn-vị = follow-up t-5). **(3) Endpoint 2-pha 1 route** `POST /admin/print-jobs/{id}/encode` (`PrintTagEncodeInput{chipUid?}` → `PrintTagEncodeResult{tag, card}`): **KHÔNG chipUid = PREPARE** (mint tag + trả URL để ghi, giữ nguyên stage) · **CÓ chipUid = CONFIRM** (ghi `chip_uid`+`encoded_at` → `ENCODED` + advance card →`PACKING`). Gộp thành 1 handler/schema thay vì 2 endpoint riêng (prepare+confirm) — vẫn đúng thứ tự vật lý (URL phải có TRƯỚC khi ghi chip). Cả mint+encode+advance trong **1 tx** (rollback nguyên khối). **Guard nfc_tag** ở đầu tx (non-nfc_tag → 400); PATCH stage chung **KHÔNG** đụng pet-tag (giữ sạch). Advance →PACKING blind (nếp D6 board decoupled staff-driven, nút chỉ hiện ở NFC_ENCODE — như PATCH anh em, KHÔNG guard source-stage); re-confirm ghi đè chip_uid/encoded_at (sửa burn lỗi trước đóng gói, write-once = khoá vật lý NTAG215 không phải DB). **(4) URL trang pet** = `PET_TAG_BASE_URL` (config core-api, default `https://lumin.pet` — D1 reserved; deploy set = storefront origin tới khi edge-rewrite), **derive** từ short_id (`/t/{shortId}`). **(5) Web NFC ghi URL** (progressive, `'NDEFReader' in window`, Chrome-Android D2); **`chip_uid` LUÔN nhập tay** (NDEFReader.write không trả serial; auto-read qua scan = `ponytail:` follow-up) — nhập tay = fallback D2 + path test được. **RBAC owner+staff** (fulfillment, nếp hàng-đợi-in `authRequired` default). **DTO `PrintQueueJob` +`productType`** (required, mọi card mang — board route theo đó) + `GetPrintQueueEntry` +`order_item_id`/`product_type`. **Hệ quả:** t-2 xong (migration 000024/000025 + pettag.sql/`db/pettag.go` + encode handler + FE cột 5 + encode sheet + verify-go/pnpm-verify/next-build xanh, `TestEncodePrintJobTagEndToEnd` prepare/confirm/reuse/broadcast/400/400/404 + spec-guardian PASS 0BLK/0WARN/2NOTE). **Đảo được:** down 000025 drop seq; 000024 no-op (value theo type khi rollback về 000001). **Loại:** (a) guard PATCH product×stage (encode endpoint là cổng thật); (b) N-tag/dòng qty>1 (1 tag v1); (c) Web NFC auto-read serial (nhập tay); (d) 2 endpoint prepare+confirm tách (gộp dual-mode); (e) ràng buộc DB nfc_tag↔stage (FE+endpoint guard đủ); (f) guard source-stage lúc confirm (nếp board decoupled).

### ADR-042 — Pet Tag NFC: kích hoạt + onboarding storefront (attach-lúc-hoàn-tất · consent point 1 = pet_profile scope) · Accepted (user 2026-07-13)
Slice **t-3** — nửa storefront: trang công khai `/t/{shortId}` + luồng kích hoạt (spec §10 onboarding). **BE+FE, KHÔNG money/statusHistory/OrderStatus/outbox/dep** — vòng đời tag vẫn tách hẳn §04. **(1) Attach owner LÚC HOÀN-TẤT (2d), KHÔNG lúc login** — spec §10 có 2 câu chữ khác thời điểm: gloss data-model (§10:364 "gắn khi đăng nhập lần đầu") vs flow (§10:401 "2d: gắn `ownerAccountId` + tạo `PetProfile`"). Chọn theo **flow 401**: `owner_account_id` + `PetProfile` + `ACTIVATED` set **nguyên tử trong 1 tx** lúc submit onboarding — một login rồi bỏ dở KHÔNG để lại tag nửa-claim (sạch hơn); ADR-040 đã hoà giải `owner_account_id` ở cả tags lẫn profiles (set khác thời điểm, owner bất biến nên không lệch). **(2) Endpoint 2:** public `GET /pet-tags/{shortId}` (status + profile summary **tối-thiểu chỉ khi ACTIVATED**: `handle/petName/species/photoUrl`, **KHÔNG owner PII**; PDPL data-min, trang đầy-đủ + che-SĐT = t-4) + customer-authed `POST /pet-tags/{shortId}/activate`. Guard atomic `WHERE status='ENCODED'` (0 row = đã kích hoạt/đua → **409** `PET_TAG_NOT_ACTIVATABLE`; UNENCODED cũng 409), unknown → **404**, no session → **401**, field xấu (name 1..40 · species enum · SĐT `^(0|\+84)\d{9}$` · **consent≠true**) → **400**. classify: `GetPetPage` **authPublic** (ai quét cũng đọc), `ActivatePetTag` **authCustomer** (spec §10 "tag tự gắn vào tài khoản vừa đăng nhập" → realm khách; token admin không satisfy). **(3) Consent point 1 = REUSE `consent_grants`** (000004) — migration **000026** thêm value `pet_profile` vào enum `consent_scope` (**ADD VALUE cô lập 1-statement**, down no-op — như 000024; `000001.down` drop cả type → `TestMigrationsReversible` xanh; enum chỉ equality-match `WHERE scope=` nên không phụ thuộc sort-order), `GrantConsentIfAbsent{scope pet_profile, channel web, policyVersion 2026-01}` trong tx activate, **idempotent** (khách nhiều-bé → 1 active grant). **KHÔNG** cơ chế consent mới (compliance §2 "đừng over-engineer"); FE consent checkbox **bắt buộc** (unbundled, không tick-sẵn, KHÔNG gate mua — kích hoạt là sau-giao-hàng) + privacy notice (mục đích / che-SĐT công khai / quyền sửa-xoá). **(4) Handle** = derive petName qua `immutable_unaccent` SQL (000012 reuse — **KHÔNG promote `x/text` lên direct dep**, fold Việt Bơ→bo) auto-suffix `-2..-9`/random-hex khi trùng, **KHÔNG 400** (khách không gõ handle; **route key = short_id** nên handle chỉ mỹ phẩm `@handle`; `pet_profiles.handle` UNIQUE = backstop; `ponytail:` no FOR UPDATE — đua hiếm/human-paced, activate fail → khách retry). **(5) FE** `/t/{shortId}` route theo status+auth (welcome 2a / wizard 2-bước [profile · contact+medical+social+consent] / **ACTIVATED placeholder** [t-3 tạm — trang 3-trạng-thái đầy đủ = t-4] / not-found / not-ready / error); login+register **`?next=/t/{shortId}`** (`safeNextPath` chống open-redirect: chỉ same-origin absolute path, chặn `//`/`/\`/absolute-URL/scheme; đọc server-side từ `searchParams` → prop, khỏi Suspense); **ảnh/gallery/theme HOÃN t-4 edit** (spec §10 validation chỉ bắt name+phone+consent); Google OAuth vẫn hoãn (D3, email-only 2a); noindex + robots `/t/` (PDPL data-min). **Hệ quả:** t-3 xong (migration 000026 + pettag.sql/`db/pettag.go` activation + `pettag_customer.go` GET+activate + FE route/wizard/states/libs + `?next=` + verify-go/pnpm-verify/next-build xanh, `TestActivatePetTagEndToEnd`[attach+profile+consent+ACTIVATED · 409/404/400/401 · handle-collision · jsonb round-trip real-PG] + `TestValidateActivateInput` + `db.TestHandleCandidates` + `TestMigrationsReversible`[000026 no-op down] + `pet-onboarding.test.ts` + spec-guardian **PASS 0BLK/0WARN/2NOTE**). **Đảo được:** down 000026 no-op (value theo type khi rollback về 000001). **Loại:** (a) attach-at-login write riêng (half-claim state → attach-at-completion atomic); (b) bảng `consents` mới (reuse `consent_grants`); (c) Go slugify + promote `x/text` (SQL `immutable_unaccent` reuse); (d) handle trùng → 400 (auto-suffix mỹ phẩm); (e) FOR UPDATE lúc activate (đua hiếm, UNIQUE backstop); (f) ảnh upload trong onboarding (hoãn t-4 edit); (g) Google OAuth (D3 deferred).

### ADR-043 — Extension auth: Bearer token (login trả token trong body theo cờ opt-in · middleware nhận Bearer-hoặc-cookie) · Accepted (user 2026-07-13)
Phase 4 (Extension MV3 **assistive-only**, ADR-011) — slice **e-1**. Extension gọi BFF **cross-origin**; admin SPA dùng `lumin_session` httpOnly cookie `SameSite=Strict` (ADR-030) — cookie **KHÔNG** gửi kèm request cross-site từ extension + core-api không bật CORS → extension cần cơ chế auth riêng (ADR-030 để ngỏ "handshake auth extension MV3"). **Quyết định (user chọn):** **(1)** `POST /auth/login` nhận thêm cờ **optional `issueToken`**; khi `true` (**CHỈ** extension gửi) body `AuthUser` đính thêm **optional `token`** = chuỗi JWT (chính `cookie.Value` vừa mint). Admin **KHÔNG** gửi cờ → response byte-identical, token vẫn CHỈ ở `Set-Cookie`. Gate bằng cờ thay vì luôn-trả vì `Set-Cookie` không JS-đọc-được còn field body thì CÓ — chỉ lộ token khi client chủ động xin, nên ADR-030 (chống XSS moi token) **giữ nguyên cho admin** (một XSS trên admin không lấy được bearer khả-chuyển từ response login). **(2)** Middleware `resolveActor` lấy token từ **`Authorization: Bearer <jwt>` HOẶC** cookie (Bearer ưu tiên); cùng `Issuer.Verify` + re-read role từ `users` row → Bearer là admin-realm JWT bình thường, **KHÔNG** realm/secret mới (khác customer realm ADR-030). **(3)** Extension lưu token trong **`chrome.storage.local`** (sandbox theo extension, trang web KHÔNG đọc được — khác localStorage của trang admin), gửi `Authorization: Bearer` mọi call; CORS = **`host_permissions`** trỏ origin core-api (MV3 tự cấp cross-origin, server **KHÔNG** cần bật CORS). **Hệ quả:** BE chạm tối thiểu — 1 optional request field `issueToken` + 1 optional response field `token` (omitempty) + Bearer-fallback trong `resolveActor` + helper `bearerToken`; **KHÔNG** migration, **KHÔNG** realm/secret mới, admin cookie path **bất biến** (ADR-030 nguyên vẹn cho admin). Token trong `chrome.storage.local` = secret: không log, xoá lúc logout, TTL theo issuer config sẵn. Regen `api.gen.go` (`make oapi`) + `schema.gen.ts` (`@lumin/api-client` codegen). **Loại (verify vs mối đe doạ thật):** (a) pairing-code từ Admin (thêm bảng + endpoint + màn Admin — chưa cần; để dành nếu muốn revoke per-device sau); (b) cookie reuse (nới `SameSite`=Lax/None + bật CORS — mong manh vì 3p-cookie deprecation + nới lỏng cookie state-changing của admin); (c) `chrome.cookies.get` đọc httpOnly cookie (khả thi nhưng cần quyền `cookies` rộng + phụ thuộc `Set-Cookie` đáp vào jar — token-in-body đơn giản hơn, ít quyền hơn); (d) luôn trả token trong body không cờ (regress ADR-030 cho admin).

### ADR-044 — Cơ chế backup Postgres = pg_dump hằng ngày + restic (thay cơ chế WAL-G của ADR-018) · Accepted (user 2026-07-15)
Bất biến ADR-018 **GIỮ NGUYÊN**: backup **offsite + mã hoá + test restore**, điều kiện launch. Chỉ **đổi cơ chế**, không đổi bất biến: một máy all-home chấp nhận downtime (ADR-009) không cần PITR RPO≈0, mà WAL-G liên tục đòi image Postgres tuỳ biến + `archive_command` + WAL store — chi phí vận hành vượt nhu cầu. **Chốt:** CronJob `pg_dump -Fc` hằng ngày (off-peak) → restic (mã hoá/dedup/retention 7d·4w·6m) → repo offsite S3-compat; restore = `pg_restore`, có drill vào DB scratch (`infra/k8s/README §Backup & restore`). **RPO ≈ 24h** (khớp accept-downtime). **3-2-1:** bản offsite + `RESTIC_PASSWORD` lưu offline; **bản copy-thứ-2 on-site (restore nhanh) HOÃN** — gap có ý thức, mở lại (nâng WAL-G/PITR) nếu lượng đơn khiến 24h RPO không chấp nhận được; invariant offsite+tested-restore không đổi. Backup creds ở Secret riêng `lumin-backup-secrets` (không nhét repo-password vào mọi app pod). Files `infra/k8s/backup-*` + `uptime-kuma.yaml`. operations.md §5 + plan.md §Phase-5 trỏ về đây.

### ADR-045 — Asset pipeline nửa OUTPUT: callback worker→core-api `PATCH /internal/asset-jobs/{id}` (service-token · host-pinned · idempotent) · Accepted (user 2026-07-15)
Nửa OUTPUT của pipeline asset (ADR-006 outbox / ADR-007 render): worker (Rust) drain `asset_job.created` off JetStream **WorkQueue `ASSET_JOBS`** (subject thật `asset_job.created`, KHÔNG `asset.job` — sửa default cũ ở worker config) → render/ingest → **báo kết quả về đâu?** Chốt: **worker gọi endpoint core-api CÓ XÁC THỰC** (không ghi thẳng DB, không event vòng-về). **(1) Realm auth thứ 3 = `authService`** — bearer bí mật **TĨNH** `WORKER_CALLBACK_TOKEN`, so sánh **constant-time**, **KHÔNG** actor/JWT (worker không có danh tính, chỉ chứng minh là worker tin-cậy). **Fail-closed:** token chưa set (rỗng) → endpoint từ chối MỌI call (guard rỗng-TRƯỚC nên rỗng==rỗng KHÔNG pass). Tách hẳn cookieAuth (ADR-030) + customerAuth + Bearer-admin (ADR-043). Chọn static secret thay JWT-service vì 1 worker home không cần expiry/claims (ponytail); chọn endpoint-có-auth thay ghi-thẳng-DB để giữ **core-api = writer DB DUY NHẤT** (bất biến xuyên codebase) + validate 1 chỗ; thay NATS-event-vòng-về vì core-api **chưa từng chạy consumer** (chỉ provision stream — natsx: "Streams are provisioned, never consumers") → thêm cơ bắp mới + nhiều code hơn. **(2) `PATCH /internal/asset-jobs/{id}` (`ReportAssetJobResult`)** nhận `AssetJobResultInput{status: processing|ready|failed, model3dUrl?, lastError?}` (status KHÔNG có `queued` — đó là trạng thái khởi tạo) → **1 tx row-locked** (`GetAssetJobByIDForUpdate` = `FOR UPDATE`): đổi status + (ready **model_ingest**) ghi `products.model3d_url` (**D3** — writer DUY NHẤT của cột mà `UpdateProduct` cố tình không đụng). **Idempotent + at-least-once safe:** `ready` là terminal + **sticky** — callback redelivery trả job nguyên trạng, KHÔNG regress status/ghi-đè url đã lưu. `attempts` giữ nguyên (callback báo lifecycle, không kế toán retry); `completed_at` = now() khi terminal, giữ cũ khi processing. **(3) Host-pin OUTPUT** `model3dUrl` = phải cùng **origin bucket assets** (`modelstore.OwnsOutputURL`: origin + base-path + non-empty key, **KHÔNG** key-shape chặt như `OwnsURL` nguồn — worker tự đặt namespace key output) **+ `.glb`** — vì `model3d_url` trở thành `<model-viewer src>` phía client (chống **stored content-injection** nếu token rò). ready model_ingest **thiếu** url → 400 (rollback, job giữ queued); url ngoại lai → 400; `sprite_render` output CHƯA có cột/consumer storefront → bỏ qua (YAGNI). `lastError` cap 2000 rune (truncate rune-safe, KHÔNG 400 — báo-lỗi bị chặn = job kẹt). **(4) Slice này CHỈ core-api side + GPU device-plugin** (`infra/k8s/nvidia-device-plugin.yaml` — DaemonSet advertise `nvidia.com/gpu`, gỡ Pending cho asset-worker); **consumer Rust + Blender/CUDA + trimesh + upload Garage = slice SAU, box-gated GTX 1060** (không verify được ngoài box — o-1c gate "Blender thấy GPU in-container"). **Hệ quả:** openapi +path +`AssetJobResultInput` +scheme `serviceAuth`; config `WORKER_CALLBACK_TOKEN` (no dev fallback); `authService` class + classify + branch constant-time; sqlc `GetAssetJobByIDForUpdate`+`SetProductModel3dUrl`; `db` wrappers + handler `internal_asset_jobs.go`; k8s core-api.yaml env `optional:true` (fail-closed tới khi set) + secret.example + asset-worker.yaml reserve-comment. **Test:** auth-gate Docker-free (`TestReportAssetJobResultAuthService` — fail-closed rỗng-token, wrong/absent 401, match không-inject-actor) + integration testcontainers (`TestReportAssetJobResultEndToEnd` — ready ghi url/completedAt · sticky redelivery · failed lastError · processing no-completedAt · missing/foreign url 400 rollback · unknown 404); `make verify-go` xanh (golangci 0 · sqlc vet/diff · oapi stale · `-race`). **Đảo được:** endpoint + cột-writer additive; bỏ = gỡ route + env, không migration. **Loại:** (a) worker ghi thẳng Postgres (writer thứ 2, bỏ qua server-authority + validate); (b) NATS event vòng-về (core-api chưa có consumer — cơ bắp mới, nhiều code hơn); (c) JWT-service token (static secret đủ 1 worker, ít máy móc hơn, không expiry-cần); (d) `OwnsURL` chặt cho output (key output worker tự đặt — chỉ pin origin+.glb); (e) `sprite_render` output path (chưa consumer storefront — model-viewer nạp `.glb`, KHÔNG sprite); (f) full Rust pipeline trong slice này (box-gated → tách slice sau).

### ADR-046 — Serve asset công khai = Garage website mode (web endpoint :3902, Host=bucket alias) thay proxy core-api / presign · Accepted (user 2026-07-15)
Garage v1.0.1 **không có anon read trên S3 API** (`Forbidden: Garage does not support anonymous access yet`) → object trong bucket **public `lumin-assets`** (ảnh sản phẩm/pet/review qua `<img>` + derivative `.glb` qua `<model-viewer>`) 404 khi GET trần `s3.luminstudio.vn/lumin-assets/…`. Pipeline model_ingest (ADR-045, #127–#130) do đó **tối** ở prod dù đã wired end-to-end. **Chốt:** phục vụ qua **Garage website mode** — web endpoint (`:3902`) serve bucket **ẩn danh**, resolve **Host = global-alias `assets.luminstudio.vn`**, **path = object key** (bucket ngầm định theo host → **KHÔNG** segment `/lumin-assets`). Vì vậy public-base thành **host-only**: 3 env `MODEL_UPLOAD_PUBLIC_BASE_URL`/`IMAGE_UPLOAD_PUBLIC_BASE_URL` (core-api) + `ASSETS_PUBLIC_BASE_URL` (worker) = `https://assets.luminstudio.vn`. **Upload KHÔNG đổi** — vẫn presigned path-style vào **S3 endpoint** `s3.luminstudio.vn` (ADR-035/036); chỉ **gốc phục vụ + host-pin** dời. Cùng bucket + key, hai lược đồ địa chỉ (path-style ghi ↔ host-implied đọc). **ZERO app code** — hai validator host-pin đã parametric theo public-base (`modelstore.objectKeyFromFinalPath` có nhánh `base==""`; worker `key_from_public_url`/`output_url` prefix-strip) → bất biến ADR-036 (`sourceModelUrl` host-pin) + ADR-045 (`OwnsOutputURL`) **giữ nguyên**, chỉ **thêm** test host-only 2 phía (`modelstore_test.TestStoreHostOnlyPublicBase` + `objectstore::website_mode_host_only_base_round_trips`). **Bảo mật:** website mode chỉ bật trên `lumin-assets`; **`lumin-payment-proofs` là bucket RIÊNG, KHÔNG alias website, KHÔNG route `:3902`** (vẫn presigned/authed, `PAYMENT_PROOF_PUBLIC_BASE_URL` giữ dạng path cũ) — biên bảo mật của `lumin-assets` giờ là "key khó đoán" thay vì "authed S3 read", chấp nhận được vì mọi thứ trong đó vốn world-readable (bất biến MỚI cho writer tương lai vào bucket này). **CORS:** `<img>` không cần; `<model-viewer>` fetch `.glb` cross-origin (`www.`→`assets.`) cần `Access-Control-Allow-Origin` — verify web endpoint có replay bucket-CORS không, thiếu thì CF Response-Header Transform Rule `ACAO: *` (public+immutable → `*` an toàn). **Prod-box (KHÔNG capture trong manifest, như CORS/layout bootstrap):** `garage bucket website --allow lumin-assets` + `bucket alias lumin-assets assets.luminstudio.vn` + CF DNS `assets.` — re-run sau mỗi garage/bucket rebuild (runbook `infra/k8s/README §Public asset serving`). **Đảo được:** trỏ 3 env về `s3.luminstudio.vn/lumin-assets` + gỡ website/alias; không migration, không đổi object. **Loại:** (a) **proxy đọc qua core-api** (`GET /public/assets/*` stream có auth) — nhỏ diff nhưng đẩy byte asset qua app-server + tunnel, ngược nguyên tắc no-proxy-qua-tunnel (ADR-035 · conventions §3D-upload); user chọn website mode để byte đi Garage→CF thẳng; (b) **presigned GET** lưu vào `model3d_url`/img-src — URL hết hạn, đổi shape → phá host-pin, không dùng cho URL lưu bền; (c) **nâng Garage lên bản có anon S3 read** — rủi ro nâng store giữ TOÀN BỘ data+backup prod hơn là thêm một alias website, mà docs Garage vẫn khuyến nghị website mode cho anon.

### ADR-047 — Lộ GPU cho render-worker = recreate k3d cluster với `--gpus all` + node image `k3s-cuda` (thay worker-ngoài-k8s / recreate-không-backup) · Accepted (user 2026-07-15)
Render-worker (ADR-007: Blender Cycles+CUDA trên GTX 1060) cần GPU trong cluster, nhưng cluster prod là **k3d** (k3s-in-docker) được tạo KHÔNG có GPU: node container `DeviceRequests: null`, image `rancher/k3s` chuẩn không có nvidia-container-toolkit → k8s không thấy/không advertise `nvidia.com/gpu`, `asset-worker.yaml` (request `nvidia.com/gpu: 1`) **Pending vĩnh viễn**. **Nền host ĐÃ SẴN** (owner set 2026-07-15): driver Windows + WSL2 passthrough (`/usr/lib/wsl/lib/nvidia-smi` thấy GTX 1060) + nvidia-container-toolkit + docker `nvidia` runtime (`docker run --gpus all` thấy card). Gap DUY NHẤT = tầng k3d. **`--gpus` chỉ có trên `k3d cluster create`, KHÔNG có trên `k3d node create`** (k3d v5.9.0) → không thêm được GPU vào node đang chạy, cũng không thêm được agent-node GPU. **Chốt: recreate cả cluster** bằng `k3d cluster create --gpus all --image k3s-cuda:<tag>` — `k3s-cuda` (`infra/k8s/k3s-cuda.Dockerfile`, recipe chuẩn k3d.io) = `rancher/k3s` (pin đúng version cluster) + nvidia-container-toolkit + `nvidia-ctk runtime configure --runtime=containerd`, để k3s bên trong tự-tạo RuntimeClass `nvidia` mà `nvidia-device-plugin.yaml` (#127) + `asset-worker.yaml` request. **Bất biến DR (BẮT BUỘC, ADR-009/ADR-044):** `k3d cluster delete` **XOÁ** volume docker chứa local-path PVC (postgres = đơn thật · garage = ảnh biên lai `lumin-payment-proofs`) → **backup + PROVE restore CẢ postgres LẪN garage TRƯỚC khi xoá** (restic hiện chỉ có postgres → thêm garage backup); recreate là **maintenance window có chủ đích**, từng-bước, re-apply toàn bộ manifest + re-bootstrap garage (layout/keys/buckets/CORS/website/alias — state prod ngoài manifest). Runbook: `infra/k8s/README §GPU cluster recreate`. **Loại:** (a) **worker ngoài k8s** (`docker run --gpus all` trên host — GPU chạy ngay, không đụng cluster) — user chọn giữ mô hình k8s-everything (owner đã pivot compose→k8s), đổi lại phải recreate + DR; (b) **recreate KHÔNG backup** — mất data đơn/biên lai, không chấp nhận; (c) **thêm GPU agent-node** — k3d không hỗ trợ `--gpus` trên node create; (d) **NVIDIA GPU Operator** — thừa cho 1 card 6GB concurrency=1 (device-plugin tĩnh #127 đủ, ponytail ADR-014). **Đảo được:** k3s-cuda chỉ đổi node runtime; về không-GPU = recreate lại với image thường (lại là một DR).

### ADR-048 — Render worker chạy NGOÀI k8s (`docker --gpus` trên WSL host) thay in-cluster; supersedes phần GPU-in-cluster của ADR-047 (WSL2 wall) · Accepted (user 2026-07-15)
ADR-047 chốt recreate cluster để lộ GPU **in-cluster**. **THỰC THI 2026-07-15: recreate xong + site khôi phục, nhưng GPU-in-pod KHÔNG chạy trên k3d+WSL2.** Recreate đưa GPU vào **NODE** container (`docker exec k3d-luminstudio-server-0 nvidia-smi -L` = GTX 1060, `/dev/dxg` present) + k3s tự tạo RuntimeClass `nvidia`, NHƯNG **pod** không init được NVML: `nvidia-device-plugin` báo `ERROR_NOT_SUPPORTED`, pod dưới `runtimeClassName: nvidia` báo `NVML N/A`. Nguyên nhân: nvidia-container-toolkit inject WSL GPU libs **host→node-container** nhưng **KHÔNG node-container→pod** (containerd LỒNG không nhận diện môi trường WSL) — đúng bức tường WSL2 đã cảnh báo cho option-B của ADR-047. **Chốt (đảo lựa chọn): render worker chạy NGOÀI k8s** = `docker run --gpus all` (hoặc systemd unit) trên WSL host — GPU chạy ngay (đã chứng minh: `docker run --gpus all ubuntu nvidia-smi` thấy card). Đây là **ADR-047 rejected-option (a)**, nay thành lựa chọn chính vì in-cluster bất khả thi trên box này. **Networking:** worker→**core-api** + **garage** dùng hostname CÔNG KHAI sẵn có (`api.luminstudio.vn` cho callback `PATCH /internal/asset-jobs/{id}` — token-gated ADR-045; `s3.luminstudio.vn` cho fetch source + upload glb) → KHÔNG hạ tầng mới; chỉ **NATS** cần lộ ra host (NATS không public — ADR-008) = **NodePort** + chạy worker trên docker network `k3d-luminstudio` → `nats://k3d-luminstudio-server-0:<nodeport>`. Worker image (`lumin-asset-worker:prod`, đã có Blender 4.2.3 + CUDA 12.4) chạy `--gpus all`, off-peak concurrency=1 (ADR-007). **Vòng đời:** KHÔNG qua `deploy.yml`/k8s Deployment — `asset-worker.yaml` (k8s, request `nvidia.com/gpu:1`) **GIỮ làm di sản nhưng KHÔNG deploy** (nếu k3d/WSL sau này hỗ trợ GPU-in-pod thì bật lại); worker lên bằng docker/systemd (runbook). **Bất biến GIỮ:** WorkQueue consumer + callback host-pinned + idempotent sticky-ready (ADR-045) KHÔNG đổi — chỉ **nơi CHẠY** đổi (in-cluster→host). model_ingest (trimesh CPU) + sprite_render (Blender GPU) giờ chạy được thật + verify được (o-1c). **ADR-047 status:** phần "recreate để GPU **in-cluster**" **SUPERSEDED bởi ADR-048** (recreate vẫn hữu ích: cluster chạy image k3s-cuda ổn cho web stack; runbook backup-first vẫn giá trị); phần **DR-backup-invariant GIỮ nguyên**. **Loại:** (a) tiếp tục đục containerd/WSL để ép GPU-in-pod — known-hard, không chắc, đã thử device-plugin + test-pod (đều NVML-fail) → phí công; (b) recreate lại về image thường — phí nền k3s-cuda + lại một DR; (c) `k3d node create --gpus` — k3d không hỗ trợ (ADR-047).

### ADR-049 — sprite_render OUTPUT path: cột `products.sprite_sheet_url` + callback `spriteSheetUrl` (host-pin `.webp`) + sprite sheet WebP lưới cố định + consumer storefront hover/no-WebGL · Accepted (user 2026-07-15)
Mở lại option (e) mà **ADR-045 hoãn** ("sprite_render output CHƯA có cột/consumer storefront → YAGNI"): dựng nốt nửa OUTPUT của sprite_render (ADR-007 giữ turntable 360° server-side — KHÔNG relitigate). **Hình dạng output = MỘT sprite sheet WebP** (không N-frame rời): 1 URL / 1 cột / 1 request / 1 object immutable sau Cloudflare cache — **soi gương y hệt `model3d_url`** (ADR-045 D3), thay mảng-URL/manifest (thừa máy móc, ponytail). Lưới **cố định** `SPRITE_FRAMES=24` / `SPRITE_COLS=6` (6×4, tile ~320px) = **hằng số dùng chung** worker↔storefront nên URL tự đủ mô tả (KHÔNG cột metadata; đổi lưới ⇒ phải re-render các product cũ — ceiling ghi cạnh hằng số, nâng cấp = `sprite_meta` jsonb nếu cần). **Cột:** `products.sprite_sheet_url text NOT NULL DEFAULT ''` (mirror `model3d_url` 000003; migration 000027) — **writer DUY NHẤT = callback tx** (`SetProductSpriteSheetURL`), `UpdateProduct` KHÔNG đụng (như model3d_url). **Callback:** `AssetJobResultInput` +`spriteSheetUrl?` (optional, KHÔNG default → optional TS [[lumin-openapi-default-makes-ts-required]]); trên `ready sprite_render` **bắt buộc** (thiếu → 400, mirror model_ingest thiếu model3dUrl), host-pin **`.webp` + `OwnsOutputURL`** (cùng origin assets — chống stored content-injection vì URL thành `<img src>` client), ghi cột trong CÙNG tx row-locked, **sticky/idempotent** như model3d_url. Mỗi pipeline ghi cột riêng: model_ingest bỏ qua spriteSheetUrl, sprite_render bỏ qua model3dUrl. **Input render = model gốc đã upload** (cùng `sourceModelUrl` host-pinned `OwnsURL` như model_ingest — derivative glb KHÔNG qua được OwnsURL strict `<prefix>/YYYY/MM/DD/<uuid>.<ext>`); render.py **tự decimate** vừa 6GB VRAM (ADR-007) nên độc lập việc ingest đã/chưa decimate. **Worker:** `Outcome`+`ResultBody`+`decide` +`sprite_sheet_url`; `SpriteRenderProcessor` thay `Unimplemented` (fetch glb → Blender Cycles+CUDA turntable N frame → tile WebP → upload `derivatives/{source_version}/sprite.webp` → report ready). Blender **subprocess** (ADR-007), concurrency=1 off-peak; **GPU-in-container gate box-only (o-1c)** — verify trên box (`ssh pc-server`), KHÔNG đóng được ở máy dev GPU-less (giống model_ingest e2e box-gated). **Storefront:** `spriteSheetUrl?` vào Product + ProductCard; card **hover (PC)/dừng-2s (mobile)** cycle frame + **fallback no-WebGL** cho model-viewer (product-detail) — **tôn trọng `prefers-reduced-motion`** (always-must #4: reduced → frame tĩnh, KHÔNG cycle). **Bảo mật:** sprite sheet world-readable trong `lumin-assets` (website mode ADR-046) — bất biến "bucket world-readable" giữ nguyên, không mở biên mới. **Đảo được:** cột + field + route đều additive; bỏ = gỡ consumer + field + drop column, model_ingest không đổi. **Loại:** (a) N-frame rời (mảng cột/manifest — thừa máy móc); (b) `sprite_meta` jsonb cho lưới (YAGNI — hằng cố định đủ, re-render nếu đổi); (c) render từ derivative glb (không qua OwnsURL strict → phải nới host-pin nguồn, rủi ro hơn tự-decimate trong render.py); (d) storefront tự xoay canvas từ .glb thay sprite (ADR-007 đã chốt giữ sprite cho fidelity + fallback no-WebGL).

### ADR-050 — Màu sản phẩm lấy tên+hex TỪ filament (copy-on-write) — mở rộng ADR-039 · Accepted (user 2026-07-16)
`colors.filament_material_id` (thêm ở 000019 cho trừ-nhựa-lúc-in, ADR-039) giờ CÒN là nguồn **tên + hex** của màu — yêu cầu chủ shop: màu sản phẩm lấy TỪ filament, không nhập tên/hex riêng. **Copy-on-write, KHÔNG đập lại schema** (theo oracle): giữ nguyên bảng `colors` (cột `name`/`hex` còn nguyên); handler màu (`CreateProductColor`/`UpdateProductColor`) tra filament liên kết (`resolveColorFilament`) rồi **đóng dấu** name+hex của nó vào dòng màu lúc ghi. `ColorInput` bỏ `name`/`hex` + **bắt buộc** `filamentMaterialId`; filament không tồn tại hoặc **chưa có hex** ("no colour chip", 000018) → **400** `filamentMaterialId` (màu không bao giờ rỗng swatch — trạng thái xấu thành bất-khả-biểu-diễn thay vì check NULL rải nhiều nơi). **An toàn đường tiền:** capture (`partColorSnapshotsFrom`) **KHÔNG đổi** — đóng băng name/hex đã-đóng-dấu vào `order_items.part_colors` jsonb, nên đổi tên filament về sau **KHÔNG lan** tới đơn đã bán (bất biến còn nguyên; test `TestColorSwatchSourcedFromFilament`: rename filament → màu cũ giữ nguyên). `pricing.PriceItem` không đọc name/hex → đường tính tiền không đổi. Màu **phẳng cũ** (`part_id` NULL, không filament) giữ name/hex riêng khi đọc (dual-mode, nếp ADR-037 nullable). Guard hex chống CSS-injection **vẫn còn** — chuyển sang nơi nhập hex (filament editor `cleanFilamentMaterialInput`, cùng `hexRe`), nên mọi hex tới `style` client đều đã qua guard. **Trần (ponytail):** đổi tên/hex filament không retro-cập-nhật màu cũ tới khi lưu lại màu đó; nâng cấp = rename-cascade hoặc resync đêm. **Đảo được:** read-policy + input additive, **KHÔNG migration** (mọi cột đã có). Đây là slice **f-1** của epic màu-filament (`docs/plans/filament-color.md`); các slice render 3D theo-part (f-2 map part↔object, f-3 viewer live, f-4 structured-glb, f-5 sprite theo-part) tiếp sau. **Loại:** (a) đập `colors` thành (part_id, filament_id, price_delta, available) đọc-xuyên filament (thêm migration + nullable name/hex + xử lý NULL rải 4 bề mặt — oracle bác, copy-on-write nhỏ hơn); (b) bỏ dual-mode màu phẳng tự-nhập (phá màu/đơn cũ); (c) capture resolve live từ filament (phá bất biến snapshot — đổi tên filament sẽ đổi đơn đã bán).

### ADR-051 — Part↔model-object mapping: `parts.model_object_name` + `products.model_object_names` + model_ingest phát danh sách object · Accepted (user 2026-07-16)
Slice **f-2** epic màu-filament (`docs/plans/filament-color.md`). Để slice sau tô mỗi **part** đúng màu filament của nó trong 3D, part cần **handle trỏ tới object có tên trong model**. **Đảo NHẸ mệnh đề "no geometry mapping" của ADR-037/038** (cả hai deferral đều scope hẹp: *"ở ADR này"* / *"Stage 3 = camera pose THÔI"*; ADR-050 đã pre-declare *"f-2 map part↔object"* trong roadmap) — chỉ thêm MỘT tên (name handle), **KHÔNG** zones/toạ độ/UV, giữ nguyên tắc "worker không đổi output đã chuẩn hoá". **Data:** `parts.model_object_name text NOT NULL DEFAULT ''` (`''` = chưa gắn → part tô filament mặc định, KHÔNG bao giờ xám) + `products.model_object_names text[] NOT NULL DEFAULT '{}'` (danh sách object model_ingest tìm thấy = tập chọn cho dropdown editor) — migration 000028; **cột list writer DUY NHẤT = callback tx** (`SetProductModelObjectNames`, cạnh `SetProductModel3dUrl` trong CÙNG tx `ready model_ingest`, sticky-idempotent như model3d_url; `UpdateProduct` KHÔNG đụng). **Ingest emit:** `ingest.py` đọc tên object **KHÔNG `force='mesh'`** (đường fuse/export + pose ADR-038 **byte-identical**; đọc riêng, best-effort: lỗi load → `[]`, không fail ingest tốt) → chảy `Manifest`(`#[serde(default)]` → tương thích ingest.py cũ) → `Outcome` → `pipeline` → `ResultBody.objectNames` (camelCase, skip-if-empty) → `AssetJobResultInput.objectNames?` (optional, KHÔNG default → optional TS). **Sanitize ở biên (KHÔNG host-pin — metadata, không phải URL/`src`):** `sanitizeModelObjectNames` trim + bỏ rỗng + cap 500 tên × 200 rune (truncate rune-safe như lastError), **KHÔNG BAO GIỜ 400** (worker service-auth; callback rớt = job kẹt). **Contract:** `Part.modelObjectName?`/`PartInput.modelObjectName?`/`Product.modelObjectNames?` đều optional (không required, không default → tránh break TS fixture). **Mapping = handle TỰ DO, KHÔNG check membership:** tên không khớp object nào (đặt trước ingest / giữ qua re-ingest / STL không tên) → render thành filament mặc định downstream (plan D-C), nên KHÔNG chặn ở boundary — chỉ cap độ dài (`cleanPartInput` → 400 nếu > 200 rune). **Admin:** PartForm = **dropdown** (chọn từ `modelObjectNames`; tên cũ model không còn liệt kê vẫn chọn được + gắn cờ **stale**, không âm thầm rơi mapping của chủ shop); part row hiện tên đã gắn. **KHÔNG [MONEY]** — checkout/pricing/`part_colors` không đụng (bất biến f-1/ADR-037 nguyên vẹn; spec-guardian PASS xác nhận 0 hit object-name ở đường tiền). **Click-on-model HOÃN sang sau f-4:** glb phục vụ (`model3d_url`) đã fuse/mất tên (`force='mesh'`) + bucket nguồn không phục vụ browser → click cần glb **có-tên, browser-reachable** = đúng thứ f-4 (structured glb) dựng (chính note "sequence f-4 before f-2's click UI" của plan, xác nhận từ code); chủ shop xin cả hai → **sắp thứ tự, không bỏ**. **Đảo được:** cột + field + emit đều additive; bỏ = drop 2 cột + gỡ field + ingest.py bỏ objectNames (`Manifest #[serde(default)]` nuốt được). **Loại:** (a) nhập tay tên object (oracle bác — drift âm thầm khi đổi tên/casing/dấu; dropdown tên thật khử drift); (b) hard-check membership (chặn đặt-trước-ingest + giữ-qua-re-ingest — D-C xử lý bằng fallback default thay vì lỗi); (c) un-fuse model_ingest để giữ tên trên glb phục vụ (đập chuẩn-hoá/pose ADR-038 — D-B: thay vào đó f-4 emit glb thứ 2); (d) host-pin objectNames (thừa — tên mesh, không phải URL/`src`).

### ADR-052 — model_ingest phát glb thứ 2 "structured" (giữ tên object/material) + cột `products.model3d_structured_url` · Accepted (user 2026-07-16)
Slice **f-4** epic màu-filament (Track B step 1). Live viewer (f-3) cần glb **giữ tên object/material** để tô mỗi part theo màu filament của nó — nhưng glb phục vụ hiện tại (`model3d_url`) đã **fuse mất tên** (`force='mesh'`, cần cho dims/recenter/watertight well-defined + là thứ pose ADR-038 curate against). **Chốt (D-B): KHÔNG un-fuse** — giữ `model.glb` fused **byte-identical**, phát THÊM `model_structured.glb` (object/material có tên, **cùng vector recenter**) + cột mới `products.model3d_structured_url`; viewer dùng structured khi có, fallback `model3d_url`. **Đảo NHẸ ADR-045 D3/ADR-038** "worker phát MỘT glb / không đổi output" — thêm derivative thứ 2, KHÔNG đổi derivative cũ; **đã foreseen** bởi ADR-051 D-B (rejected option un-fuse → "thay vào đó f-4 emit glb thứ 2") + roadmap ADR-050 → không relitigate, không đánh dấu Superseded (không mâu thuẫn). **Landmine (D-B) = cùng translation:** `ingest.py` tính `translation = -fused.bounding_box.centroid` **MỘT lần**, áp cho cả fused mesh VÀ scene structured (khác vector → pose curate de-frame một cái). **VERIFIED off-box** (trimesh là CPU, chạy trên Mac — **KHÔNG box-gated**): probe + real `ingest.py` e2e chứng minh tên+material+recenter sống qua `export_glb` (2-object giữ `["Chao đèn","Đế"]`+màu; STL đơn-mesh → không structured → fallback). **ingest.py:** `structured_artifact()` load source **KHÔNG `force='mesh'`** (Scene giữ tên) → áp same translation → export; **gộp** luôn việc đọc `object_names` của f-2 vào cùng 1 scene-load; **best-effort** (lỗi load/export → không structured, KHÔNG fail ingest tốt); source đơn-mesh (STL) → `([], None)`. **Worker:** `Manifest.structured_glb_path` (`#[serde(default)]` — tương thích ingest.py cũ) → model_ingest đọc + upload key thứ 2 content-addressed `derivatives/{ver}/model_structured.glb` (cùng idempotency) → `Outcome`/`ResultBody.model3d_structured_url` (skip-if-none). **Callback:** `AssetJobResultInput.model3dStructuredUrl?` **OPTIONAL cả trên ready model_ingest** (source không tên → không có → **200, KHÔNG 400** như model3dUrl bắt buộc); host-pin **`.glb` + `OwnsOutputURL`** qua CÙNG `cleanOutputURL` như model3dUrl (nó thành `<model-viewer src>` client → chống stored content-injection); ghi cột trong CÙNG tx ready-model_ingest (writer DUY NHẤT, sticky), **guard non-empty** (source không tên KHÔNG blank structured của lần ingest trước). `Product.model3dStructuredUrl?` optional (không required — nếp `spriteSheetUrl`/`modelObjectNames`). **Cột:** `model3d_structured_url text NOT NULL DEFAULT ''` (mirror model3d_url 000003 / sprite_sheet_url 000027; migration 000029); `UpdateProduct` KHÔNG đụng. **KHÔNG [MONEY]** (checkout/pricing/`part_colors`/`PriceItem` không đụng; cột chỉ là display metadata; spec-guardian PASS xác nhận). **Đảo được:** cột + field + emit đều additive; bỏ = drop cột + gỡ field + ingest.py bỏ structured (`Manifest #[serde(default)]` nuốt). **Loại:** (a) un-fuse model_ingest (1 glb có tên thay fused) — đập dims/recenter/watertight well-defined + pose ADR-038; (b) structured **bắt buộc** trên ready (như model3dUrl) — source không tên/STL sẽ fail oan, D-B cho optional + fallback; (c) client parse tên từ fused glb (đã mất tên — bất khả); (d) không host-pin structured (nó là `<model-viewer src>` — phải pin như model3dUrl).

### ADR-053 — Storefront live per-part recolor + structured glb đặt tên material = object (hoàn tất Track B) · Accepted (user 2026-07-16)
Slice **f-3** epic màu-filament (Track B step 2, "payoff" nhìn thấy được). Khách chọn filament cho một part → viewer tô lại **đúng mesh part đó** thời gian thực. **Cơ chế:** model-viewer 4.3.1 tô lại theo **MATERIAL** (`getMaterialByName(name).pbrMetallicRoughness.setBaseColorFactor(hex|rgba)`); ánh xạ mesh→material là **symbol-private** (`[$nodeFromIndex]`, không public — xác nhận từ `.d.ts`). Nên **structured glb (ADR-052) giờ đặt tên material = tên object** (`ingest.py structured_artifact`: `material.name = object_name`) để viewer `getMaterialByName(part.modelObjectName)` trúng — **tinh chỉnh nhỏ, additive** của ADR-052 (không đổi manifest/API; ADR-052 nói "giữ tên object/material" = preserve, đây thêm *đặt tên* material chủ động). Best-effort trong try/except sẵn có; geometry không material → bỏ qua (part đó không tô, không crash). **Storefront:** `PartView.modelObjectName` + `ProductDetailView.model3dStructuredUrl` (đã có trong schema từ f-2/f-4 merged → **KHÔNG codegen**) + hàm thuần `partColorsForViewer(parts,colors,partColorByPart)→{objectName:hex}`; `model-3d-viewer.tsx` nạp `model3dStructuredUrl || model3dUrl` (structured để tô được; fused → getMaterialByName null → no-op), một `useEffect` áp màu trên event `load` + mỗi lần đổi lựa chọn; `product-detail.tsx` memo `viewerPartColors` từ state `partColorByPart` sẵn có. **Suy biến an toàn:** part chưa map / glb fused / object đổi tên → getMaterialByName null → bỏ qua, **KHÔNG bao giờ xám/crash** (giữ màu baked). **KHÔNG [MONEY]** — dùng `ColorView.hex` (đúng giá trị đã dùng cho swatch bg, guard ở filament editor per ADR-050), không đụng pricing/checkout; hex là DATA client, không phải markup. **a11y:** tô màu KHÔNG thêm chuyển động → `prefers-reduced-motion` không ảnh hưởng; viewer giữ `interaction-prompt=none`, không auto-rotate. **PDPL/gstatic:** glb (fused + structured) **KHÔNG nén** (trimesh export_glb, không Draco/KTX2) → model-viewer nạp trọn từ `src`, **zero fetch bên thứ 3** — giữ posture self-host (`model-3d-viewer.tsx`). **VERIFIED trong browser thật** (Chrome automation): nạp structured glb 2-object vào model-viewer 4.3.1 → getMaterialByName("Chao đèn"/"Đế") trúng theo tên object (dấu tiếng Việt OK) → setBaseColorFactor (hex-string VÀ RGBA-array) đổi pixel render đỏ→lục+lam (screenshot xác nhận). Đây là giả định cuối cùng plan gắn cờ "chưa verify" — nay chứng minh (model-viewer là CPU/browser, không box-gated). **Re-ingest** product cũ = bước ops (structured glb cũ chưa đặt tên material). **Đảo được:** storefront additive (bỏ = viewer nạp fused, không tô); ingest material-name additive (bỏ = material vô danh → no-op). **Loại:** (a) tô theo material INDEX thay tên (mong manh — thứ tự material không ổn định qua re-export); (b) đọc mesh→material qua API model-viewer (symbol-private, không public/ổn định); (c) đặt tên material ở f-4 từ đầu (không biết cần tới khi dựng viewer — gộp vào f-3, nhỏ); (d) truyền map objectName→material qua kênh phụ (thừa — đặt tên material tại nguồn là self-describing).

---

## Open / informational (chưa chốt, không chặn code)
- Pháp nhân: hộ kinh doanh vs công ty → ảnh hưởng ngưỡng thuế/hoá đơn điện tử (xem `compliance.md`).
- Địa chỉ/điểm nhận thật → quyết định Google Business Profile + ship nội thành (Ahamove/Grab) hay bỏ.
