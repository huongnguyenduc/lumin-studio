# Acceptance ledger — Lumin Studio

> **Mục đích:** acceptance criteria **máy-kiểm-được** cho 3 cụm bất biến xương sống, viết kiểu **EARS**
> (`WHEN <điều kiện>, the system shall <hành vi>`), mỗi dòng gắn **một test id** và bắt đầu **chưa tick** `[ ]`.
> Đây là dạng kiểm-được-bằng-máy của "Test P0" trong [`plan.md`](plan.md) — **không** phải nguồn chân lý mới
> (nguồn vẫn là `spec.md` §02/§04 + `conventions.md`). Bổ sung ADR-023.

> **Cách dùng (deterministic, không advisory):**
> - Một dòng được tick `[x]` **chỉ khi** test id liên kết đã tồn tại và **đang pass**.
> - **Phase-0 TODO:** thêm `packages/core` test `acceptance.ledger.test.ts` parse file này và **fail** nếu một dòng
>   `[x]` có test id không resolve được / không pass. Khi test đó tồn tại, `verify-before-stop` tự ép qua green-suite gate
>   — ledger trở thành cổng chặn thật, không chỉ là checklist.
> - `spec-guardian` chỉ **WARN** nếu một dòng có vẻ chưa được test (LLM enforce phủ định yếu); **test mới là gate**.
> - **EARS-per-feature (ADR-027):** feature có hành vi-invariant thật → lúc duyệt plan, append 1-3 dòng EARS +
>   test-id MỚI vào file này (đừng để ledger đứng yên ở 3 cụm Phase-0). Backbone (money/state) nên là
>   **property-test** generative + **mutation kill-gate** (ADR-027 · `plan.md` ARM).

## Cụm 1 — Order state machine (`spec.md` §04 · `domain-core.md`)

- [x] `OSM-01` — WHEN một đơn đổi trạng thái (mọi cặp `from × to`), the system shall đi qua **transition guard** chung và
  từ chối transition không hợp lệ. *(test: `order_state.transition_table`)*
- [x] `OSM-02` — WHEN bất kỳ transition hợp lệ nào xảy ra, the system shall **append** `statusHistory {from, to, at, byUser}`
  (đúng một bản ghi). *(test: `order_state.appends_status_history`)*
- [x] `OSM-03` — WHEN đơn chuyển sang `CANCELLED` hoặc `REFUNDED`, the system shall **bắt buộc** `reason` không rỗng (và `REFUNDED` cần `refundProofUrl`).
  *(test: `order_state.cancel_refund_requires_reason`)*
- [x] `OSM-04` — WHEN một staff (không phải owner) cố `reconcile → PAID`, the system shall **từ chối** (owner-only, ADR-010).
  *(test: `order_state.reconcile_paid_owner_only`)*
- [x] `OSM-05` — WHEN transition guard chạy theo `role`, the system shall enforce RBAC cho mọi (from, to, role) — staff
  không sửa cài đặt/STK. *(test: `order_state.rbac_matrix`)*

## Cụm 2 — Tiền (`conventions.md` §Tiền · ADR-019)

- [x] `MNY-01` — WHEN tính tổng đơn, the system shall tính ở **server** và đảm bảo `sum(parts) == total` (int VND, không
  thập phân). *(test: `money.parts_sum_equals_total`)*
- [x] `MNY-02` — WHEN client gửi `total`, the system shall **bỏ qua/ từ chối** giá trị đó và tính lại từ parts.
  *(test: `money.rejects_client_total`)*
- [x] `MNY-03` — WHEN format tiền hiển thị, the system shall dùng **một** formatter trong `packages/core` → `390.000₫`
  (không space, U+20AB); không nơi nào khác gọi `Intl.NumberFormat`/`toLocaleString`. *(test: `money.single_formatter` +
  ESLint `no-restricted-syntax`)*

## Cụm 3 — Checkout / thanh toán (`spec.md` §Order lifecycle · ADR-010)

- [x] `CHK-01` — WHEN khách ở màn QR tĩnh chưa đính ảnh CK, the system shall **chưa** tạo đơn. *(test:
  `checkout.no_order_before_proof`)*
- [x] `CHK-02` — WHEN khách đính ảnh biên lai + bấm xác nhận, the system shall `POST /orders` tạo đơn ở `PENDING_CONFIRM`
  kèm ảnh CK. *(test: `checkout.creates_order_on_proof`)*
- [x] `CHK-03` — WHEN tạo đơn hàng cá nhân hoá, the system shall yêu cầu tickbox "không đổi trả" + bước echo nội dung khắc
  **trước** thanh toán (ADR-012). *(test: `checkout.personalized_requires_ack`)*

## Cụm 4 — Relay outbox→NATS (`ADR-029` · `domain-core.md` §Outbox · `docs/plans/core-http-relay.md`)

> **Go-gated — CỐ Ý để `[ ]` ở ledger TS.** Parser `packages/core/test/acceptance.ledger.test.ts` chỉ enforce dòng
> `[x]` và **chỉ resolve test id TS** (`it()/test()` trong `packages/**/*.test.ts`); test của `REL-*` là **Go**
> (`services/core-api/internal/relay`), parser không thấy được → tick `[x]` sẽ làm parser ĐỎ (plan §5: cross-language
> id resolution out-of-scope của parser). Vậy `REL-*` GIỮ `[ ]` ở ledger này — **gate thật** là
> `tests/harness/guard.test.sh §ARM-GUARD` (khoá quét-tập-pending + relay-start-in-`main.go`) **+** chính các test Go
> đó (đã RAN xanh vs PG+NATS thật). `[ ]` ở đây = "không do parser-TS gác", KHÔNG phải "chưa test".

- [ ] `REL-01` — WHEN một dòng outbox đã commit ở `status='pending'` (kể cả tx seq-thấp commit **muộn** sau tx seq-cao đã publish), the system shall publish nó lên NATS JetStream bằng cách **quét cả TẬP pending `ORDER BY seq`**
  — KHÔNG watermark `seq>cursor`, KHÔNG `SKIP LOCKED` — theo thứ tự `publish → await PubAck → mark-published`, không
  mất event tiền. *(test: `relay.TestRelayLateLowSeqDrains` + `relay.TestRelayDrainsPendingToStream`)*
- [ ] `REL-02` — WHEN NATS unreachable hoặc stream chưa provision (no-responders), the system shall coi là **transient**:
  để cả batch `pending`, **KHÔNG** tăng `attempts`, re-ensure topology, drain khi NATS hồi (accept-downtime, ADR-009) —
  phân biệt với **poison** (PubAck reject trên broker reachable → `attempts++` → `failed` sau `RelayMaxAttempts`, không
  chặn head-of-line). *(test: `relay.TestRelayNoStreamTransientThenRecovers` + `relay.TestDrainTransientLeavesBatchPendingNoAttempts` + `relay.TestDrainPoisonQuarantinedAfterMaxAttempts`)*

## Cụm 5 — HTTP error envelope (`ADR-032` · `docs/plans/core-http-relay.md` §3d)

> **Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 4): test của `ERR-*` là **Go** (`services/core-api/internal/httpapi`),
> parser TS không resolve được → tick `[x]` sẽ làm parser ĐỎ. **Gate thật** = `guard.test.sh §ARM` (khoá router wire
> custom error hook + `mapError` map `TransitionError`) **+** chính các test Go đó. `[ ]` = "không do parser-TS gác".

- [ ] `ERR-01` — WHEN một domain error nổi lên biên HTTP (`*order.TransitionError` / `db.Err*` / `money.ErrInvalidAmount`), the system shall trả về **một `ErrorEnvelope {code, messageKey, fields?}`** (ADR-032) ở đúng status ánh xạ
  (`INVALID_EDGE`→409 · `RBAC`→403 · `REASON/REFUND/PROOF_REQUIRED`+`NO_ITEMS`+`INVALID_*`→422 · `NOT_FOUND`→404 ·
  `INVALID_ACTOR/TIMESTAMP`→400 · unmapped→500) và **KHÔNG BAO GIỜ** forward `TransitionError.Message` tiếng Việt ra wire
  (always-must #3 i18n; client map `messageKey`→next-intl). *(test: `httpapi.TestMapErrorTable` +
  `httpapi.TestMapErrorNeverLeaksDomainMessage` + `httpapi.TestDomainRouteReturns501Envelope`)*

## Cụm 6 — Auth self-issued login (`ADR-030` · `docs/plans/core-http-relay.md` §3e-1)

> **Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 4/5): test của `AUTH-*` là **Go**
> (`services/core-api/internal/{auth,httpapi,db}`), parser TS không resolve được → tick `[x]` sẽ làm parser ĐỎ. **Gate
> thật** = `guard.test.sh §ARM` (cookie HttpOnly + bcrypt-compare + `VerifyPassword(nil)` nhánh unknown-email) **+**
> chính các test Go đó (RAN Docker-free + vs PG thật). `[ ]` = "không do parser-TS gác", KHÔNG phải "chưa test".

- [ ] `AUTH-01` — WHEN một email không tồn tại **hoặc** mật khẩu sai gửi tới `POST /auth/login`, the system shall trả về **cùng một 401 đồng nhất** (unknown-email không phân biệt được với wrong-password — chống user-enumeration), luôn chạy đúng một lần bcrypt-compare để equalize timing, và **KHÔNG** set session cookie. *(test: `httpapi.TestLoginWrongPasswordUniform401` + `httpapi.TestLoginUnknownEmailUniform401` + `auth.TestVerifyPassword`)*
- [ ] `AUTH-02` — WHEN đăng nhập thành công, the system shall phát JWT ký (`sub`=users.id, `role`, `exp`=now+TTL) đặt trong cookie **httpOnly+Secure+SameSite** (token ngoài tầm JS — chống XSS-theft) và **KHÔNG** đưa token vào response body. *(test: `auth.TestIssueSetsSecureHttpOnlyCookie` + `auth.TestIssuedTokenCarriesClaims` + `httpapi.TestLoginSuccessSetsHttpOnlyCookieTokenNotInBody`)*

## Cụm 7 — Auth boundary + RBAC (`ADR-030` · `docs/plans/core-http-relay.md` §3e-2)

> **Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 4/5/6): test của `RBA-*` là **Go**
> (`services/core-api/internal/httpapi`), parser TS không resolve được → tick `[x]` sẽ làm parser ĐỎ. **Gate thật** =
> `guard.test.sh §ARM` (router wire `authMiddleware` không-nil + `resolveActor` `auth.Verify` + role đọc từ `UserByID`)
> **+** chính các test Go đó. `[ ]` = "không do parser-TS gác", KHÔNG phải "chưa test".

- [ ] `RBA-01` — WHEN một request tới một admin endpoint mà thiếu/hỏng session cookie **hoặc** mang vai trò không đủ (staff chạm owner-only `PATCH /admin/settings/bank-account`), the system shall **từ chối ở biên HTTP** trước khi vào handler — 401 `UNAUTHORIZED` khi thiếu/hỏng credential (token verify chữ ký+expiry, role đọc từ `users` row không tin claim), 403 `FORBIDDEN` khi credential hợp lệ nhưng role không đủ — và op chưa phân loại **fail-closed** (mặc định require actor). *(test: `httpapi.TestAuthMiddlewareRequiredRejectsMissingCookie` + `httpapi.TestAuthMiddlewareOwnerOnlyRejectsStaff` + `httpapi.TestClassifyFailsClosed` + `httpapi.TestAdminRouteUnauthenticatedReturns401Envelope`)*

## Cụm 8 — Order-intake pricing (`ADR-019` · `docs/plans/core-http-relay.md` §3f)

> **Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 4/5/6/7): test của `PRC-*` là **Go**
> (`services/core-api/internal/pricing`), parser TS không resolve được → tick `[x]` sẽ làm parser ĐỎ. **Gate thật** =
> `guard.test.sh §ARM` (pricing derive `BasePrice`+`PriceDelta`, `Selection` không mang giá client, mã đơn qua
> `nextval('order_code_seq')`) **+** chính các test Go đó (unit + property, RAN Docker-free). `[ ]` = "không do parser-TS gác".

- [ ] `PRC-01` — WHEN một dòng đơn được định giá lúc checkout, the system shall **tính `unitPrice` phía server** từ catalog (`base_price` + delta màu + Σ delta option) và **KHÔNG BAO GIỜ** tin giá client gửi (`Selection` không có trường giá), đồng thời từ chối lựa chọn không hợp lệ — màu/option không thuộc sản phẩm, màu `available:false`, option trùng, hoặc text khắc vượt `maxChars` (đếm theo rune) — trước khi đơn được tạo (ADR-019). *(test: `pricing.TestPriceItemIsSumOfCatalogParts` + `pricing.TestPriceItemRejectsInvalidSelection` + `pricing.TestPriceItemEngraveBoundary`)*
- [ ] `PRC-02` — WHEN phí vận chuyển được tính cho một địa chỉ, the system shall **tra `shippingFee` phía server** từ `settings.shipping_rules` theo `province` (khớp chính xác, hoặc rule `"*"` mặc định — KHÔNG có cấp quận/huyện, ADR-017) và trả lỗi (→422) khi không rule nào khớp thay vì âm thầm tính phí 0. *(test: `pricing.TestShippingFee` + `pricing.TestShippingFeeNoMatch` + `pricing.TestShippingFeeRejectsMalformed`)*

## Cụm 9 — Order transitions (`docs/plans/core-http-relay.md` §3h · locked #9 · §6 D12)

> **Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 4/5/6/7/8): test của `PAY-*`/`SHP-*` là **Go**
> (`services/core-api/internal/{httpapi,db}`), parser TS không resolve được → tick `[x]` sẽ làm parser ĐỎ. **Gate
> thật** = `guard.test.sh §ARM` (transition.go route money-in qua `ConfirmPaymentTx` + owner-gate biên `order.RoleOwner` +
> SHIPPING `SetTrackingCodeTx`) **+** chính các test Go đó (unit Docker-free + integration vs PG thật). `[ ]` = "không do parser-TS gác".

- [ ] `PAY-01` — WHEN owner đối soát `PENDING_CONFIRM→PAID` qua `POST /orders/{id}/transitions`, the system shall route qua **`ConfirmPaymentTx`** (emitter `order.paid` **DUY NHẤT**) phát **đúng một** `order.paid`, trong khi **mọi edge khác** đi `AdvanceStatusTx` và **KHÔNG** phát `order.paid` (footgun dispatch — locked #9), và **từ chối `staff` reconcile 403 ở biên** (`ConfirmPaymentTx` cố định `role=owner` nên domain guard không tự chặn). *(test: `httpapi.TestTransitionWalkEmitsPaidOnceAndPersistsTracking` + `httpapi.TestTransitionStaffReconcileForbidden`)*
- [ ] `SHP-01` — WHEN chuyển `PRINTING→SHIPPING`, the system shall **bắt buộc `trackingCode` không rỗng** (spec §04; thiếu → **422 `TRACKING_CODE_REQUIRED`** ở biên trước tx) và persist mã vào `orders.tracking_code` **trong CÙNG tx** với flip trạng thái (atomic — không bao giờ SHIPPING mà thiếu mã); QC packing-photo hoãn cùng upload surface (§0). *(test: `httpapi.TestTransitionWalkEmitsPaidOnceAndPersistsTracking` + `httpapi.TestTransitionShippingRequiresTrackingCode` + `db.TestSetTrackingCode`)*

## Cụm 10 — Checkout / order intake (`docs/plans/core-http-relay.md` §3g · critique BLOCKER · CHK-04/05)

> **Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 4/5/6/7/8/9): test của `CHK-*` là **Go**
> (`services/core-api/internal/httpapi`), parser TS không resolve được → tick `[x]` sẽ làm parser ĐỎ. **Gate thật** =
> `guard.test.sh §ARM` (checkout.go derive giá qua `pricing.PriceItem` + phí qua `pricing.ShippingFee` + inbox gate
> `errForbidden` + một seam `CreateOrderTx`) **+** chính các test Go đó (unit Docker-free + integration vs PG thật). `[ ]` = "không do parser-TS gác".

- [ ] `CHK-04` — WHEN khách tạo đơn web qua `POST /orders`, the system shall **bắt buộc `paymentProofUrl` là URL http(s) đúng `finalUrl` host/path/key-prefix do server upload signer cấu hình, kiểm tại HTTP boundary trước mọi DB read** (thiếu/malformed/foreign host/path → **422 `PROOF_REQUIRED`**), và đơn hợp lệ sinh ra ở `PENDING_CONFIRM` với genesis statusHistory `{from:null, to:PENDING_CONFIRM, byUser:"customer"}` cho guest (sentinel documented — ngoại lệ locked #6), tiền tính hoàn toàn phía server (client gửi `unitPrice`/`total`/`subtotal`/`shippingFee` → **400 loud-reject**, không âm thầm bỏ qua). *(test: `httpapi.TestCreateOrderWebRequiresPaymentProof` + `httpapi.TestCreateOrderWebRequiresHostPinnedPaymentProof` + `httpapi.TestCreateOrderRejectsClientMoneyFields` + `httpapi.TestCreateOrderWebEndToEnd`)*
- [ ] `CHK-05` — WHEN caller gửi `channel=inbox` tới `POST /orders`, the system shall **từ chối 403 FORBIDDEN trừ khi middleware optional-auth đã resolve một actor staff/owner** (inbox mint đơn born-`PAID` không cần proof — money-creation primitive, critique BLOCKER), và đơn inbox hợp lệ sinh ra `PAID` + stamp `payment_confirmed_at` + phát **đúng một** `order.created`, **KHÔNG** `order.paid` (born-PAID là creation, không phải reconcile). *(test: `httpapi.TestCreateOrderInboxRequiresStaffActor` + `httpapi.TestCreateOrderInboxAnonymousWire` + `httpapi.TestCreateOrderInboxStaffEndToEnd`)*

## Cụm 11 — Dashboard net revenue (`docs/plans/core-http-relay.md` §3i · spec §04)

> **Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 4..10): test của `DASH-*` là **Go**
> (`services/core-api/internal/{db,httpapi}`), parser TS không resolve được → tick `[x]` sẽ làm parser ĐỎ. **Gate thật**
> = `guard.test.sh §ARM` (dashboard.sql net-revenue theo `payment_confirmed_at` + loại `REFUNDED` + handler `hcmDayBounds`)
> **+** chính các test Go đó (unit Docker-free + integration vs PG thật). `[ ]` = "không do parser-TS gác".

- [ ] `DASH-01` — WHEN dashboard tính doanh thu hôm nay ở `GET /admin/dashboard`, the system shall cộng `total` của các đơn có `payment_confirmed_at` rơi trong NGÀY hôm nay (`Asia/Ho_Chi_Minh` UTC+7, cửa sổ nửa-mở `[start,end)` — anchor theo NGÀY THU TIỀN, KHÔNG theo ngày tạo, KHÔNG cắt UTC-midnight) và **loại** `REFUNDED` (giữ `CANCELLED`-sau-PAID vì shop giữ tiền, spec §04), **KHÔNG** dùng `status IN (…)` ngây thơ (rớt doanh thu CANCELLED-sau-PAID); `newOrdersToday` anchor theo ngày TẠO; zero-state trả 0 và `recentOrders` `[]` không rỗng (spec §03). *(test: `db.TestDashboardNetRevenue` + `db.TestDashboardWindowBoundary` + `httpapi.TestHcmDayBounds` + `httpapi.TestBuildDashboardSnapshot` + `db.TestDashboardZeroState`)*

## Cụm 12 — Admin settings / STK (`docs/plans/core-http-relay.md` §3k · conventions §57 · ADR-012)

> **Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 4..11): test của `STK-*` là **Go**
> (`services/core-api/internal/httpapi`), parser TS không resolve được → tick `[x]` sẽ làm parser ĐỎ. **Gate thật** =
> `guard.test.sh §ARM` (settings.go đổi STK qua `UpdateBankAccountTx` audit-on-commit + owner-gate `order.RoleOwner` +
> `changed_by` từ `actorFrom(ctx)` không body) **+** chính các test Go đó (unit Docker-free + integration vs PG thật). `[ ]` = "không do parser-TS gác".

- [ ] `STK-01` — WHEN owner đổi STK qua `PATCH /admin/settings/bank-account`, the system shall (a) chỉ cho **owner** ghi — staff bị chặn **403** ở biên `authOwnerOnly` **và** handler tự re-assert `order.RoleOwner` (defense-in-depth vì STK là money-out cao-giá-nhất — bad STK reroute mọi tiền khách), (b) validate VietQR field shape ngay tại HTTP boundary trước cả body-processing (`bin` **đúng 6 chữ số** napas, `accountNumber` toàn chữ số ≤19, `accountName` non-empty sau trim → **400 per-field loud-reject** — money-out field server render QR tĩnh, STK rác phải bị chặn), (c) ghi qua **`UpdateBankAccountTx`**: cột `settings.bank_account` **+** một row `setting_bank_audit` append-only trong **CÙNG một tx** (đổi STK không bao giờ land mà thiếu audit trail — conventions §57), và (d) `changed_by` lấy từ **actor context** (users.id) **KHÔNG** từ body. *(test: `httpapi.TestUpdateBankAccountEndToEnd` + `httpapi.TestUpdateBankAccountRejectsNonOwner` + `httpapi.TestCleanBankUpdate`)*

## Cụm 13 — Storefront catalog read (`docs/plans/phase-1-storefront.md` §2 · P1-a/P1-c/P1-d/P1-e)

> **Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 4..12): test của `CAT-*` là **Go**
> (`services/core-api/internal/httpapi` + `internal/contract`), parser TS không resolve được → tick `[x]` sẽ làm parser ĐỎ.
> **Gate thật** = `guard.test.sh §ARM` (CAT-01: active-only `db.ErrNotFound` non-leak + classify `authPublic`; CAT-02:
> list active-only tại SQL `ListActiveProducts status='active'` + `maxPageSize` bound + classify `GetProducts` authPublic; CAT-03:
> classify `GetCategories` authPublic **+** `ListCategories` scope active-product tại SQL (`EXISTS ... status='active'` — non-leak, category chỉ-chứa-hàng-ẩn không rò); CAT-04:
> search predicate TRONG cùng query active-only (`ListActiveProducts` giữ `status='active'` + `plainto_tsquery` + `immutable_unaccent` → search không leak hàng ẩn) + `maxSearchLen` bound) **+**
> `TestProductStatusParity`/`TestOptionTypeParity` (OpenAPI↔Postgres) **+** chính các test Go đó (unit Docker-free + integration vs PG thật). `[ ]` = "không do parser-TS gác".

- [ ] `CAT-01` — WHEN một client GET `/products/{slug}` công khai (không cần session), the system shall trả sản phẩm **active** kèm `colors[]` + `options[]` (giá int-VND thô `basePrice`/`priceDelta` — **KHÔNG** format phía server, always-must #2), và trả **404 `NOT_FOUND` đồng nhất** khi slug không tồn tại **hoặc** sản phẩm `draft`/`archived` (không phân biệt hàng ẩn với hàng không tồn tại — chống probe tồn-tại catalog), **KHÔNG** kèm `productType` (D-P1-1). *(test: `httpapi.TestGetProductBySlugEndToEnd` + `httpapi.TestProductDTO` + `httpapi.TestAuthMiddlewarePublicCatalogRunsWithoutCookie` + `contract.TestProductStatusParity`)*
- [ ] `CAT-02` — WHEN một client GET `/products` công khai (không cần session), the system shall trả **chỉ sản phẩm `active`** dưới dạng **card projection** phân trang (giá int-VND thô `basePrice` — **KHÔNG** format phía server always-must #2; **KHÔNG** colors/options/description → không N+1), **KHÔNG** để lộ `draft`/`archived` (lọc `status='active'` tại nguồn SQL — chống probe tồn-tại catalog như detail), lọc theo **category slug** (slug lạ → trang rỗng, **KHÔNG** 404; `category=`/`sort=` rỗng hoặc bỏ trống ⇒ coi như **bỏ lọc / sort mặc định** = tất cả·newest, không phải trang rỗng/400), sort theo **whitelist** (`newest`/`price_asc`/`price_desc`/`rating` — không bao giờ đưa text client thô vào `ORDER BY`), phân trang **có chặn** (`pageSize` ≤ 48 → quá-cap là **400 `VALIDATION`**, chống DoS trên endpoint public không rate-limit; offset trang-quá-xa không tràn), và hỗ trợ **conditional GET** (weak `ETag` + `Cache-Control`; `If-None-Match` khớp → **304** không body). *(test: `httpapi.TestGetProductsEndToEnd` + `httpapi.TestGetProductsRejectsBadParamsWithoutDB` + `httpapi.TestPageParams` + `httpapi.TestSortParam` + `httpapi.TestProductCardsDTO` + `httpapi.TestWeakETagDeterministicAndSensitive` + `httpapi.TestIfNoneMatch`)*
- [ ] `CAT-03` — WHEN một client GET `/categories` công khai (không cần session), the system shall trả **taxonomy category BROWSABLE** (mỗi category = `id`/`slug`/`name`; **chỉ** category có ≥1 hàng `active` — lọc `EXISTS ... status='active'` tại nguồn SQL) theo thứ tự **name→slug** (tổng-thứ-tự ổn định để `ETag` không nhảy), **KHÔNG** để lộ category chỉ-chứa `draft`/`archived` hoặc rỗng (chống dead-end chip + rò tên category chưa phát-hành — cùng non-leak như CAT-01/02), coi **không có category browsable là `[]`** (**KHÔNG** 404, **KHÔNG** `null` — spec §03 zero-state), giữ endpoint **public** (classify `authPublic` — chip duyệt không kẹt sau tường auth admin; không mang tiền), và hỗ trợ **conditional GET** (weak `ETag` + `Cache-Control` — cùng hình caching với `/products`; `If-None-Match` khớp → **304** không body). *(test: `httpapi.TestGetCategoriesEndToEnd` + `httpapi.TestCategoriesDTO`)*
- [ ] `CAT-04` — WHEN một client GET `/products?q=<term>` công khai (không cần session), the system shall **lọc catalog theo tìm-kiếm no-dấu** (ADR-016) trên **name + description** — "den" khớp "đèn" (fold dấu 2 phía qua `immutable_unaccent` migration 000012, **kể cả đ/Đ** — chữ có gạch không phân rã Unicode nên `translate` tường minh), term đi qua **`plainto_tsquery`** (KHÔNG nội suy text client thô vào SQL — cùng lẽ whitelist sort), predicate **ANDed TRONG scope active-only tại nguồn SQL** (search **KHÔNG BAO GIỜ** để lộ `draft`/`archived` — cùng non-leak như CAT-01/02/03), coi `q` rỗng/space là **bỏ tìm-kiếm** (toàn catalog, không phải trang rỗng), term **chặn độ dài** (`maxSearchLen`=100 rune → quá-cap **400 `VALIDATION`**, chống DoS/nội-suy trên endpoint public không rate-limit), giữ **sort/paginate/ETag không đổi** (envelope `total` phản ánh tập đã-tìm; **KHÔNG** relevance-rank — catalog nhỏ, tránh mở contract enum sort), classify **`authPublic`**. *(test: `httpapi.TestGetProductsSearch` [integration real-PG: đ-fold + active-only non-leak + AND category + description-searched + ETag-varies] + `httpapi.TestSearchParam` + `httpapi.TestGetProductsRejectsBadParamsWithoutDB` [q-over-cap→400])*

## Cụm 14 — Storefront price quote (`docs/plans/phase-1-storefront.md` §2 · P1-b · `docs/plans/phase-2-checkout.md` P2-b)

> **Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 13): test của `QTE-*` là **Go**
> (`services/core-api/internal/httpapi`), parser TS không resolve được → tick `[x]` sẽ làm parser ĐỎ.
> **Gate thật** = `guard.test.sh §ARM` (oapi stale-check + parity + testcontainers-boot, generic) **+**
> chính các test Go (unit Docker-free `priceQuoteLine`/`quoteTotals`/pre-DB + integration vs PG thật,
> chứng minh `authPublic` không cookie + `PRODUCT_UNAVAILABLE` non-leak + `messageKey` đúng).

- [ ] `QTE-01` — WHEN một client POST `/price/quote` công khai (không cần session) với một hoặc nhiều `items` (mỗi item = `OrderItemInput`, **KHÔNG** mang giá), the system shall **tính `unitPrice`/`lineTotal` mỗi dòng + `subtotal` phía server** từ catalog qua `pricing.PriceItem` (int-VND thô, **KHÔNG** shipping/address/tax, **KHÔNG** format phía server, always-must #2), từ chối lựa chọn không hợp lệ (màu/option lạ, `available:false`, trùng, khắc vượt `maxChars` đếm theo rune) → **422** kèm `messageKey` (KHÔNG lộ prose miền, ADR-032), coi sản phẩm không tồn tại/không `active` là **422 `PRODUCT_UNAVAILABLE`** đồng nhất (chống probe catalog), và giới hạn `items` ≤ 50 (chống khuếch-đại DoS trên endpoint public không rate-limit) → over-cap/empty/nil-body là lỗi shape (**400 `VALIDATION`** / **422 `NO_ITEMS`**). *(test: `httpapi.TestQuotePriceEndToEnd` + `httpapi.TestQuotePriceRejectionsEndToEnd` + `httpapi.TestPriceQuoteLineRejectsInvalidSelection` + `httpapi.TestQuoteTotalsCrossLineOverflow` + `httpapi.TestQuotePricePreDBRejections`)*
- [ ] `QTE-02` — WHEN một client POST `/price/quote` kèm `province` (P2-b), the system shall **gấp phí ship + tổng phía server** — tra `settings.shipping_rules` qua **`pricing.ShippingFee`** (cùng authority như checkout charge path) rồi `money.CalcTotals` → thêm `shippingFee`+`total` (int-VND thô), coi tỉnh không có rule (và không `*` wildcard) là **422 `NO_SHIPPING_RULE`** kèm `messageKey` (**KHÔNG** silent ₫0), coi `province` vắng/blank là **chỉ line/subtotal — byte-identical** pre-P2-b (`shippingFee`/`total` omitempty giữ nil), và giữ **PARITY**: money của quote **== số tiền `POST /orders` charge** cho cùng cart+tỉnh (kể cả surcharge khắc) vì cùng `money.LineItem{UnitPrice,Quantity}`+fee đi qua `money.CalcTotals` mà `CreateOrderTx.lineItems` chạy. *(test: `httpapi.TestQuotePriceParityWithOrder` [integration real-PG: cart CÓ KHẮC, quote==order field-by-field] + `httpapi.TestQuotePriceProvinceNoRule` [422 no-rule] + `httpapi.TestQuotePriceEndToEnd` [no-province nil-shipping byte-identical] + `httpapi.TestQuoteTotalsSumsLineTotals` [unit fold-fee])*

## Cụm 15 — Storefront guest order lookup (`docs/plans/phase-1-storefront.md` §2 · P1-n)

> **Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 13/14): test của `LKP-*` là **Go**
> (`services/core-api/internal/httpapi`), parser TS không resolve được → tick `[x]` sẽ làm parser ĐỎ.
> **Gate thật** = `guard.test.sh §ARM` (LKP-01: `subtle.ConstantTimeCompare` so SĐT + uniform `db.ErrNotFound`
> cho code-lạ==SĐT-sai non-leak + per-code token-bucket throttle `s.lookup.allow` + classify `LookupOrder` authPublic ·
> TRK-01: `hmac.Equal` constant-time verify token + `s.lookup.allow` per-code + main.go fail-fast `UsesForgeableTrackingSecret`)
> **+** chính các test Go đó (unit Docker-free limiter/DTO/normalize/429/signer + integration vs PG thật). `[ ]` = "không do parser-TS gác".

- [ ] `LKP-01` — WHEN một khách GET `/orders/lookup?code=&phone=` công khai (không cần session), the system shall yêu cầu **CẢ `code` LẪN `phone` khớp** — trả **404 `NOT_FOUND` byte-đồng-nhất** cho code lạ **HOẶC** SĐT sai (không phân biệt đơn tồn-tại — chống enumerate mã đơn), so sánh SĐT **constant-time** (`subtle.ConstantTimeCompare`, chạy dummy-compare cả trên nhánh code-miss để timing không rò — pattern AUTH-01), áp **token-bucket per-code** (429 `RATE_LIMITED`, `s.lookup.allow` trước khi đọc DB) chống dò SĐT của một mã đã biết (conventions §Bảo mật; WAF là lớp per-IP; **KHÔNG** failure-lockout — mã đơn tuần-tự nên lockout-theo-mã để attacker khoá chủ đơn thật khỏi đơn của họ, review wf_4ef2b511), và chỉ trả **`PublicOrderTimeline` tối thiểu** (`code`/`status`/`milestones{status,at}`/`trackingCode?`/`createdAt`) — **KHÔNG** bao giờ Order nội bộ (không customer PII/địa chỉ/tiền/proof/note/`byUser`/`reason`; ADR-032), giữ endpoint **public** (classify `authPublic`). *(test: `httpapi.TestLookupOrderEndToEnd` + `httpapi.TestLookupLimiter` + `httpapi.TestLookupOrderRateLimitReturns429Envelope` + `httpapi.TestPublicTimelineDTO` + `httpapi.TestNormalizePhone`)*
- [ ] `TRK-01` — WHEN một khách GET `/orders/track?code=&token=` công khai (không cần session/SĐT, link C3 `/o/{code}-{token}`, P2-i/D-P2-8), the system shall xác thực **token capability HMAC** `base64url(HMAC-SHA256(TRACKING_SECRET, orderCode))` bằng **constant-time compare** (`hmac.Equal`) đối chiếu token recompute từ `row.Code` — trả **404 `NOT_FOUND` byte-đồng-nhất** cho code lạ **HOẶC** token sai (present-but-wrong) (không phân biệt đơn tồn-tại — chống enumerate, chạy compare cả trên nhánh code-miss để timing không rò, pattern AUTH-01/LKP-01), **`code`/`token` THIẾU → 400 `VALIDATION`** (required-param, fire TRƯỚC DB → uniform bất kể đơn tồn tại, không phải oracle), áp **token-bucket per-code dùng CHUNG với lookup** (`s.lookup.allow` trước khi đọc DB → 429 `RATE_LIMITED`), chỉ trả **`PublicOrderTimeline` tối thiểu y hệt `/orders/lookup`** (byte-đồng-nhất, cùng `publicTimelineDTO`) — **KHÔNG** bao giờ Order nội bộ (ADR-032), giữ endpoint **public**; đồng thời `POST /orders` 201 trả **`trackingToken`** (schema `CreateOrderResult`, **chỉ** ở 201 — không lộ ở endpoint đọc nào khác) để FE dựng link theo dõi, với `TRACKING_SECRET` **fail-fast** khi forgeable (dev-secret không opt-in — `UsesForgeableTrackingSecret`, BLOCKER-F, như JWT_SECRET), **KHÔNG migration** (token deterministic, recompute lúc đọc → rotate secret vô hiệu mọi link cũ). *(test: `httpapi.TestTrackOrderEndToEnd` [integration real-PG] + `httpapi.TestTrackingSignerRoundTrip` [Docker-free] + `config.TestUsesForgeableTrackingSecret`)*

## Cụm 16 — Storefront product reviews (`docs/plans/phase-1-storefront.md` §2 · P1-l)

> **Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 13/14/15): test của `REV-*` là **Go**
> (`services/core-api/internal/httpapi`), parser TS không resolve được → tick `[x]` sẽ làm parser ĐỎ.
> **Gate thật** = `guard.test.sh §ARM` (REV-01: `ListReviewsByProduct` lọc `status='published'` tại nguồn SQL +
> handler `ProductStatusActive`→`db.ErrNotFound` non-leak + classify `GetProductReviews` authPublic) **+** chính
> các test Go đó (unit Docker-free `reviewsDTO`/pre-DB + integration vs PG thật chứng minh review ẩn KHÔNG rò). `[ ]` = "không do parser-TS gác".

- [ ] `REV-01` — WHEN một khách GET `/products/{slug}/reviews` công khai (không cần session), the system shall trả **CHỈ review `published`** cho sản phẩm **`active`** dưới dạng **danh sách phân trang mới-nhất-trước** (lọc `status='published'` tại **nguồn SQL** `ListReviewsByProduct` nên review ẩn/moderated-away KHÔNG bao giờ lọt ra — cùng non-leak `status='active'` của catalog), coi **slug lạ HOẶC sản phẩm nháp/lưu-trữ** đều **404 `NOT_FOUND` đồng nhất** (reviews của sản phẩm ẩn KHÔNG phục vụ, không probe tồn-tại catalog), **KHÔNG** lộ danh tính người đánh giá (projection bỏ `customer_id` — không PII công khai, PDPL), chỉ trả nội dung review (`id`/`rating`/`body`/`images`/`reply?`/`createdAt`; `reply` null tới khi shop trả lời) với `pageSize` ≤ 48 (over-cap → **400 `VALIDATION`**, chống DoS trên endpoint public không rate-limit), phát **weak ETag** + `Cache-Control` (If-None-Match khớp → **304** không body), và giữ endpoint **public** (classify `authPublic`). *(test: `httpapi.TestGetProductReviewsEndToEnd` + `httpapi.TestReviewsDTO` + `httpapi.TestReviewDTOCarriesNoAuthorIdentity` + `httpapi.TestReviewsDTOCorruptJSONBHardFails` + `httpapi.TestGetProductReviewsRejectsBadParamsWithoutDB`)*

## Cụm 17 — Storefront home grid (FE) (`docs/plans/phase-1-storefront.md` §3 · P1-f)

> **CỐ Ý để `[ ]`** (cùng tinh thần Cụm 13..16, khác lý do): test của `SF-*` là **TS storefront**
> (`apps/storefront/test/*.test.ts`), nhưng ledger parser (`packages/core/test/acceptance.ledger.test.ts`)
> chỉ quét `packages/**` → KHÔNG resolve được test-id ở `apps/**` → tick `[x]` sẽ làm parser ĐỎ.
> **Gate thật** = chính các test TS đó (`pnpm --filter @lumin/storefront test`, chạy trong `app-gates`) **+**
> `import 'server-only'` (biến biên client-bundle thành lỗi-biên-dịch) **+** ESLint no-Intl (MNY-03). `[ ]` = "không do parser-TS packages gác".

- [ ] `SF-01` — WHEN một caller gọi POST `/api/revalidate` để xoá cache catalog (on-write purge), the system shall chỉ chấp nhận khi trình đúng header `x-revalidate-secret` khớp `REVALIDATE_SECRET` (so sánh **hằng-thời-gian** `timingSafeEqual`) rồi mới `revalidateTag('catalog')`, và **fail-CLOSED**: `REVALIDATE_SECRET` chưa cấu hình → **500 đóng** (KHÔNG bao giờ mở purge vô danh), thiếu/sai/lệch-độ-dài secret → **401**, không rò gì ngoài `{revalidated:false}` — secret cao-entropy là phòng-thủ duy nhất nên một purge vô danh (cache-thrash DoS) bị chặn như một write. *(test: `apps/storefront/test/revalidate-auth.test.ts` → `verifyRevalidateSecret`: closed-on-unset · 401-missing · 401-wrong · 401-wrong-length · 200-match)*
- [ ] `SF-02` — WHEN storefront chiếu một API `ProductCard` sang thẻ lưới home (`toProductCardView`) và fetch server-side, the system shall giữ `basePrice` **int-VND thô** (chỉ `@lumin/core formatVnd` định dạng ở hạ nguồn qua `PriceTag`, KHÔNG tiền-định-dạng, KHÔNG `Intl` trong storefront), lấy **`images[0]` làm ảnh bìa** (ADR-007 sprite-first) và **thu về `undefined`** khi không có ảnh (images rỗng HOẶC chuỗi-rỗng → placeholder, không bao giờ `src=""`), đưa `ratingAvg` null → `null` (ẩn khối Rating), và đọc catalog **server-only** (`CORE_API_URL` + `createApiClient` KHÔNG lọt client bundle — `import 'server-only'` gác ở biên-dịch). *(test: `apps/storefront/test/catalog.test.ts` → `toProductCardView`: int-VND-raw · images[0]-cover · empty/empty-string→undefined · null-rating)*

## Cụm 18 — Storefront product detail (FE) (`docs/plans/phase-1-storefront.md` §3 · P1-h)

> **CỐ Ý để `[ ]`** (cùng lý do Cụm 17): test của `SF-*` là **TS storefront** (`apps/storefront/test/*.test.ts`),
> ledger parser chỉ quét `packages/**` → tick `[x]` sẽ làm parser ĐỎ. **Gate thật** = chính test TS đó
> (`pnpm --filter @lumin/storefront test`, chạy trong `app-gates`) **+** `import 'server-only'` ở `lib/catalog`
> (gác biên client-bundle) **+** ESLint no-Intl (MNY-03).

- [ ] `SF-03` — WHILE khách xem trang chi tiết `/san-pham/{slug}` (`ProductDetail`), the system shall **khoá nút "Thêm vào giỏ"** (disabled) tới khi khách chọn một màu **còn hàng** — quyết định khoá là hàm thuần `canAddToCart(selectedColorId, colors)`: chưa chọn (`null`) → khoá; chọn màu `available:false` → **vẫn khoá** (hết hàng KHÔNG bao giờ mở khoá); chọn id không thuộc sản phẩm → khoá; sản phẩm KHÔNG có màu nào → khoá không áp dụng (mở, vì không có gì để chọn). Giá hiển thị là `basePrice` int-VND thô qua `PriceTag`/`formatVnd` — **KHÔNG cộng `priceDelta` màu/tuỳ-chọn ở client** (tổng tính ở server qua `POST /price/quote`, P1-k). *(test: `apps/storefront/test/product-detail-view.test.ts` → `canAddToCart`: null-khoá · available-mở · unavailable-khoá · unknown-id-khoá · no-colors-mở)*
- [ ] `SF-04` — WHEN core-api trả một màu `available:false` cho trang chi tiết, the system shall render swatch đó **không chọn được** (`disabled`, `aria-label` "{name} — tạm hết") và hiện copy hết-hàng `core.errors.colorOutOfStock` ("Màu này tạm hết nhựa — chọn màu khác nha."), đồng thời `toProductDetailView` **giữ nguyên cờ `available` + `priceDelta` từng màu** (nguồn của khoá + out-of-stock); và một slug lạ HOẶC nháp/lưu-trữ đều `notFound()` **đồng nhất** (không rò tồn-tại catalog). *(test: `apps/storefront/test/product-detail-view.test.ts` → `toProductDetailView` giữ available/priceDelta · `isColorSelectable`; 404-uniform gác bởi handler P1-a `CAT-01`)*

## Cụm 19 — Storefront engrave / personalize (FE) (`docs/plans/phase-1-storefront.md` §3 · P1-j)

> **CỐ Ý để `[ ]`** (cùng lý do Cụm 17/18): test của `SF-*` là **TS storefront** (`apps/storefront/test/*.test.ts`),
> ledger parser chỉ quét `packages/**` → tick `[x]` sẽ làm parser ĐỎ. **Gate thật** = chính test TS đó
> (`pnpm --filter @lumin/storefront test`, chạy trong `app-gates`) **+** ESLint no-Intl (MNY-03: đếm ký tự dùng
> `Array.from`/`normalize`, KHÔNG `Intl`). Nguồn chân lý hạn khắc = server `pricing.validateEngrave` (rune count).

- [ ] `SF-05` — WHILE khách nhập khắc tên (một `text` option) trên trang chi tiết `/san-pham/{slug}`, the system shall đếm ký tự **mirror server rune-count** (`engraveLength` = đếm **code point thô** qua `Array.from(text)` — KHÔNG `.length` UTF-16, KHÔNG grapheme `Intl.Segmenter`, **KHÔNG NFC-normalize** vì server cũng không normalize; khớp `utf8.RuneCountInString(p.Text)` **thô** của `pricing.validateEngrave` cho **cùng chuỗi**, kể cả NFD — user-confirmed 2026-07-04) và **khoá "Thêm vào giỏ"** khi bất kỳ khắc nào vượt `maxChars` (`canAddToCartWithOptions` = colour-lock AND mọi engraving trong hạn), coi text rỗng/chỉ-khoảng-trắng **không bao giờ khoá** (khắc là tuỳ chọn — mirror `TrimSpace==""`), `maxChars` null = không giới hạn, khoảng-trắng-cuối **tính vào hạn** (đúng như server), và over-limit hiện `role=alert` `productDetail.engraveTooLong` + `maxChars` báo qua Input `hint` (native-associated, không clobber) — **KHÔNG cộng `priceDelta` ở client** (tổng server-authoritative qua `POST /price/quote`, P1-k). *(test: `apps/storefront/test/product-detail-view.test.ts` → `engraveLength` ASCII/NFC/**NFD-raw=2**/non-BMP · `isEngraveWithinLimit` blank/null/limit/trailing-space · `canAddToCartWithOptions` truth-table)*
- [ ] `SF-06` — WHEN core-api trả `options[]` cho trang chi tiết, the system shall chiếu qua `toProductDetailView` **giữ `id`/`label`/`description`/`type`/`priceDelta`** và collapse `maxChars` absent/null → `null`, render `type:text` thành **engrave field** (label = `option.label` + live preview + counter) và `type:choice` thành **add-on toggle** (label + `priceDelta` qua `PriceTag`/`@lumin/core`; contract KHÔNG có sub-`values[]` → chọn/bỏ **boolean**), giữ selection **UI-only** tới khi cart lấy live total (P1-k), và **KHÔNG** dựng zoneId UI (§5 DROP — server nhận `zoneId` free-form, chỉ validate text + rune hạn; zone picker draggable-orb là P1-i). *(test: `apps/storefront/test/product-detail-view.test.ts` → `toProductDetailView` options mapping + `maxChars` null-collapse)*

## Cụm 20 — Storefront cart (FE) (`docs/plans/phase-1-storefront.md` §3 · P1-k)

> **CỐ Ý để `[ ]`** (cùng lý do Cụm 17/18/19): test của `SF-*` là **TS storefront** (`apps/storefront/test/*.test.ts`),
> ledger parser chỉ quét `packages/**` → tick `[x]` sẽ làm parser ĐỎ. **Gate thật** = chính test TS đó
> (`pnpm --filter @lumin/storefront test`, chạy trong `app-gates`) **+** ESLint no-Intl (MNY-03) — tiền chỉ qua
> `PriceTag`/`@lumin/core formatVnd`, tạm tính **server-authoritative** (`POST /price/quote`). Ranh giới: **KHÔNG checkout**.

- [ ] `SF-07` — WHEN khách thêm/sửa/xoá món trong giỏ `/gio-hang`, the system shall giữ giỏ qua reload (localStorage) và áp các reducer **thuần**: gộp theo **cấu hình** (`cartLineKey` = product + colour + `optionIds`-sorted + engrave-text ⇒ cùng cấu hình cộng dồn `quantity`), **giảm-tại-1 → xoá** (`setItemQuantity` với `qty≤0` loại dòng), clamp `1..MAX_QUANTITY`, chặn vượt `MAX_LINES`, và `sanitizeCart` **bỏ mọi entry localStorage hỏng/lệch-schema** (đọc không bao giờ ném) — **KHÔNG cộng tiền ở client** (CartItem không mang giá; conventions §Tiền). *(test: `apps/storefront/test/cart.test.ts` → `cartLineKey` · `buildCartItem` (engrave off optionIds, blank=none, key-merge) · `addItem` (merge/append/clamp/`MAX_LINES`/no-mutate) · `setItemQuantity` (dec-at-1-remove/clamp) · `removeItem` · `sanitizeCart`)*
- [ ] `SF-08` — WHEN giỏ được định giá, the system shall lấy **tạm tính + mọi line total CHỈ từ `POST /price/quote`** (server-authoritative) qua Server Action `quoteCart` (client KHÔNG đọc `CORE_API_URL`), với `cartQuoteItems` **gấp engrave option id vào `optionIds`** + bỏ `colorId` khi null + **KHÔNG gửi personalization/giá** lên wire (§5: `zoneId` free-form, tái nhập ở Phase-2 checkout), giữ line **đúng thứ tự index** với response, và **KHÔNG có checkout** — footer chỉ hiện tạm tính + ghi chú ship-tính-sau, **zero order/payment/address code** (ranh giới Phase-1/2 cứng, plan §0). *(test: `apps/storefront/test/cart.test.ts` → `cartQuoteItems` (fold engrave · omit null colorId · order) · `cartSignature` · `cartCount`)*

## Cụm 21 — Storefront customer auth realm (`docs/plans/phase-1-storefront.md` §2 · P1-r · ADR-030)

> **Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 13..16): test của `CUST-*` là **Go**
> (`services/core-api/internal/httpapi`), parser TS không resolve được → tick `[x]` sẽ làm parser ĐỎ.
> **Gate thật** = `guard.test.sh §ARM` (CUST-01: `resolveCustomer` verify qua issuer **customerAuth** riêng + classify `RegisterCustomer`/`LoginCustomer`/`LogoutCustomer` **authPublic** & `GetCustomerOrders` **authCustomer**; CUST-02: `ListOrdersByCustomer` scope `customer_id` tại SQL + main.go `UsesForgeableCustomerJWTSecret` fail-fast) **+** chính các test Go đó (unit Docker-free realm-isolation + integration register→login→orders vs PG thật). `[ ]` = "không do parser-TS gác".

- [ ] `CUST-01` — WHEN một khách POST `/customer/register` (name+email+phone+password) hoặc `/customer/login` (email+password) công khai, the system shall xác thực ở **realm khách RIÊNG** (`auth.CustomerCookieName=lumin_customer` + secret `CUSTOMER_JWT_SECRET` khác admin ⇒ **token admin KHÔNG BAO GIỜ** verify được như session khách và ngược lại — cô lập mật-mã, ADR-030), băm mật khẩu **bcrypt** (không lưu/không trả clear), phát JWT ký đặt trong cookie **httpOnly+Secure+SameSite** (token **KHÔNG** trong body — chặn XSS-theft), coi **email đăng-nhập unique không-phân-biệt-hoa-thường** (partial-unique `lower(email) WHERE password_hash IS NOT NULL` ⇒ trùng khi register là **409 `EMAIL_TAKEN`** do DB, không có race find-then-insert), trả **401 đồng nhất** cho email-lạ **và** sai-mật-khẩu (chạy một bcrypt-compare cả trên nhánh không-tồn-tại/nil-hash — timing không rò, không enumerate), và **KHÔNG cho** một row khách guest (`password_hash IS NULL`) đăng nhập dù trùng SĐT/email. *(test: `httpapi.TestCustomerRegisterLoginFlow` [integration real-PG] + `httpapi.TestResolveCustomerRealmIsolation` + `httpapi.TestRegisterCustomerValidation` + `httpapi.TestClassifyCustomerRealm`)*
- [ ] `CUST-02` — WHEN một khách **đã đăng nhập** GET `/customer/orders`, the system shall trả **chỉ đơn của chính họ** (scope cứng theo `customer_id` từ subject session đã-verify tại SQL `ListOrdersByCustomer` — id lạ ⇒ danh sách rỗng, không bao giờ đơn người khác) dưới dạng **`PublicOrderTimeline` như guest-lookup** (`code`/`status`/`milestones`/`trackingCode?`/`createdAt` — **KHÔNG** tiền/PII/địa chỉ/proof nội bộ, ADR-032), đòi **session khách hợp lệ** (thiếu/hỏng/cookie-admin ⇒ **401** — classify `authCustomer`, resolve qua issuer khách; đơn guest đặt trước khi register **KHÔNG** tự-liên-kết — claiming SĐT chưa xác minh là lỗ bảo mật, hoãn), và server **fail-fast** khi `CUSTOMER_JWT_SECRET` là dev-secret không opt-in (`UsesForgeableCustomerJWTSecret` — token khách giả-mạo-được ⇒ đọc lịch sử đơn người khác, rò PII). *(test: `httpapi.TestGetCustomerOrdersScoped` [integration real-PG: scope + router 200 + no-PII-leak] + `httpapi.TestCustomerOrdersRequiresCustomerSession` + `config` fail-fast tests)*

## Cụm 22 — Checkout config + STK gate (`docs/plans/phase-2-checkout.md` P2-a · D-P2-1 · conventions §Bảo mật)

> **Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 4..12): test của `CHK-06/07` là **Go**
> (`services/core-api/internal/httpapi`), parser TS không resolve được → tick `[x]` sẽ làm parser ĐỎ. **Gate thật** =
> `guard.test.sh §ARM` (checkout_config.go dựng `vietqrUrl` từ STK đã lưu qua `vietQRImageURL` không nhận input client +
> classify `GetCheckoutConfig` **authPublic**; checkout.go web-create gác `errNoSTKConfigured` khi STK trống) **+** chính
> các test Go đó (unit Docker-free helper + integration vs PG thật). `[ ]` = "không do parser-TS gác".

- [ ] `CHK-06` — WHEN storefront GET `/checkout/config` công khai (không session), the system shall trả **whitelist** đúng bốn trường — `bankAccount` (STK), `vietqrUrl` **dựng server-side từ STK đã lưu** (`img.vietqr.io`, D-P2-1: KHÔNG nhận field client, KHÔNG amount/memo → client không tráo được tài khoản đích, conventions §Bảo mật/ADR-010; `accountName` URL-escape), `shippableProvinces` (**keys `settings.shipping_rules`, loại `*` wildcard** + dedup, non-nil `[]`), `refundPolicy` (đổi-trả pre-purchase, compliance §3) — và **KHÔNG lộ** `shopInfo`/PII/bảng phí ship/field settings khác, coi shop **chưa cấu hình STK** (`bank_account` trống/`{}`/thiếu `bin`+`accountNumber`) là **422 `NO_STK_CONFIGURED`** (KHÔNG half-config với QR không render được), và `shipping_rules` hỏng là **500** (server config fault, không rò partial). *(test: `httpapi.TestGetCheckoutConfigEndToEnd` [integration real-PG] + `httpapi.TestGetCheckoutConfigNoSTKConfigured` + `httpapi.TestStkFromSettingsGate` + `httpapi.TestVietQRImageURLServerBuilt` + `httpapi.TestShippableProvincesExcludesWildcard`)*
- [ ] `CHK-07` — WHEN khách tạo đơn **web** qua `POST /orders` mà shop **chưa cấu hình STK**, the system shall **từ chối 422 `NO_STK_CONFIGURED` TRƯỚC mọi write** (không có STK thì không có cách nhận thanh toán web — cùng tín hiệu `/checkout/config` cho) và **KHÔNG để lại** row `customers`/`orders` nào (gate chạy trước tx), trong khi đơn **inbox** (staff tạo, born-`PAID`, CHK-05) **KHÔNG** cần STK lúc tạo. *(test: `httpapi.TestCreateOrderWebRequiresSTK` [integration real-PG: 422 + zero-row] + `httpapi.TestCreateOrderInboxStaffEndToEnd` [inbox không cần STK])*

## Cụm 23 — Payment-proof upload (`docs/plans/phase-2-checkout.md` P2-c · D-P2-2 · ADR-035)

> **Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 4..22): test của `CHK-08` là **Go**
> (`services/core-api/internal/{config,httpapi}`), parser TS không resolve được → tick `[x]` sẽ làm parser ĐỎ. **Gate
> thật** = OpenAPI strict-server/codegen stale-check + chính các test Go Docker-free (policy signing, router authPublic,
> config defaults/env). `[ ]` = "không do parser-TS gác".

- [ ] `CHK-08` — WHEN storefront cần upload ảnh biên lai trước `POST /orders`, the system shall cấp qua `POST /checkout/payment-proof-upload` một **presigned POST** Garage/S3 công khai (không session) với key server-generated không chứa PII, `finalUrl` **host-pinned** từ cấu hình server, policy ngắn hạn (≤5 phút) có **`content-length-range` ≤10MB** và exact `Content-Type` chỉ `image/jpeg|image/png|image/webp` (oversize/wrong-mime bị S3 policy từ chối — KHÔNG dựa client khai size), form fields SigV4 không lộ secret, public signer có token-bucket backstop (**429 `RATE_LIMITED`**), và config upload thiếu/sai **fail-closed** (500 generic, không phát URL giả/partial). *(test: `proofstore.TestStorePresignsHostPinnedPostPolicy` + `proofstore.TestStoreOwnsOnlyHostPinnedURLs` + `proofstore.TestStoreRejectsBadInput` + `httpapi.TestCreatePaymentProofUploadHandler` + `httpapi.TestCreatePaymentProofUploadRateLimited` + `httpapi.TestCreatePaymentProofUploadPublicRoute` + `config.TestLoadPaymentProofUploadDefaults` + `config.TestLoadHonoursPaymentProofUploadEnv`)*
- [ ] `CHK-09` — WHEN một đơn đã ở trạng thái terminal (`COMPLETED`/`CANCELLED`/`REFUNDED`) lâu hơn thời hạn lưu (mặc định 90 ngày tính từ `orders.updated_at` = mốc chuyển terminal, KHÔNG từ lúc upload), the system shall — qua sweeper nền `internal/retention` chạy khi uploads được cấu hình — **xoá object biên lai khỏi Garage RỒI mới null `payment_proof_url`** (object-first: crash giữa chừng để lại ref tới object đã xoá → sweep sau re-select + re-clear idempotent, KHÔNG bao giờ ref sạch tới object còn sống), **chỉ** xoá URL host-pinned do server cấp (URL lạ → không gọi S3 nhưng vẫn null ref theo retention), lỗi một đơn được log-and-skip không chặn batch, và orphan upload (upload không thành đơn, không có mốc terminal) do **bucket lifecycle rule** (infra/README) dọn — backstop. *(test: `retention.TestSweepDeletesObjectThenClearsReference` + `retention.TestSweepKeepsReferenceWhenObjectDeleteFails` + `retention.TestSweepClearsReferenceEvenWhenObjectNotOwned` + `retention.TestSweepSkipsNilProof` + `retention.TestNewClampsNonPositiveConfig` + `proofstore.TestStoreDeleteIgnoresForeignURLs` + `db.TestPurgeableProofOrders` [integration real-PG])*

## Cụm 24 — Storefront checkout info step (FE) (`docs/plans/phase-2-checkout.md` P2-d · C1 · compliance §2/§3)

> **CỐ Ý để `[ ]`** (cùng lý do Cụm 17..20): test của `CHK-10` là **TS storefront**
> (`apps/storefront/test/checkout-form.test.ts`), ledger parser chỉ quét `packages/**` → tick `[x]` sẽ làm parser ĐỎ.
> **Gate thật** = chính test TS đó (`pnpm --filter @lumin/storefront test`, chạy trong `app-gates`) **+** ESLint no-Intl
> (tiền chỉ qua `PriceTag`/`@lumin/core formatVnd`, tổng **server-authoritative** qua `POST /price/quote` kèm `province`)
> **+** đổi-trả/PDPL disclosure render (Playwright Phase 5). Ranh giới: **KHÔNG submit** — `POST /orders` là P2-f.

- [ ] `CHK-10` — WHEN khách vào `/thanh-toan` (C1, guest-first) đọc giỏ và điền thông tin giao hàng, the system shall **validate phía client mirror server** (`validateCheckoutForm` = `checkout.go intake.validate`): tên **2–60 rune** (đếm rune KHÔNG UTF-16, trim), SĐT `^(0|\+84)\d{9}$` **strip whitespace** trước match (`normalizePhone`: "0901 234 567" → nộp "0901234567"), email **optional** (blank bỏ qua; có thì "chứa @"), `province`/`ward`/`street` **non-blank** (địa chỉ tỉnh→phường→đường, KHÔNG district ADR-017), map ra payload **đúng shape server** (`{customer{name,phone,email?}, shippingAddress{province,ward,street}, note?}` — email/note bỏ khi rỗng), tính **Tạm tính+Phí ship+Tổng CHỈ từ `POST /price/quote` kèm province** (server-authoritative, KHÔNG client-math; tỉnh không rule → `no_shipping_rule` chặn continue), và hiện **đổi-trả disclosure + link `/chinh-sach` cho MỌI giỏ** + **PDPL privacy notice unbundled không marketing-tick** TRƯỚC thanh toán (compliance §2/§3). *(test: `apps/storefront/test/checkout-form.test.ts` → `validateCheckoutForm` (name rune-bound · phone regex+normalize · email optional · required address · multi-error · server-shaped mapping) · `normalizePhone`)*

## Cụm 25 — Storefront checkout payment step (FE) (`docs/plans/phase-2-checkout.md` P2-f · C2/C2½ · D-P2-7)

> **CỐ Ý để `[ ]`** (cùng lý do Cụm 17..24): test của `CHK-11` là **TS storefront**
> (`apps/storefront/test/*`), không phải Go — parser ledger `packages/core` chỉ resolve id test TS ở app-gates.

- [ ] `CHK-11` — WHEN khách ở bước thanh toán (C2) quét VietQR + đính ảnh biên lai + bấm "Xác nhận đặt đơn", the system shall build body `POST /orders` qua `buildWebOrderInput`: `channel=web`, `customer`/`shippingAddress` từ C1, `items` có **priced-fields (`productId`/`colorId`/`optionIds`/`quantity`) LẤY THẲNG từ `cartQuoteItems`** (parity by construction — đơn KHÔNG thể tính khác tổng khách vừa thấy), dòng có khắc kèm `personalization{text, zoneId=engrave.optionId}` (zoneId non-blank server yêu cầu, §5 free-form; blank-text bị drop mirror `personalizationFrom` trim), acks ADR-012 map **1:1** khi personalized, `paymentProofUrl` = `finalUrl` **host-pinned** từ presigned POST (P2-c) — ảnh upload **browser→Garage trực tiếp**, KHÔNG qua core-api — và **`note` KHÔNG gửi VÀ KHÔNG echo trên màn review** (`CreateWebOrderInput` không có field `note`; echo trên confirm screen sẽ ngụ ý đã lưu → ẩn; vẫn thu ở C1, gap contract deferred); submit **disable tới khi proof xong + total settled** (double-submit guard D-P2-7) + **C2½ full-screen loading** khi in-flight (KHÔNG chỉ disable nút); map lỗi **422 `NO_STK_CONFIGURED`/`NO_SHIPPING_RULE`** ra copy riêng + phần còn lại một `error` loud-reject (client gửi ZERO giá → reject = retry, không âm thầm đi tiếp); shop **chưa cấu hình STK** → closed-notice thay QR hỏng; sau **201** xoá giỏ + hiện confirmation tối thiểu **giữ `trackingToken`** (wait-screen đầy đủ + link `/o/{code}-{token}` = P2-g). *(test: `apps/storefront/test/checkout-form.test.ts` → `buildWebOrderInput` (plain mapping · quote-parity `toMatchObject` · engrave `personalization`+zoneId+acks · blank-engrave drop · `note` excluded))*

## Cụm 26 — Admin login + auth-redirect (FE) (`docs/plans/phase-3-admin.md` P3-a · ADR-030)

> **CỐ Ý để `[ ]`** (cùng lý do Cụm 17..25): test của `ADM-01` là **TS admin** (`apps/admin/test/*`), không phải Go —
> parser ledger `packages/core` chỉ resolve id test TS ở app-gates. **Nền:** ADR-030 (self-issued JWT) là nguồn chân lý;
> "Cloudflare Access" doc-drift đã **synced** khỏi `conventions.md §Bảo mật` + `.claude/rules/admin.md` + `plan.md` + `architecture.md` (BLOCKER-E done).

- [ ] `ADM-01` — WHEN owner/staff đăng nhập ở `/dang-nhap` (email+password), the system shall gọi core-api `POST /auth/login` **server-side (BFF)** rồi **re-issue** session cookie `lumin_session` trên host admin (httpOnly + SameSite=Strict + Secure-khi-prod + Max-Age lấy từ upstream, token KHÔNG chạm client JS) → điều hướng `/`; ánh xạ **401/400 → thông báo "sai email hoặc mật khẩu" ĐỒNG NHẤT** (không enumeration, mirror core-api 401), 5xx/mạng → "thử lại" (không set cookie); một request **thiếu cookie** chạm route admin bị `middleware` redirect `/dang-nhap` **trước khi** render (còn cookie hỏng/hết hạn = core-api 401, không ping-pong vì Max-Age==JWT TTL); đã đăng nhập chạm `/dang-nhap` → redirect `/`; **đăng xuất** xoá cookie → request sau về login (JWT stateless, không revoke server-side). *(test: `apps/admin/test/auth.test.ts` → `parseSessionCookie` (value+Max-Age, Clear-cookie=null, non-positive Max-Age drop) + `login` action (200 set-cookie · 401→invalid no-set · 5xx→error · 200-no-cookie fail-closed · throw→error))*

## Cụm 27 — Admin orders list (BE) (`docs/plans/phase-3-admin.md` P3-b · spec §04)

> **Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 4..12): test của `ADM-02` là **Go**
> (`services/core-api/internal/{db,httpapi}`), parser TS không resolve được → tick `[x]` sẽ làm parser ĐỎ. **Gate thật** =
> chính các test Go đó (unit Docker-free `adminOrderSummariesDTO`/page/status-filter + integration vs PG thật: filter ·
> phân trang · newest-first · join khách/SP/số-món) **+** classify fail-closed (`GetAdminOrders`→`authRequired`). `[ ]` = "không do parser-TS gác".

- [ ] `ADM-02` — WHEN admin gọi `GET /admin/orders` (owner **hoặc** staff, `authRequired` — thiếu cookie → 401 tại boundary trước handler), the system shall trả **một trang** order summary **mới-nhất-trước** (`created_at` DESC, `id` DESC làm tiebreak để phân trang ổn định), **tuỳ chọn lọc theo một `status`** (bỏ param = tất cả, "Tất cả"), kèm envelope `page`/`pageSize`/`total` (total = số đơn khớp filter qua mọi trang); mỗi hàng mang `code`/`customerName` (join `customers`)/`firstItemName`+`itemCount` (cho cột "sản phẩm", subquery bounded no-N+1)/`channel`/`status`/`total` (**raw int-VND**, không format server, always-must #2)/`createdAt`; `page<1`·`pageSize<1`·`pageSize>50`·`status` ngoài enum → **400 trước mọi read** (oapi không enforce min/max/enum); zero-state → `items` `[]` không-null + `total` 0 (spec §03); **KHÔNG** dùng `PublicOrderTimeline` whitelist (đây là projection nội bộ có PII/kênh/tiền, ADR-032). *(test: `httpapi.TestAdminOrderSummariesDTO` + `TestAdminOrdersPageParams` + `TestAdminOrdersStatusFilter` + `TestGetAdminOrdersEndToEnd` + `TestGetAdminOrdersEmptyIsRenderable` + `TestGetAdminOrdersRejectsBadInput` + `TestGetAdminOrdersRequiresAuth` + classify `GetAdminOrders`→`authRequired`)*

## Cụm 28 — Admin orders list (FE) (`docs/plans/phase-3-admin.md` P3-c · spec §04)

> **CỐ Ý để `[ ]`** (cùng lý do Cụm 26): test của `ADM-03` là **TS admin** (`apps/admin/test/*`), không phải
> `packages/**` — parser ledger `packages/core` chỉ resolve id test TS ở `packages`. **Gate thật** = chính test đó
> (pure adapter/URL/paging: `parseStatusFilter`/`productLabel`/`toOrderRows`/`pageCount`/`buildOrdersHref`) **+** `pnpm verify`
> (typecheck/lint/format) + `next build`. RSC render + tương tác lọc/chọn để Playwright Phase 5. `[ ]` = "không do parser-TS gác".

- [ ] `ADM-03` — WHEN owner/staff mở `/don-hang` (middleware đã gác cookie), the system shall đọc `status`/`page` từ URL rồi fetch `GET /admin/orders` **server-side** (forward session cookie httpOnly, ADR-030; `no-store`), map wire→row và render: **native `<select>`** lọc trạng thái (Tất cả + 7 status; đổi → điều hướng `?status=` **reset về trang 1**), **desktop table** (Mã/Khách/Sản phẩm/Tổng/Kênh/Trạng thái/Ngày) + **mobile card-stack**, **`total` qua `formatVnd` ZERO client-math** (always-must #2), `status` qua `OrderStatusBadge` + `channel`/`status` enum→i18n label (always-must #3), ngày qua `formatVnDate`, cột "sản phẩm" = `firstItemName` + "+N" (itemCount−1); **multi-select scaffold** (checkbox + chọn-tất-cả + thanh "Đã chọn N đơn" + Bỏ chọn; nút bulk "Đổi trạng thái" = seam **trơ** → P3-e); **phân trang** prev/next (pageCount = ceil(total/pageSize), clamp hai đầu); **empty/loading/error** đủ (empty → nhánh empty của table · loading → skeleton route `don-hang/loading.tsx` · lỗi fetch → `(app)/error.tsx` retry); `?status=` lạ → coi như "Tất cả", `?page=` lẻ/≤0/junk → floor+clamp ≥1 (không đẩy non-integer xuống endpoint → 400). *(test: `apps/admin/test/orders.test.ts` → `parseStatusFilter` (known/empty/junk/case) · `productLabel` (1-item · +N) · `toOrderRows` (rename+map+empty) · `pageCount` (exact/spill/empty) · `buildOrdersHref` (omit-defaults/status+page))*
## Cụm 29 — Admin order detail (BE) (`docs/plans/phase-3-admin.md` P3-d · spec §04)

> **Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 4..12/27): test của `ADM-04` là **Go** (`services/core-api/internal/httpapi`),
> parser TS không resolve được → tick `[x]` sẽ làm parser ĐỎ. **Gate thật** = chính các test Go đó (integration vs PG thật:
> đọc-đơn-đủ-detail + unknown-id→404 + auth-gate 401) **+** classify fail-closed (`GetAdminOrder`→`authRequired`). Mapping
> row→DTO đã pin Docker-free ở `TestToOrderDTOFullMapping` (Cụm order-DTO).

- [ ] `ADM-04` — WHEN admin gọi `GET /admin/orders/{id}` (owner **hoặc** staff, `authRequired` — thiếu cookie → 401 tại boundary trước handler; id sai định dạng uuid → 400 tại binding trước handler), the system shall trả **toàn bộ Order nội bộ** cho một đơn: khách PII (name/phone/socialHandle/email) + line items + địa chỉ giao (không cấp huyện, ADR-017) + tiền (`subtotal`/`shippingFee`/`total` **raw int-VND**, tính ở server, always-must #2) + `paymentProofUrl`/`refundProofUrl` + `note` nội bộ + `trackingCode` + `statusHistory` **đầy đủ** (kèm `byUser`/`reason`) — đúng projection mà `PublicOrderTimeline` whitelist **cố ý bỏ** (ADR-032); **KHÔNG** thay đổi trạng thái ở đây (đổi trạng thái đi qua `POST /orders/{id}/transitions`, RBAC-gated, P3-e); id không tồn tại → **404 NOT_FOUND đồng nhất** (không lộ tồn-tại). *(test: `httpapi.TestGetAdminOrderEndToEnd` [đọc-đủ-detail + unknown→404] + `TestGetAdminOrderRequiresAuth` + `TestToOrderDTOFullMapping` [mapping thuần] + classify `GetAdminOrder`→`authRequired`)*

## Cụm 30 — Admin order detail: transition UI + QC-photo gate (FE+BE) (`docs/plans/phase-3-admin.md` P3-e · spec §04)

> **Go-gated `ADM-05` + TS-gated `ADM-06` — CỐ Ý để `[ ]`** (Go: cùng lý do Cụm 27/29 — tick `[x]` làm parser TS ĐỎ;
> TS admin: cùng lý do Cụm 26/28 — test ở `apps/admin/test/*` gate bởi vitest của app, không phải parser ledger). **Gate
> thật** = chính các test đó (Go integration vs PG thật cho `ADM-05`, vitest pure-adapter cho `ADM-06`).

- [ ] `ADM-05` — WHEN admin gọi `POST /orders/{id}/transitions` với `to=SHIPPING` (owner/staff, `authRequired`), the system shall **đòi CẢ `trackingCode` LẪN `qcPhotoUrl`** (ảnh đóng gói QC, D-P3-6) — thiếu một trong hai → **422** (`TRACKING_CODE_REQUIRED` / `QC_PHOTO_REQUIRED`) tại boundary **trước tx** (sau binding); khi đủ → flip PRINTING→SHIPPING qua domain guard **rồi** ghi `tracking_code`+`qc_photo_url` trong **CÙNG tx** (`SetShippingArtifacts`; cột `qc_photo_url` = migration **000014**, > head, monotonic) để trạng thái và hai artifact commit nguyên tử — đơn không bao giờ tới SHIPPING mà thiếu artifact; mọi Order-response phơi `qcPhotoUrl` (raw, denormalized như `trackingCode`/`refundProofUrl`) + line items **kèm tên người-đọc-được** (`productName` join products, `colorName` join colors, `optionLabels` join options — để admin biết LÀM GÌ, không phải uuid). *(test: `httpapi.TestTransitionShippingRequiresQcPhoto` [omit/blank→422] + `TestTransitionShippingRequiresTrackingCode` + `TestTransitionWalkEmitsPaidOnceAndPersistsTracking` [DTO+persist qc] + `db.TestSetShippingArtifacts` + `TestToOrderDTOFullMapping` [productName/colorName/optionLabels])*

- [ ] `ADM-06` — WHEN owner mở `/don-hang/{id}` (middleware đã gác cookie), the system shall fetch `GET /admin/orders/{id}` **server-side** (forward cookie httpOnly, `no-store`; 404 → trạng thái "không tìm thấy", lỗi khác → `(app)/error.tsx`) và render **detail nội bộ** (progress 5-bước suy từ statusHistory + banner terminal cho CANCELLED/REFUNDED · line items với tên/màu/tuỳ-chọn/khắc + `formatVnd` ZERO client-math, always-must #2 · khách PII + địa chỉ · proof payment/refund/QC dạng link + trackingCode · note nội bộ) + **action bar** build từ `canTransition(status, to, role)` của `@lumin/core` (không list thủ công → không bao giờ mời cạnh server sẽ từ chối): **1-chạm** confirm→PAID (owner-only) + advance (→PRINTING/COMPLETED); **dialog** `<dialog>` cho →SHIPPING (trackingCode + upload QC), →CANCELLED (lý do radio), →REFUNDED (lý do + upload proof) — submit **khoá tới khi đủ field** (mirror BE), ảnh upload **thẳng lên Garage** (presigned-POST P2-c reuse) rồi gửi `finalUrl` trên transition; thành công → `router.refresh()`; **row→detail** đã nối ở `/don-hang` (code = Link); **empty/loading/error** đủ (loading skeleton `[id]/loading.tsx`). *(test: `apps/admin/test/order-detail.test.ts` → `reachedMilestoneIndex` · `progressSteps` [fresh/mid/completed/closed] · `transitionKind` · `availableTransitions` [owner-full · staff-no-owner-only · terminal-empty])*

## Cụm 31 — Admin print queue: list + stage advance (BE) (`docs/plans/phase-3-admin.md` P3-f · spec §02 PrintJob · D6)

> **Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 4..12/27/29): test của `ADM-07`/`ADM-08` là **Go**
> (`services/core-api/internal/httpapi`), parser TS không resolve được → tick `[x]` sẽ làm parser ĐỎ. **Gate thật** =
> chính các test Go đó (integration vs PG thật: board-list-enriched + stage-advance + bad-stage→400 + unknown→404 +
> auth-gate 401) **+** classify fail-closed (`GetPrintQueue`/`AdvancePrintJobStage`→`authRequired`). Mapping row→card pin
> Docker-free ở `TestPrintQueueDTO`.

- [ ] `ADM-07` — WHEN admin gọi `GET /admin/print-queue` (owner **hoặc** staff, `authRequired` — thiếu cookie → 401 tại boundary trước handler), the system shall trả **toàn bộ bảng in**: mỗi print job (mọi stage NEED_PRINT/PRINTING/PACKING/SHIPPED) là một **card giàu thông tin** join `orders.code` + `products.name` + `order_items.quantity` (+ `colorName`/`printer`/`eta` tuỳ chọn — bare row chỉ có id, vô dụng ở máy in), **sắp theo stage rồi created_at** (FIFO mỗi cột; client group thành 4 cột kanban + đếm mỗi cột); `stage` là enum `PrintStage` client map sang nhãn cột (always-must #3); **không phân trang** (bảng in nhỏ; `ponytail:` SHIPPED tích luỹ → thêm "recent N"/archive nếu phình); rỗng → slice non-nil `[]` (spec §03). *(test: `httpapi.TestGetPrintQueueEndToEnd` [2-card enriched + orderCode-đúng] + `TestGetPrintQueueEmptyIsRenderable` + `TestGetPrintQueueRequiresAuth` + `TestPrintQueueDTO` [mapping thuần + nullable cả hai chiều] + classify `GetPrintQueue`→`authRequired`)*

- [ ] `ADM-08` — WHEN admin gọi `PATCH /admin/print-jobs/{id}` với `{stage}` (owner/staff, `authRequired`), the system shall đặt **CHỈ stage bảng in** (kéo-thả staff) qua `AdvancePrintStage` — **KHÔNG** transition OrderStatus của khách: print queue **STORED + staff-driven**, mịn hơn order status và tiến **độc lập** (D6); đổi OrderStatus đi qua `POST /orders/{id}/transitions` (gác RBAC + statusHistory + QC-photo/trackingCode →SHIPPING, P3-e) mà một cú kéo card **không bao giờ được vượt** (nếu tự-sync sẽ **bypass QC gate** + phá owner-only PAID); body thiếu / `stage` ngoài enum → **400** tại handler **trước write** (oapi bỏ enum-check → `printStages` membership là tường; tránh `::print_stage` cast 500), id không có → **404**; thành công → đọc lại card enriched (cùng shape list) **rồi broadcast card đó lên SSE** (P3-g, best-effort post-commit). *(test: `httpapi.TestGetPrintQueueEndToEnd` [advance→PRINTING trả enriched + broadcast tới subscriber + bad-stage→400 + nil-body→400 + unknown→404] + `TestAdvancePrintJobStageRejectsBadStage` + `TestAdvancePrintJobStageRequiresAuth` + `TestPrintStagesMembership` + classify `AdvancePrintJobStage`→`authRequired`)*

## Cụm 32 — Admin print queue: live SSE stream (BE) (`docs/plans/phase-3-admin.md` P3-g · ADR-008 · conventions §Realtime)

> **Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 4..12/27/29/31): test của `ADM-09` là **Go**
> (`services/core-api/internal/httpapi`), parser TS không resolve được → tick `[x]` sẽ làm parser ĐỎ. **Gate thật** =
> chính các test Go đó (hub fan-out + broadcast-never-blocks + auth-401 + SSE-headers + broadcast-on-PATCH-tới-subscriber
> vs PG thật). **CÒN TREO — BLOCKER-B (KHÔNG tự-động-hoá được ở đây):** SSE qua **named cloudflared tunnel** phải
> **smoke-test tay** (buffer/timeout 100s/524) trước khi P3-h ship; nếu tunnel làm SSE bập bênh → ship poll-only cho v1
> (open-q #4, ADR-008 cho phép), fallback poll đã có (`use-order-poll`, P1-o).

- [ ] `ADM-09` — WHEN admin mở `GET /admin/print-queue/stream` (owner **hoặc** staff — auth **cưỡng chế trong handler** qua `resolveActor` trên cookie `lumin_session` mà EventSource gửi same-origin, **KHÔNG** phải strict-op nên tự gác; thiếu/hỏng cookie → 401 trước mọi stream-byte), the system shall mở một **SSE stream** (`text/event-stream` + `Cache-Control: no-transform` + `Content-Encoding: identity` + `X-Accel-Buffering: no` + `http.Flusher` mỗi frame + **heartbeat comment mỗi 25s** — vượt idle-cap ~100s của Cloudflare + phá buffer tunnel, conventions §Realtime/ADR-008) và **đẩy một print card giàu thông tin** (`event: stage`, JSON `data:` một dòng) **mỗi lần** một stage PATCH advance job đó — **fan-out in-process** (core-api single-instance, ADR-009; **NATS KHÔNG lộ ra browser**, ADR-008; `ponytail:` nếu scale ngang thì bọc hub bằng NATS-subscribe sau); route sống **NGOÀI `middleware.Timeout`** nên stream không bị huỷ ở 30s (cũng KHÔNG có global `WriteTimeout` — main.go để trống đúng cho SSE); client ngắt (`r.Context().Done()`) → unsubscribe sạch; broadcast **non-blocking best-effort** (buffer đầy → drop → client tự hoà giải qua GET/fallback-poll). *(test: `httpapi.TestPrintStreamHubFanout` + `TestPrintStreamHubBroadcastNeverBlocks` + `TestPrintStreamHubBroadcastNilSafe` + `TestStreamPrintQueueRequiresAuth` + `TestStreamPrintQueueAuthedSetsSSEHeaders` + `TestWriteSSEEvent` + `TestGetPrintQueueEndToEnd` [broadcast-on-PATCH tới subscriber]; **smoke-test named-tunnel = gate tay, BLOCKER-B**)*

## Cụm 33 — Admin print board: kanban kéo-thả + SSE live (FE) (`docs/plans/phase-3-admin.md` P3-h · D-P3-2/D6 · ADR-008)

> **FE — giữ `[ ]`** (cùng quy ước Cụm 26/28/30-FE): logic thuần (`groupByStage`/`nextStage`/`mergeCard`) đã có
> unit-test Docker-free `apps/admin/test/print-queue.test.ts`, nhưng **không** có test TS mang đúng id `ADM-10`;
> hành vi kéo-thả/SSE end-to-end để **Playwright Phase 5** (như các màn admin khác). Tick `[x]` sẽ làm parser
> `packages/core` đỏ. **Còn treo — BLOCKER-B:** SSE qua named tunnel là **gate tay**; nếu bập bênh → board tự
> hạ xuống **poll-only** (fallback đã build, không cần đổi UI).

- [ ] `ADM-10` — WHEN admin mở `/hang-doi-in` (đã đăng nhập; RSC fetch board qua cookie-forward, lỗi → `(app)/error.tsx`, rỗng → zero-state riêng, loading → skeleton 4 cột), the system shall dựng **bảng kanban 4 cột** (Cần in/Đang in/Đóng gói/Đã gửi = 4 `PrintStage`, client group qua `groupByStage`, đếm mỗi cột, FIFO theo thứ tự server) và cho staff đổi stage **hai đường**: **kéo-thả** (@dnd-kit, MouseSensor 8px + TouchSensor giữ-200ms để vuốt vẫn cuộn được) **HOẶC** nút **"Chuyển sang {stage}"** mỗi card (đường **bàn phím/AT/mobile**, D-P3-2 — chỉ spread `listeners` không spread `attributes` để không lồng button-trong-role=button); cả hai gọi `PATCH /admin/print-jobs/{id}` đặt **CHỈ print stage** (**KHÔNG** transition OrderStatus — D6; thiết kế vẽ "kéo → tự đổi trạng thái khách" nhưng backbone tách rời, đổi status đi qua transition-flow có QC-gate P3-e); cập nhật **lạc quan** (card chạy ngay → hoà giải với card server trả về → **revert đúng stage cũ** nếu lỗi + báo `advanceError`); giữ **live** qua **SSE** (`EventSource` → same-origin proxy `app/api/print-stream/route.ts` forward cookie httpOnly tới core-api; mỗi `event: stage` merge một card qua `mergeCard`, idempotent với card PATCH trả về) + **fallback poll** re-GET mỗi 15s **chỉ khi** stream mất (BLOCKER-B/open-q #4; visibility-pause; cũng là đường nhặt job mới SSE không phát); `prefers-reduced-motion` → tắt drop-animation; badge "thiếu nhựa" **hoãn sang P3-s** (chưa có field data — không render cảnh báo không căn cứ). *(test: `apps/admin/test/print-queue.test.ts` [`groupByStage` 4-cột+FIFO · `nextStage` walk→SHIPPED=null · `mergeCard` replace-by-id idempotent + append]; kéo-thả/SSE/optimistic-revert = Playwright Phase 5)*

## Cụm 34 — Admin settings writes: shipping-rules · refund-policy · reply-template CRUD (BE+FE) (`docs/plans/phase-3-admin.md` P3-i · spec §02 Setting/ReplyTemplate · conventions §57 · ADR-012)

> **Cụm 33 (`ADM-10`) = P3-h** (print board FE, ngay trên — đã merge #70) → P3-i cấp số **trên cái đang ở trước** (monotonic, memory `lumin-migration-numbering-monotonic`), nên nhảy 32 → 34.
> **`ADM-11` Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 4..12/27/29/31/32): test là **Go**, parser ledger `packages/core` chỉ resolve id **TS** → để `[x]` sẽ fail app-gate. **`ADM-12` FE** cũng giữ `[ ]` (cùng quy ước Cụm 26/28/30/33-FE): logic thuần có test `apps/admin/test/settings.test.ts` nhưng không mang đúng id `ADM-12`; **gate thật** = chính test đó (vitest) + Playwright Phase 5.

- [ ] `ADM-11` — WHEN owner gọi các settings-write (`PATCH /admin/settings/shipping-rules`, `PATCH /admin/settings/refund-policy`, `POST`/`PATCH`/`DELETE /admin/reply-templates[/{id}]` — mọi cái **owner-only** qua classify→`authOwnerOnly` **+** re-assert `assertOwner` trong handler [staff→**403**, no-actor→**401**, **trước mọi DB touch**]; staff không sửa cài đặt — domain-core RBAC), the system shall: **shipping-rules** thay **toàn bộ** bảng phí, persist **đúng shape `[]pricing.ShippingRule`** (province+fee — cùng jsonb `pricing.ShippingFee` đọc ở checkout, nên một shape lệch sẽ sai phí **mọi** đơn) sau khi validate province non-empty/unique + fee ≥ 0 (âm/rỗng/trùng → **400** field-map **trước write**; bảng rỗng hợp lệ = shop tạm không ship, checkout tự 422 per-province); **refund-policy** persist text đã trim (rỗng hợp lệ; cap độ dài); **reply-template** CRUD với `variables` **suy từ body server-side** (regex `{token}`, unique + first-seen — không tin client → không lệch với body), title/body required + cap, unknown id (PATCH/DELETE) → **404** (`db.ErrNotFound`; DELETE dùng `:execrows`=0); **STK audit giữ nguyên** (P3-i không đụng `UpdateBankAccountTx`); shipping/refund **KHÔNG audit** (open-q #2 — chỉ STK là money-out cao-giá). *(test: `httpapi.TestSettingsWritesAreOwnerOnly` [5 endpoint × staff-403/no-actor-401 Docker-free] + `TestCleanShippingRules` [+ round-trip qua `pricing.ShippingFee`] + `TestExtractTemplateVariables` + `TestCleanReplyTemplateInput` + `TestUpdateShippingRulesEndToEnd` [persist→resolver + neg-fee→400 real-PG] + `TestUpdateRefundPolicyEndToEnd` + `TestReplyTemplateCRUDEndToEnd` [create/update derive vars + delete + unknown→404] + classify 5 ops → `authOwnerOnly`)*

- [ ] `ADM-12` — WHEN owner mở `/cai-dat` (đã đăng nhập; RSC fetch `GET /admin/settings` cookie-forward `no-store`, lỗi → `(app)/error.tsx`, loading → skeleton), the system shall render **Cài đặt › Thanh toán & ship**: form STK (bin/accountNumber/accountName + lý do) → `PATCH bank-account` **+ cảnh báo "chưa cấu hình STK ⇒ chặn checkout"** khi `isStkConfigured` false (đủ cả ba field VietQR mới coi là đã cấu-hình); **bảng phí ship** thêm/sửa/xoá dòng {khu vực, phí} + hint wildcard `*`=mặc định + `formatVnd` hiển thị (ZERO client-math, always-must #2) → `PATCH shipping-rules`; **textarea chính sách đổi trả** → `PATCH refund-policy`; card "Trang con khác" link `/cai-dat/mau-tra-loi` (Extension/Nhân viên/Kênh chat = placeholder "sắp có"). Ở `/cai-dat/mau-tra-loi`: **grid mẫu trả lời** + CRUD qua `<dialog>` native (thêm/sửa: tên+nội dung + preview biến client-side; xoá: confirm inline) → `POST`/`PATCH`/`DELETE reply-templates`; mọi write **owner-only ở server** (FE giả định owner — chưa có `/auth/me`/staff tới P3-q, `ponytail:`; `forbidden` surface inline), thành công → `router.refresh()`; i18n `settings.*` (không literal `{}` trong copy — ICU); mỗi màn đủ **empty/loading/error**; reduced-motion tự thoả (không animation). *(test: `apps/admin/test/settings.test.ts` → `isStkConfigured` · `extractVariables` · `shippingRulesOf`; form/dialog/optimistic = Playwright Phase 5)*

## Cụm 35 — Admin catalog writes: product · color · option CRUD (BE) (`docs/plans/phase-3-admin.md` P3-j-a · spec §02 Product/Color/Option · §08 RBAC · ADR-032)

> **Cụm 34 (`ADM-12`) = P3-i FE** (ngay trên — đã merge #71) → Cụm 35 nối tiếp (monotonic). P3-j tách 2 PR (Scope §1-PR-1-trục): **P3-j-a = catalog write CRUD** (Cụm này); **P3-j-b = model-upload infra + asset-jobs** (Cụm sau, có ADR). Model3d_url **KHÔNG** đụng ở P3-j-a (asset pipeline sở hữu — P3-j-b).
> **`ADM-13` Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 4..12/27/29/31/32/34): test là **Go**, parser ledger `packages/core` chỉ resolve id **TS** → để `[x]` sẽ fail app-gate (memory `lumin-go-ears-stay-unchecked`).

- [ ] `ADM-13` — WHEN owner gọi các catalog-write (`POST`/`PATCH`/`DELETE /admin/products[/{id}]` + `POST`/`PATCH`/`DELETE /admin/products/{id}/colors[/{colorId}]` + `.../options[/{optionId}]` — mọi write **owner-only** qua classify→`authOwnerOnly` **+** re-assert `assertOwner` trong handler [staff→**403**, no-actor→**401**, **trước mọi DB touch**]; spec §08 sản phẩm là quyền owner, staff chỉ đơn/hàng-in/đánh-giá) HOẶC admin đọc (`GET /admin/products[?status=]` + `GET /admin/products/{id}` — `authRequired` owner+staff, như settings read), the system shall: **đọc** trả projection admin **mọi status** (list `AdminProductSummary` không colors/options → no N+1, filter status tuỳ chọn = tab Tất cả/Đang bán/Nháp/Lưu trữ, **không phân trang** — catalog nhỏ, FE search client-side; detail = full `Product` + colors/options, unknown → **404**) — **KHÁC** public active-only (ADR-032); **create/update product** validate **trước write** (name/slug[`^[a-z0-9-]+$`]/material[∈ PLA·PETG·recycled-PLA]/status[enum]/dimensions[w,d,h>0]/basePrice[≥0] → **400** field-map), slug trùng → **400** field `slug` (UNIQUE 23505), categoryId lạ → **400** field `categoryId` (FK 23503), unknown id (PATCH) → **404**, và **KHÔNG bao giờ đụng `model3d_url`** (asset pipeline sở hữu — P3-j-b); **delete product** hard-delete chỉ khi chưa có đơn/asset-job (colors/options CASCADE theo), có lịch sử → **409** `PRODUCT_IN_USE` (FK RESTRICT 23503 — steer sang archive = PATCH status→archived), unknown → **404**; **color/option** CRUD scope theo **(product, child)** nên child dưới product khác → **404**, hex validate `^#[0-9a-fA-F]{3,6}$` (chống CSS-injection vào swatch inline-style), priceDelta ≥ 0, option type ∈ text·choice, maxChars > 0 nếu có; money raw int-VND (always-must #2), không audit (catalog không phải money-out). *(test: `httpapi.TestAdminProductCRUDEndToEnd` [create→read-by-id→update→list-by-status→delete real-PG] + `TestDeleteProductInUseReturns409` [đơn tham chiếu → RESTRICT→409] + `TestAdminProductWritesAreOwnerOnly` [9 write × staff-403/no-actor-401 Docker-free] + `TestCleanProductInput` [slug/material/dimensions/basePrice edge] + `TestCleanColorInput` [hex-reject] + `TestCleanOptionInput` [type/maxChars] + `TestProductColorOptionScopedByProduct` [cross-product→404] + classify 9 write→`authOwnerOnly`, 2 read→`authRequired`)*

## Cụm 36 — Admin model-upload + asset-jobs (BE) (`docs/plans/phase-3-admin.md` P3-j-b · ADR-036/005/006 · D-P3-5 · spec §02 AssetJob)

> **Cụm 35 (`ADM-13`) = P3-j-a** (catalog CRUD BE, ngay trên) → Cụm 36 nối tiếp (monotonic). Đây là **nửa còn lại** của P3-j: hạ tầng upload model + enqueue render-job. **ADR-036** (user 2026-07-11): single presigned POST ≤100MB, KHÔNG S3 multipart (refine ADR-005). Phần **OUTPUT** (worker render → ghi `model3d_url`/sprite lên Product) là slice-3 sau — Cụm này chỉ dựng **INPUT** (upload + queue).
> **`ADM-14` Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 4..12/27/29/31/32/34/35): test là **Go** (`services/core-api`), parser ledger `packages/core` chỉ resolve id **TS** → để `[x]` sẽ fail app-gate (memory `lumin-go-ears-stay-unchecked`).

- [ ] `ADM-14` — WHEN owner gọi model-upload / asset-job-write (`POST /admin/products/{id}/model-upload` + `POST /admin/products/{id}/asset-jobs` — **owner-only** qua classify→`authOwnerOnly` **+** re-assert `assertOwner` [staff→**403**, no-actor→**401**, **trước store/DB touch**]; catalog-asset = quyền owner, spec §08) HOẶC admin đọc (`GET /admin/products/{id}/asset-jobs` — `authRequired` owner+staff, như product read), the system shall: **model-upload** ký **một presigned POST** (ADR-036 — KHÔNG multipart) tới bucket công khai riêng `lumin-assets` với policy pin `content-length-range` **≤100MB** (trần body Cloudflare Tunnel, ADR-005) + `Content-Type` chính xác ∈ {`model/gltf-binary`,`model/stl`,`model/3mf`} (MIME khác/octet-stream/ảnh → **400**), key ngẫu nhiên PII-free `models/YYYY/MM/DD/<uuid>.<ext>`, trả `finalUrl` **host-pinned** (server-derived, không tin client); browser upload thẳng browser→Garage (core-api không proxy body); store nil (chưa cấu hình bucket) → **500** fail-closed (không ký form giả-mạo-được); **asset-job create** validate **trước write** — `sourceModelUrl` phải **host-pinned** (chỉ URL do store này phát; foreign/spoofed → **400** field `sourceModelUrl`, chống enqueue nguồn lạ), `jobType` ∈ {`model_ingest`,`sprite_render`} (khác → **400**), `sourceVersion` shape content-hash (hex 8..128 → **400** nếu rỗng/ngắn/non-hex) → `CreateAssetJobTx` tạo row `queued` **+** enqueue outbox `asset_job.created` **cùng tx** (publish-on-commit, ADR-006 — relay slice-3 drain lên NATS), unknown product → **404** (FK `asset_jobs.product_id` 23503); **asset-job list** trả mọi job của product **mới nhất trước** (status queued/processing/ready/failed cho editor render-status panel), product tồn tại nhưng chưa job → `[]`, unknown product → **404**. Money N/A (asset-job không mang tiền); **KHÔNG** đụng `model3d_url` (worker OUTPUT sở hữu — D3). *(test: `httpapi.TestProductAssetJobsEndToEnd` [presign→enqueue-from-host-pinned→**outbox `asset_job.created` cùng tx**→list→foreign-url→400→bad-content-type→400→unknown-product→404 real-PG] + `TestAssetJobWritesAreOwnerOnly` [2 write × staff-403/no-actor-401 Docker-free] + `TestModelEndpointsNotConfigured` [nil store→500] + `TestCleanAssetJobInput` [jobType/host-pin/version edge] + `modelstore.TestStore*` [presign host-pin POST-policy · OwnsURL reject foreign/query/bad-ext · content-type allowlist · config cap ≤100MB] + classify 2 write→`authOwnerOnly`, 1 read→`authRequired`)*

## Cụm 37 — Admin product list FE (`/san-pham`) (`docs/plans/phase-3-admin.md` P3-k · Track D · spec §03 State · ADR-032)

> **Cụm 36 (`ADM-14`) = P3-j-b** (model-upload + asset-jobs BE, ngay trên) → Cụm 37 nối tiếp (monotonic). Đây là FE tiêu thụ `GET /admin/products` (P3-j-a): grid + tab + search client-side (catalog nhỏ, endpoint không phân trang). Editor `/san-pham/{id}` + FAB `/san-pham/moi` là **seam P3-l** (link đã nối, route bật ở P3-l).
> **`ADM-15` FE** — CỐ Ý để `[ ]` (cùng quy ước Cụm 26/28/30/33/34-FE): logic thuần có test `apps/admin/test/products.test.ts` nhưng không mang đúng id `ADM-15`; **gate thật** = chính test đó (vitest) + Playwright Phase 5.

- [ ] `ADM-15` — WHEN owner mở `/san-pham` (đã đăng nhập; RSC fetch **cả catalog** `GET /admin/products` cookie-forward `no-store` — **không** `?status`, endpoint không phân trang, lỗi → `(app)/error.tsx`, loading → skeleton), the system shall render **danh sách sản phẩm**: header (tiêu đề + tổng số + nút "+ Thêm sản phẩm" → `/san-pham/moi`, **seam P3-l**) + **grid thẻ** (ảnh cover = `images[0]` hoặc placeholder dotgrid · **badge trạng thái** active/draft/archived→tone teal/neutral/sun · tên clamp 2 dòng · **giá `formatVnd(basePrice)`** raw int-VND ZERO client-math, always-must #2 · cả thẻ = Link `/san-pham/{id}`, **seam P3-l**); **tab** Tất cả/Đang bán/Nháp/Lưu trữ (toggle `aria-pressed`, đếm mỗi tab) + **ô tìm** client-side khớp name **hoặc** slug (từ khoá không dấu vẫn trúng qua slug) — cả hai lọc **client-side** (không re-fetch); **empty tách 2**: catalog rỗng → CTA "Thêm sản phẩm"; lọc/tìm không khớp → "không khớp" + nút "Bỏ lọc" reset. i18n `products.*` (không hard-code, ICU-safe); badge trạng thái = enum→label i18n (always-must #3); nút primary dùng token `bg-primary`/`text-on-primary` (AA-safe flame-700, **KHÔNG** trắng-trên-flame-500 như design — fail AA, a11y KHOÁ); mỗi state đủ **empty/loading/error**; reduced-motion tự thoả (hover-lift `motion-reduce:transform-none`). *(test: `apps/admin/test/products.test.ts` → `toProductCards` [cover/searchKey] · `filterProducts` [tab · không-dấu-qua-slug · combined] · `countByTab`; grid/tab/search tương tác = Playwright Phase 5)*

## Cụm 38 — Admin configurator catalog: parts + option choices (BE) (`docs/plans/p3l-product-editor.md` Stage 2a · ADR-037 · spec §02 Part/OptionChoice)

> **Cụm 37 (`ADM-15`) = P3-k FE** (danh sách sản phẩm, ngay trên) → Cụm 38 nối tiếp (monotonic). Đây là **Stage 2a** của epic ADR-037 (configurator): thêm bộ-phận có tên (mỗi part có bộ màu riêng) + lựa-chọn liệt kê cho option `choice`, **catalog-write only** (pricing + order-capture = Stage 2b). Additive: `colors.part_id` nullable, choice-option không có choices = toggle như cũ.
> **`ADM-16` Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 4..12/27/29/31/32/34/35/36): test là **Go** (`services/core-api`), parser ledger `packages/core` chỉ resolve id **TS** → để `[x]` sẽ fail app-gate (memory `lumin-go-ears-stay-unchecked`).

- [ ] `ADM-16` — WHEN owner gọi configurator-write (`POST`/`PATCH`/`DELETE /admin/products/{id}/parts[/{partId}]` + `POST`/`PATCH`/`DELETE /admin/products/{id}/options/{optionId}/choices[/{choiceId}]` — **owner-only** qua classify→`authOwnerOnly` **+** re-assert `assertOwner` [staff→**403**, no-actor→**401**, **trước mọi DB touch**]; catalog = quyền owner, spec §08) HOẶC admin đọc product detail (`GET /admin/products/{id}` — `authRequired`, nay kèm `parts[]` + `colors[].partId` + `options[].choices[]`), the system shall (ADR-037): **part CRUD** — tạo/sửa/xoá bộ-phận (name ≤200 rune, displayOrder), unknown product → **404** (FK 23503), sửa/xoá scope theo **(product, part)** → part dưới product khác → **404**, **xoá part CASCADE màu của nó** (màu đã có đơn chặn cascade → **409** `PRODUCT_IN_USE`, steer archive); **color↔part** — `ColorInput.partId` tuỳ chọn, set thì part **phải thuộc cùng product** (`GetPartByProduct`; part lạ → **400** field `partId` — nửa catalog-write của guard "màu ∈ part ∈ product", pricing re-check ở Stage 2b), null = màu phẳng cấp SP (mặc định, hành vi cũ); **option-choice CRUD** — tạo/sửa/xoá choice dưới option (label ≤200, description ≤2000, priceDelta ≥0 int-VND, displayOrder), option **phải thuộc product** (`GetOptionByProduct` → option dưới product khác → **404**), choice scope theo **(option, choice)** → **404**; **detail assembly** — `GET /admin/products/{id}` nhóm choices theo option_id vào `options[].choices[]` (rỗng = `[]`, không null) + `parts[]` + `colors[].partId`; storefront `GET /products/{slug}` cũng trả parts/choices (additive, product phẳng = rỗng). Money raw int-VND (always-must #2); **KHÔNG** migration order-side/pricing ở PR này (Stage 2b). *(test: `httpapi.TestConfiguratorCRUDEndToEnd` [part→color-với-part→flat-color→option→choice→detail-assembly→update→cross-product-404→foreign-part-400→delete-part-cascade real-PG] + `TestAdminProductWritesAreOwnerOnly` [+6 configurator write × staff-403/no-actor-401 Docker-free] + `TestCleanPartInput` + `TestCleanOptionChoiceInput` [priceDelta<0 reject] + classify 6 write→`authOwnerOnly`)*

## Cụm 39 — Configurator pricing + order-side capture (BE) (`docs/plans/p3l-product-editor.md` Stage 2b · ADR-037 · spec §02)

> **Cụm 38 (`ADM-16`) = Stage 2a** (catalog write, ngay trên) → Cụm 39 nối tiếp (monotonic). Đây là **Stage 2b** của epic ADR-037: **2b-1** mở rộng `pricing.PriceItem` (MONEY PATH — màu-theo-part + choice-theo-option) + **2b-2** bắt lựa-chọn vào đơn (`order_items.part_colors`/`option_choices` jsonb, additive ADR-004) + nối wire. Một product **có parts** giờ đặt được end-to-end; product **phẳng** byte-đồng-nhất như cũ.
> **`CHK-09` Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 38/4..12/…): test là **Go** (`services/core-api`), parser ledger `packages/core` chỉ resolve id **TS** → để `[x]` sẽ fail app-gate (memory `lumin-go-ears-stay-unchecked`).

- [ ] `CHK-09` — WHEN storefront quote (`POST /price/quote`) HOẶC tạo đơn (`POST /orders`) một dòng có cấu hình ADR-037, the system shall (server-authoritative, always-must #2): **màu — parts-mode XOR flat-mode** — product có parts cần **đúng một màu mỗi part**, mỗi màu **∈ part được khai** (cross-charge guard: trả delta part-A nhưng ghim part-B → **422**), product phẳng dùng `colorId` đơn (legacy, byte-đồng-nhất); trộn hai chế độ (flat `colorId` trên product-có-parts, hoặc `partColors` trên product phẳng) → **422**; **option — choice priced by picked choice** — option `choice` có choices tính theo **choice được chọn** (delta của choice; **base của option BỎ QUA** — một nguồn giá), option text/toggle-legacy tính theo option delta; toggle một choice-option-có-choices thay vì chọn → **422**; **8 sentinel** (`ErrColorForPartsProduct`/`ErrPartColorForFlatProduct`/`ErrMissingPartColor`/`ErrDuplicatePartColor`/`ErrColorNotForPart`/`ErrOptionNeedsChoice`/`ErrChoiceNotForOption`/`ErrDuplicateOptionChoice`) → **422 `INVALID_SELECTION`**, mỗi summand overflow-checked; **capture** — lựa-chọn ghi **snapshot** vào `order_items.part_colors`/`option_choices` (jsonb `[]{partId,colorId}` / `[]{optionId,choiceId}`, NOT NULL DEFAULT `[]`, không backfill) qua `CreateOrderTx`, DTO đơn **round-trip** lại (omit khi rỗng — dòng phẳng byte-đồng-nhất); **parity** — quote dùng **cùng `Selection`** với charge → unit-price quote == charge. Wire `OrderItemInput.partColors`/`optionChoices` **optional** (vắng = product phẳng — client cũ không đổi; storefront configurator = Stage 2c). *(test: `pricing.TestPriceConfiguratorHappy`/`…ChoiceIgnoresOptionBase`/`…LegacyToggle`/`…Invalid` [12 invalid-selection cases covering the 8 new + reused sentinels] + `pricing.TestPriceFlatRejectsPartColors` [Docker-free] + `httpapi.TestCreateOrderPartsProductEndToEnd` [wire→price→persist→DTO round-trip + quote==charge, real-PG])*

## Cụm 40 — Storefront configurator consume: per-part colours + enumerated choices + cart key (FE) (`docs/plans/p3l-product-editor.md` Stage 2c · ADR-037 · spec §02)

> **Cụm 39 (`CHK-09`) = Stage 2b** (pricing + order capture BE, ngay trên) → Cụm 40 nối tiếp (monotonic). Đây là **Stage 2c** của epic ADR-037: storefront **dựng UI + gửi** lựa-chọn (2a/2b đã sẵn contract + server-side). Product **phẳng** byte-đồng-nhất (mọi field configurator optional, omit khi rỗng); product **có parts/choices** giờ cấu-hình + đặt được end-to-end từ web. Row **TS-gated** (vitest storefront) như các SF-* trên → giữ `[ ]` cùng nếp (annotation *(test:…)* là bằng chứng phủ).

- [ ] `SF-09` — WHEN khách cấu-hình một sản phẩm ở `/san-pham/{slug}` rồi thêm vào giỏ + định giá (ADR-037), the system shall: **projection** — `toProductDetailView` surface `parts[]` + `colors[].partId` + `options[].choices[]` (guard SSG/ISR `?? []`; product phẳng = rỗng); **render** — product **có parts** dựng **một nhóm swatch mỗi bộ-phận** (`colorsForPart`, chọn một màu/part → `partColorByPart`), product phẳng giữ picker màu đơn; option `choice` **có choices** → **radio-group native** (một pick bắt buộc → `optionChoices`), option `choice` **không choices** → toggle như cũ (`optionIds`), option `text` → khắc như cũ; **gate** — `canAddConfiguredToCart` khoá CTA tới khi **đủ màu mỗi part** (∈ đúng part, còn hàng — mirror `ErrMissingPartColor`/`ErrColorNotForPart`/unavailable) **VÀ đủ lựa-chọn mỗi enumerated option** (`ErrOptionNeedsChoice`) **VÀ mọi khắc trong hạn** — không bao giờ để khách gửi cấu-hình server sẽ **422**; **cart key** — `cartLineKey` thêm **2 trục canonical-sorted** (partColors + optionChoices, UUID trước free-text engrave) chống **dòng-giỏ-ma** ({Chao:Đỏ,Đế:Trắng} ≠ {Chao:Trắng,Đế:Đỏ} tuy cùng productId+colorId=null); **capture** — `buildCartItem` snapshot `partColors`/`optionChoices` (id pairs, wire-shaped) + nhãn hiển thị `"{part}: {màu}"`/`"{option}: {choice}"` (giỏ không có product data → chụp lúc add), product-có-parts để `colorId=null` (gửi cả hai → 422); **send** — `cartQuoteItems` + `buildWebOrderInput` gửi hai mảng **omit-khi-rỗng** (dòng phẳng byte-đồng-nhất trên wire) qua `QuoteItem` Pick + spread; **persist** — `sanitizeCart` coerce lỏng hai mảng (mặc định `[]` cho giỏ lưu trước-2c → không rớt dòng phẳng cũ), drop pair hỏng (server re-validate → tamper không mis-price); **KHÔNG cộng tiền client** (tổng từ `POST /price/quote`, always-must #2). *(test: `apps/storefront/test/product-detail-view.test.ts` → `toProductDetailView` (parts/partId/choices) · `colorsForPart` · `allPartsSelected` (membership+availability+cross-part) · `allChoicesSelected` · `canAddConfiguredToCart` (flat-reduce · parts+choice lock · engrave gate); `apps/storefront/test/cart.test.ts` → `cartLineKey` (order-independent 2 trục · ghost-line guard · flat byte-identical) · `buildCartItem` (parts+choice capture+labels · ghost-line distinct) · `cartQuoteItems` (send+omit) · `sanitizeCart` (default legacy · coerce+drop-malformed); `apps/storefront/test/checkout-form.test.ts` → `buildWebOrderInput` (partColors/optionChoices thread + no flat colorId))*

## Cụm 41 — Admin order-detail + print queue đọc lựa-chọn cấu-hình (BE denormalize-at-capture + admin FE) (`docs/plans/p3l-product-editor.md` Stage 2d · ADR-037 pt 2 · spec §02)

> **Cụm 40 (`SF-09`) = Stage 2c** (storefront consume FE, ngay trên) → Cụm 41 nối tiếp (monotonic). Đây là **Stage 2d** của epic ADR-037: đóng vòng đọc — admin/print **hiển thị** lựa-chọn cấu-hình theo **ADR-037 pt 2 (denormalize-at-capture)**. Checkout **đóng băng tên** (part/màu/option/choice) vào `order_items.part_colors`/`option_choices` jsonb lúc tạo đơn (2b-2 chỉ lưu id-pair) → admin order-detail + print card đọc **nhãn** không join catalog sống; đơn đã bán **bất biến** trước đổi tên/xoá màu về sau. Product **phẳng** byte-đồng-nhất.
> **`ADM-17` Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 38/39/…): test là **Go** (`services/core-api`) + admin FE render, parser ledger `packages/core` chỉ resolve id **TS** → để `[x]` sẽ fail app-gate (memory `lumin-go-ears-stay-unchecked`).

- [ ] `ADM-17` — WHEN admin xem order-detail (`GET /admin/orders/{id}`) HOẶC print board (`GET /admin/print-queue` + PATCH re-read) một dòng có cấu hình ADR-037, the system shall: **capture (denormalize, ADR-037 pt 2)** — `checkout.priceLine` SAU `PriceItem` (id đã validate) resolve mỗi lựa-chọn thành **snapshot có tên** (`order.PartColorSnapshot{partId,partName,colorId,colorName,hex}` / `OptionChoiceSnapshot{optionId,optionLabel,choiceId,choiceLabel}`) đọc từ **đúng catalog đã fetch để định giá** — KHÔNG query thêm, KHÔNG đổi `unit` (tiền); pricing input (`Selection`) vẫn **id-only** (client không được tin cho tên); **read — order detail** — `orderItemsDTO` parse snapshot → id-pairs (`partColors`/`optionChoices`, hợp đồng cũ giữ nguyên) **VÀ** nhãn hiển thị (`partColorLabels` "Chao đèn: Cam" / `optionChoiceLabels` "Kích thước: M", theo thứ tự lựa-chọn), omit khi rỗng (dòng phẳng byte-đồng-nhất); **read — print** — `ListPrintQueue`/`GetPrintQueueEntry` thêm `oi.part_colors` vào SELECT, `printQueueDTO` → `partColorLabels` (filament nào cho part nào tại máy in), omit cho dòng phẳng; **render (admin FE)** — order-detail `itemSpecs` chèn partColorLabels/optionChoiceLabels (cùng thứ tự với giỏ storefront), print `CardFace` thêm dòng màu-theo-part; **KHÔNG** migration (jsonb sẵn từ 000016), **KHÔNG** join catalog sống ở read, **KHÔNG** đổi money. *(test: `httpapi.TestCreateOrderPartsProductEndToEnd` [+`partColorLabels`/`optionChoiceLabels` resolved-from-DB-names round-trip, real-PG] + `httpapi.TestOrderItemsDTOConfiguratorLabels` [id-pairs + nhãn theo thứ tự, Docker-free] + `httpapi.TestToOrderDTOFullMapping` [dòng phẳng KHÔNG mang field configurator] + `httpapi.TestPrintQueueDTO` [parts card → `partColorLabels`, flat card → nil])*

## Cụm 42 — Model 3D góc xem mặc định (camera pose shop lưu) — BE (`docs/plans/p3l-product-editor.md` Stage 3 · ADR-038 · spec §02)

> **Cụm 41 (`ADM-17`) = Stage 2d** (admin/print đọc cấu-hình, ngay trên) → Cụm 42 nối tiếp (monotonic). Đây là **Stage 3** của epic editor: BE persist **góc xem mặc định** cho viewer 3D (`products.model3d_view` jsonb nullable, ADR-038). Worker `model_ingest` đã chuẩn-hoá geometry + viewer auto-frame → NULL = auto-frame (bất biến); góc-shop-lưu là **curation bồi thêm**. FE consume (viewer set camera-orbit + editor knobs) = **Stage 5** (ledger "Stage 5 consumes 1-4"). Product **phẳng / chưa lưu góc** byte-đồng-nhất (field optional, nil khi NULL).
> **`ADM-18` Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 38/39/40/41): test là **Go** (`services/core-api`), parser ledger `packages/core` chỉ resolve id **TS** → để `[x]` sẽ fail app-gate (memory `lumin-go-ears-stay-unchecked`).

- [ ] `ADM-18` — WHEN owner lưu góc xem mặc định (`PATCH /admin/products/{id}/model-view` — **owner-only** qua classify→`authOwnerOnly` **+** re-assert `assertOwner` [staff→**403**, no-actor→**401**, **trước mọi DB touch**]) HOẶC admin/storefront đọc product detail (`GET /admin/products/{id}` **hoặc** `GET /products/{slug}`), the system shall (ADR-038): **persist** — `products.model3d_view` **jsonb nullable** = một camera pose **atomic** `{orbitTheta,orbitPhi,orbitRadius,targetX,targetY,targetZ}` (deg·deg·% + 3 mét), set nguyên khối (nút "Lưu góc mặc định" riêng, KHÔNG nhét vào `ProductInput`); **validate server = tường** → **400** field-map: `orbitTheta ∈ [-360,360]`, `orbitPhi ∈ [0,180]`, `orbitRadius ∈ (0,1000]`, `targetX/Y/Z ∈ [-100,100]`, reject NaN/Inf (KHÔNG phải tiền → float hợp lệ, khác int-VND); unknown id → **404** (`:execrows` 0 rows); success → **204** (editor giữ pose vừa gửi); **read** — `productDTO` (dùng chung admin-get + storefront-get) trả `model3dView` optional, **nil khi NULL** (viewer auto-frame — hành vi hiện tại); **KHÔNG** đổi worker (pose tương đối canonical pose, re-ingest deterministic giữ hiệu lực), **KHÔNG** đổi money/pricing, **KHÔNG** backfill (SP cũ = NULL). *(test: `httpapi.TestProductModelViewEndToEnd` [save→admin+storefront read-back jsonb→unknown-404→out-of-range-400-giữ-nguyên, real-PG] + `httpapi.TestCleanModelView` [range/boundary/NaN/Inf per-field, Docker-free] + `TestAdminProductWritesAreOwnerOnly` [+model-view write × staff-403/no-actor-401] + classify `UpdateProductModelView`→`authOwnerOnly`)*

## Cụm 43 — Vật tư: filament tồn + bình quân gia quyền — BE (ADR-039 · spec §02 · slice 4a)

> **Cụm 42 (`ADM-18`) = Stage 3** (model-view, ngay trên) → Cụm 43 nối tiếp (monotonic). Đây là **Stage 4 slice 4a** của epic editor: greenfield Vật tư (**ADR-039**, user chọn FULL costing engine). Slice này = **inventory only**: palette filament shop-wide + lô nhập ("nhập cuộn") + tồn & giá vốn/đơn-vị **DERIVE** (bình quân gia quyền theo qty còn lại). Trừ-khi-in + cost snapshot = slice 4b; máy/phụ/hao-hụt = 4c; FE 4-tab = 4d.
> **`ADM-19` Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 38–42): test là **Go** (`services/core-api`), parser ledger `packages/core` chỉ resolve id **TS** → `[x]` sẽ fail app-gate (memory `lumin-go-ears-stay-unchecked`).

- [ ] `ADM-19` — WHEN owner quản-lý filament (`POST`/`PATCH /admin/filament-materials`, `POST .../{id}/import` — **owner-only** qua classify→`authOwnerOnly` **+** re-assert `assertOwner` [staff→**403**, no-actor→**401**, **trước mọi DB touch**]) HOẶC admin đọc (`GET /admin/filament-materials[?includeArchived]`, `GET .../{id}` — **owner+staff** default `authRequired`), the system shall (ADR-039 slice 4a): **persist** — bảng `filament_materials` (palette mỗi màu-có-tên: `material` PLA/PETG/recycled-PLA/Resin **text+CHECK** ADR-028, `unit` gram/ml, `hex?` **regex-guard CSS-injection**, `low_stock_threshold`, `archived`) + `filament_batches` (mỗi lô nhập: `qty_original`/`qty_remaining`/`total_cost_vnd` **int-VND** ADR-019); **tồn + giá vốn/đơn-vị DERIVE** (không lưu) = `Σ(qty_remaining × total_cost/qty_original) ÷ Σ(qty_remaining)` tính **một biểu thức numeric, float8 hiển thị** (RATE, KHÔNG phải money int — đóng băng int chỉ ở snapshot 4b); **nhập cuộn** = spoolCount × qtyPerSpool → qty_original, spoolCount × pricePerSpool → total_cost (validate bounds = overflow guard) → **200** material-with-stock; **validate = tường** → **400** field-map (name/material/unit/hex/threshold + import bounds); unknown material id → **404** (Get/Update `ErrNoRows`, Import FK 23503); **archived** = soft-delete (list default ẩn, `includeArchived` hiện). *(test: `httpapi.TestFilamentInventoryEndToEnd` [create→import 2 lô→**bình quân (180×390+1000×416)÷1180=₫412/g**→edit-giữ-tồn→archive-ẩn→unknown-404, real-PG] + `TestCleanFilamentMaterialInput`/`TestCleanFilamentImportInput` [range/hex/derive, Docker-free] + `TestFilamentWritesAreOwnerOnly` [3 write × staff-403/no-actor-401] + classify 3-write→`authOwnerOnly` + 2-read→`authRequired`)*

## Cụm 44 — Vật tư: trừ-khi-in (deduct-on-print) + định-mức catalog — BE (ADR-039 · spec §02 · slice 4b)

> **Cụm 43 (`ADM-19`) = Stage 4 slice 4a** (filament inventory, ngay trên) → Cụm 44 nối tiếp (monotonic). Đây là **Stage 4 slice 4b** của epic editor: **money/ops** — khi print job vào PRINTING **lần đầu**, trừ filament **FIFO** khỏi lô cũ nhất + đóng băng giá vốn thực vào ledger `filament_consumption`; **định-mức** (est-gram per-part/product + màu→cuộn) capture trên catalog write. **Lệch ADR (bồi vào 4c, KHÔNG cắt):** `cost_snapshot` rollup (máy/phụ/hao-hụt) + `est_print_hours` → **4c** (rollup cần bảng máy/phụ của 4c; filament-only snapshot chốt-lúc-in không hoàn thiện được — no-backfill). Giá vốn filament vẫn đóng băng lúc in trong `filament_consumption.cost_vnd` (4c rollup đọc lại). FE 4-tab = 4d.
> **`ADM-20` Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 38–43): test là **Go** (`services/core-api`), parser ledger `packages/core` chỉ resolve id **TS** → `[x]` sẽ fail app-gate (memory `lumin-go-ears-stay-unchecked`).

- [ ] `ADM-20` — WHEN staff kéo print job vào **PRINTING** (`PATCH /admin/print-jobs/{id}` — **owner+staff** default `authRequired`, D6 độc-lập order status) HOẶC owner đặt định-mức trên catalog write (`POST/PATCH /admin/products[/{id}]` · `.../parts` · `.../colors` — **owner-only** classify+`assertOwner`), the system shall (ADR-039 slice 4b): **định-mức (persist)** — `products.est_filament_qty` (SP phẳng) + `parts.est_filament_qty` (per-part ADR-037 two-tone) + `colors.filament_material_id` **nullable FK** (màu → cuộn shop, `ON DELETE SET NULL`), tất cả **additive DEFAULT 0/NULL** (đơn/SP cũ giữ nguyên); bad `filamentMaterialId` → **400** field (phân biệt FK-constraint-name khỏi product-404); **trừ-khi-in (money/ops)** — `AdvancePrintStageTx`: **claim atomic** (`UPDATE … SET stage='PRINTING', filament_deducted_at=now() WHERE id=$1 AND filament_deducted_at IS NULL RETURNING`) → chỉ **lần đầu** →PRINTING trừ (re-drag PACKING→PRINTING / hai staff cùng kéo = 0 rows → chỉ set stage, **KHÔNG** trừ lại); resolve dòng in → **FIFO-decrement** lô cũ nhất (`FOR UPDATE` serialize), **clamp sàn 0** (thiếu ≠ chặn board ADR-012, log shortfall — ghi qty **THỰC** để không đầu độc mẫu số bình quân), ghi **một** `filament_consumption` row (`kind=print`, `qty` thực, `cost_vnd` = Σ(gram-lô × ₫/lô) **big.Rat làm tròn một lần** = đóng băng giá vốn filament, `order_item_id`, `product_name`); SP phẳng est 0 / màu chưa nối cuộn / part đã xoá → **skip sạch**; toàn bộ trên **một tx** (claim + trừ + ledger atomic — lỗi rollback cả claim → retry, KHÔNG half-draw); **KHÔNG** `cost_snapshot` (rollup máy/phụ = 4c), **KHÔNG** money-path khách (giá vốn nội-bộ tách khỏi `PriceItem`). *(test: `httpapi.TestDeductOnPrintEndToEnd` [→PRINTING trừ 150g FIFO stock 200→50, ledger ₫58500 frozen + `filament_deducted_at`, re-drag PACKING→PRINTING idempotent, real-PG] + `TestDeductOnPrintShortfallClamps` [est>tồn → clamp, ledger ghi qty **thực** 50 không phải est 150] + `db.TestFifoDraw`/`db.TestRatToVND` [FIFO/clamp/round-once, Docker-free] + `httpapi.TestResolveDeductionLines` [flat/parts/skip, Docker-free] + `TestCleanPartInput` [est ≥ 0])*

## Cụm 45 — Vật tư: máy (giờ khấu hao) + chi phí phụ + scrap-log — BE (ADR-039 · spec §02 · slice 4c-1)

> **Cụm 44 (`ADM-20`) = Stage 4 slice 4b** (deduct-on-print, ngay trên) → Cụm 45 nối tiếp (monotonic). Đây là **Stage 4 slice 4c-1** của epic editor: **cost-input layer** — `machines` (khấu hao → ₫/giờ) + `aux_costs` (overhead) CRUD + scrap-log (hao-hụt). Rate tài nguyên owner-curate; **KHÔNG money-freeze** (₫/giờ + phân bổ = derive downstream). **4c chia đôi:** 4c-1 = input tables (đây) · **4c-2** = COGS snapshot rollup (chốt-lúc-in, oracle) + waste-factor + KPI/margin read + `est_print_hours` (migration 000021). FE 4-tab = 4d.
> **`ADM-21` Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 38–44): test là **Go** (`services/core-api`), parser ledger `packages/core` chỉ resolve id **TS** → `[x]` sẽ fail app-gate (memory `lumin-go-ears-stay-unchecked`).

- [ ] `ADM-21` — WHEN owner quản-lý máy/chi-phí-phụ (`POST/PATCH/DELETE /admin/machines[/{id}]` · `/admin/aux-costs[/{id}]`) HOẶC ghi scrap (`POST /admin/filament-materials/{id}/scrap`) — tất cả **owner-only** (classify→`authOwnerOnly` + `assertOwner` [staff→**403**, no-actor→**401**, **trước mọi DB touch**]) HOẶC admin đọc (`GET /admin/machines`, `GET /admin/aux-costs` — **owner+staff** default `authRequired`), the system shall (ADR-039 slice 4c-1): **persist** — bảng `machines` (`purchase_price_vnd`/`depreciation_months>0`/`expected_hours_per_month>0` int-VND+CHECK, `is_primary`, `active`) + `aux_costs` (`label`, `kind` per_order\|per_month **text+CHECK** ADR-028, `amount_vnd` int-VND); **₫/giờ DERIVE** (không lưu, ADR-039 pt 8) = `purchase ÷ (months × hours)` tính ở DTO (CHECK >0 = divisor an toàn), **float** (rate, KHÔNG money int); **scrap** = draw FIFO qua **cùng helper trừ-khi-in** (`Filament.Decrement`, `kind='scrap'`) trong một tx (FOR UPDATE lock giữ qua multi-statement) → cùng ledger `filament_consumption` mà waste-factor (4c-2) đọc; unknown material → **404** (check `GetMaterial` **trước** draw vì draw-no-lots sẽ no-op câm); over-scrap → **clamp** (không lỗi); **validate = tường** → **400** field-map (name/price/months/hours · label/kind/amount · scrap qty>0); unknown machine/aux id → **404** (`RETURNING` 0 rows). **KHÔNG** money-freeze (₫/giờ + phân bổ derive), **KHÔNG** cost_snapshot (4c-2), **KHÔNG** money-path khách. *(test: `httpapi.TestMachinesCRUDEndToEnd` [create ₫/h 24tr÷(24×100)=₫10000 → update ₫/h ₫20000 → delete → unknown 404, real-PG] + `TestAuxCostsCRUDEndToEnd` [per_order+per_month CRUD] + `TestScrapFilamentEndToEnd` [scrap 40g→stock 160 + ledger kind=scrap ₫15600 → unknown-404 → over-scrap 500 clamp→stock 0] + Docker-free `TestCleanMachineInput`/`TestCleanAuxCostInput`/`TestCleanScrapInput`/`TestMachineDTOCostPerHour` + `TestCostingWritesAreOwnerOnly` [7 write × staff-403/no-actor-401] + classify pin 9 op)*

## Cụm 46 — Vật tư: COGS snapshot chốt-lúc-in + KPI/margin read — BE (ADR-039 · spec §02 · slice 4c-2)

> **Cụm 45 (`ADM-21`) = Stage 4 slice 4c-1** (máy/phụ/scrap cost inputs, ngay trên) → Cụm 46 nối tiếp (monotonic). Đây là **Stage 4 slice 4c-2** của epic editor: **money-freeze** — khi print job vào PRINTING **lần đầu**, rollup best-effort **SAU commit** đóng băng giá vốn cả đơn vào `order_items.cost_snapshot` (jsonb NULL). Đọc lại filament **đóng băng 4b** (`filament_consumption.cost_vnd`, oracle R2: **SUM** per `order_item_id`, KHÔNG re-derive từ lô sống) + rollup máy/hao-hụt/phụ. `products.est_print_hours` (định-mức giờ máy, lưu **phút chính xác**) feed machineVnd. KPI read (`GET /admin/costing-summary`) dùng **cùng công thức** waste-factor + đơn-thực-30d → biên khớp dashboard. FE 4-tab + snapshot card = 4d.
> **`ADM-22` Go-gated — CỐ Ý để `[ ]`** (cùng lý do Cụm 38–45): test là **Go** (`services/core-api`), parser ledger `packages/core` chỉ resolve id **TS** → `[x]` sẽ fail app-gate (memory `lumin-go-ears-stay-unchecked`).

- [ ] `ADM-22` — WHEN print job vào **PRINTING lần đầu** (`PATCH /admin/print-jobs/{id}` — sau khi 4b trừ filament trong-tx) HOẶC owner đặt định-mức giờ (`POST/PATCH /admin/products[/{id}]`, `estPrintHours`) HOẶC admin đọc KPI (`GET /admin/costing-summary` — **owner+staff** default `authRequired`), the system shall (ADR-039 slice 4c-2 pt 5/6/7): **migration 000021** — `order_items.cost_snapshot` **jsonb NULLABLE** (NULL = "chưa chốt", **KHÁC** ₫0-drawn — oracle R1: một print starved trừ 0g vẫn ghi snapshot filamentVnd=0, KHÔNG để NULL) + `products.est_print_minutes` (int, additive DEFAULT 0; wire = `estPrintHours` giờ, handler đổi giờ×60 = **phút chính xác** → money-freeze không float, không pgtype.Numeric); **rollup best-effort SAU commit** (`Costing.SnapshotOrderItem`) — chỉ khi lần này **claim** trừ filament (first →PRINTING); **KHÔNG** trên tx tạo đơn / **KHÔNG** fail move / **KHÔNG** chặn board (lỗi → log + `cost_snapshot` NULL backfill-được — costing KHÔNG cuốn đơn đã trả); **compute** (int-VND làm tròn **một lần** qua `ratToVND` blessed 4b, big.Rat exact — không float trong tiền đóng băng): `filamentVnd` = Σ `filament_consumption.cost_vnd` (kind=print) của item (oracle R2 SUM, đọc frozen) · `machineVnd` = est_print_hours × ₫/giờ máy `isPrimary` (chọn **most-recently-updated active primary** LIMIT 1 — spec-guardian 4c-1 NOTE; không primary → 0) · `wasteVnd` = (filamentVnd + machineVnd) × hệ-số-hao-hụt-30d (`Σscrap ÷ Σprint` gram, guard print=0→0; base = **filament+machine** khớp design card, KHÔNG aux) · `auxVnd` = `Σper_order + Σper_month ÷ đơn-thực-30d` (guard 0-đơn → phần per_month = 0) · `totalVnd` = tổng; **đọc lại** — `order_items.cost_snapshot` → `OrderItem.costSnapshot` DTO (owner+staff, omit khi NULL → dòng flat/chưa-chốt byte-identical; biên = giá bán − totalVnd tính client); **KPI** — `GET /admin/costing-summary` dùng **cùng** `SnapshotShopInputs` + `auxPerOrderVnd`/`wasteFactorFloat` helpers (đơn-thực-30d = `payment_confirmed_at ≥ now()−30d AND status <> 'REFUNDED'`, khớp predicate net-revenue dashboard.sql) → `wasteFactor`/`auxPerOrderVnd`/`realOrders30d`/`primaryMachineVndPerHour?`. **KHÔNG** money-path khách (giá vốn/biên tách khỏi `PriceItem`). *(test: `httpapi.TestCostSnapshotEndToEnd` [→PRINTING → snapshot filament ₫58500 + máy 6h×₫2000=₫12000 + hao-hụt (58500+12000)×0.1=₫7050 + phụ ₫18000 = **₫95550**, DTO đọc lại, `GET /admin/costing-summary` waste 0.1/aux 18000/₫h 2000, real-PG] + `TestCostSnapshotNoPrimaryMachine` [không primary → machineVnd 0, vẫn chốt non-NULL] + Docker-free `db.TestComputeCostSnapshot` [**design card ₫119,178** + guards: no-machine/no-print/aux-amortize/0-đơn/starved] + `httpapi.TestCostSnapshotDTO` [JSON-tag alignment writer↔wire] + `TestCleanProductInput` [estPrintHours 6.5→390 phút · negative/NaN/over-cap → 400] + classify pin `GetCostingSummary`)*

## Cụm 47 — Vật tư & chi phí FE (`/vat-tu`) — read-only 4-tab dashboard (ADR-039 slice 4d-1 · Track D · spec §03 State)

> **Cụm 46 (`ADM-22`) = Stage 4 slice 4c-2** (COGS snapshot BE, ngay trên) → Cụm 47 nối tiếp (monotonic). Đây là **Stage 4 slice 4d-1** — FE tiêu thụ 4 read endpoint đã merged (filament-materials #82 · machines/aux-costs #84 · costing-summary #85): KPI row + 4 tab (Filament · Giờ máy · Chi phí phụ · Hao hụt) **read-only**. Write dialogs (nhập cuộn / thêm máy / thêm chi phí / ghi in hỏng) = **4d-2**; snapshot card + biên trên order-detail = **4d-3** (oracle carry-forwards).
> **`ADM-23` FE** — CỐ Ý để `[ ]` (cùng quy ước Cụm 37 `ADM-15`): logic thuần có test `apps/admin/test/materials.test.ts` nhưng không mang đúng id `ADM-23`; **gate thật** = chính test đó (vitest) + Playwright Phase 5.

- [ ] `ADM-23` — WHEN owner/staff mở `/vat-tu` (đã đăng nhập; RSC **Promise.all** 4 read `GET /admin/{filament-materials,machines,aux-costs,costing-summary}` cookie-forward `no-store`, lỗi → `(app)/error.tsx`, loading → skeleton), the system shall render **bảng điều khiển giá vốn read-only**: **KPI row** (tồn filament = Σ stockQty gram + đếm màu sắp hết · đơn giá giờ máy = `primaryMachineVndPerHour` server-authoritative · hệ số hao hụt = `wasteFactor` → +X,X% · phân bổ phụ/đơn = `auxPerOrderVnd` + `realOrders30d`) + **4 tab WAI-ARIA** (role tablist/tab/tabpanel, roving tabindex + Arrow/Home/End): **Filament** (bảng màu/loại/giá-vốn-đơn-vị/tồn/trạng-thái [đủ teal · sắp-hết danger · theo-dõi sun] + card bình-quân-gia-quyền) · **Giờ máy** (bảng máy + card cách-tính máy chính khấu-hao→₫/giờ) · **Chi phí phụ** (2 bảng per-order/per-month + card phân-bổ = `auxPerOrderVnd` authoritative) · **Hao hụt** (card hệ-số-hao-hụt 30 ngày). **Tiền:** int-VND qua `formatVnd`; **rate** (₫/đơn-vị avg · ₫/giờ) là float → `Math.round` một lần cho MỘT formatter, 0/absent → "—" KHÔNG "0₫"; qty qua `formatVnNumber`; % qua `formatVnRating`; **ZERO** `toLocaleString`/`Intl` rải rác (always-must #2). i18n `materials.*` (không hard-code); trạng thái = enum→label i18n (#3); tab active token `bg-primary`/`text-on-primary` (AA-safe); mỗi tab đủ **empty** (informative — CTA tạo ở 4d-2) **· loading · error**; reduced-motion tự thoả. **Gap design→API flagged:** KPI avg-giá-vốn/biên-TB cần snapshot-aggregate endpoint (4c-2 chưa build) → render 4 KPI backed thật, KHÔNG fabricate. **KHÔNG** write · **KHÔNG** contract/codegen/migration/dep. *(test: `apps/admin/test/materials.test.ts` → `filamentStatus`/`filamentRows`/`filamentStockGrams`/`lowStockCount`/`primaryMachine`/`splitAuxCosts`/`sumAmountVnd`/`wastePercent`/`unitSymbol`; render/tab tương tác = Playwright Phase 5)*

## Cụm 48 — Vật tư & chi phí FE (`/vat-tu`) — write dialogs (ADR-039 slice 4d-2 · Track D · spec §03 State)

> **Cụm 47 (`ADM-23`) = Stage 4 slice 4d-1** (đọc, ngay trên) → Cụm 48 nối tiếp (monotonic). Đây là **Stage 4 slice 4d-2** — 5 dialog GHI khớp 5 CTA design screen 8, gọi write endpoint đã có sẵn từ BE (4a Create/Import · 4c-1 machines/aux Create · scrap). Pure FE: **KHÔNG** contract/codegen/migration/dep. Edit/delete từng dòng KHÔNG thuộc slice này (design screen 8 chỉ hiện thêm/nhập/ghi — YAGNI, để dành).
> **`ADM-24` FE** — CỐ Ý để `[ ]` (cùng quy ước Cụm 37/47): logic thuần có test `apps/admin/test/materials.test.ts` nhưng không mang đúng id `ADM-24`; **gate thật** = chính test đó (vitest) + Playwright Phase 5.

- [ ] `ADM-24` — WHEN owner mở một dialog ghi từ action-bar của tab tương ứng trên `/vat-tu` (Filament → **thêm vật tư** `POST /admin/filament-materials` + **nhập cuộn** `POST …/{id}/import`; Giờ máy → **thêm máy** `POST /admin/machines`; Chi phí phụ → **thêm chi phí** `POST /admin/aux-costs`; Hao hụt → **ghi in hỏng** `POST …/{id}/scrap`) rồi submit, the system shall gọi Server Action tương ứng (`materials-actions.ts`, cookie-forward, owner-only ở server → staff 403 collapse `forbidden`) và khi thành công **router.refresh()** đọc lại RSC. **Native `<dialog>` showModal** (Esc/backdrop close + focus-trap); submit **khoá tới khi** field bắt buộc hợp lệ (client chỉ chặn submit dở — server re-validate mọi field: bound tiền/qty, hex regex, id lạ → 404 → collapse `notFound`). **Tiền:** nhập số nguyên int-VND thô vào number input (`parseIntField` → non-neg int hoặc null; count ≥1 · money ≥0 gate ở client), **echo `formatVnd` sống dưới ô** = MỘT formatter core, **ZERO** `toLocaleString`/`Intl` rải rác (always-must #2). i18n `materials.dialog.*` (không hard-code); enum (loại nhựa/đơn vị/kind/màu) qua `<select>` label-wired; error envelope thô KHÔNG lộ (#3, ADR-032); import/scrap disable khi palette rỗng. **KHÔNG** contract/codegen/migration/dep · **KHÔNG** money-freeze (ghi input, snapshot đóng băng ở 4c-2). *(test: `apps/admin/test/materials.test.ts` → `parseIntField` [non-neg int · trim · reject blank/fractional/negative/non-numeric]; dialog render + submit + refresh = Playwright Phase 5)*

## Cụm 49 — Giá vốn & lãi trên order-detail — snapshot card FE (ADR-039 slice 4d-3 · Track D · spec §03 State)

> **Cụm 48 (`ADM-24`) = Stage 4 slice 4d-2** (write dialogs, ngay trên) → Cụm 49 nối tiếp (monotonic). Đây là **Stage 4 slice 4d-3** (khép Stage 4) — order-detail (`don-hang/{id}`) hiện **giá vốn đóng băng** (`OrderItem.costSnapshot`, 4c-2) + **biên** mỗi dòng in. Pure FE đọc: **KHÔNG** contract/codegen/migration/dep. Render trung thực snapshot đã đóng băng, KHÔNG tính lại từ máy/phụ sống (oracle carry-forwards 4c-2).
> **`ADM-25` FE** — CỐ Ý để `[ ]` (cùng quy ước Cụm 37/47/48): logic thuần (`lineMargin`) có test `apps/admin/test/order-detail.test.ts` nhưng không mang đúng id `ADM-25`; **gate thật** = chính test đó (vitest) + Playwright Phase 5.

- [ ] `ADM-25` — WHEN owner mở `don-hang/{id}` của một đơn đã có ≥1 dòng in (`OrderItem.costSnapshot` non-NULL, đóng băng lần đầu →PRINTING), the system shall render card **"Giá vốn & lãi"** (chỉ hiện khi có ≥1 dòng đã chốt): mỗi dòng in hiện **breakdown** giá vốn từ blob đóng băng — filament (`filamentVnd`) · giờ máy (`machineVnd`, hint `{estPrintHours}h × {machineVndPerHour}` **từ blob**, ẩn khi rate 0) · hao hụt (`wasteVnd`, hint `{wasteFactor×100}%` từ blob, ẩn khi 0) · chi phí phụ (`auxVnd`) → **Tổng giá vốn** (`totalVnd`) · **Giá bán** (= `unitPrice × quantity` — snapshot đã ×qty ⇒ so với doanh thu cả dòng) · **Lãi** (`revenue − totalVnd`, teal ≥0 · danger <0, phần trăm `biên X,X%`); dòng **chưa in** (`costSnapshot` NULL) hiện **"Chưa chốt"** — KHÔNG ₫0 margin (oracle carry-forward #1). **Tiền:** int-VND qua `formatVnd` (âm → `−` + `formatVnd(|x|)` vì formatter chặn số âm); rate/% (`estPrintHours`/`wasteFactor`/biên) qua `formatVnRating` (float 1 chữ số); **ZERO** `toLocaleString`/`Intl` rải rác (always-must #2). Rate hiện **từ blob đóng băng**, KHÔNG derive từ máy/phụ sống (carry-forward #2 — drift phá freeze). **KHÔNG** cộng gộp lãi cả đơn khi >1 dòng chốt (aux phân bổ per-order đóng băng mỗi dòng → cộng sẽ trùng; note caveat, hiện lãi per-line — carry-forward #3). i18n `orderDetail.*` (không hard-code). **KHÔNG** contract/codegen/migration/dep · **KHÔNG** money-freeze (đọc snapshot, không ghi). *(test: `apps/admin/test/order-detail.test.ts` → `lineMargin` [design ₫320.000−₫119.178=₫200.822 biên 62,8% · ×qty · âm khi bán dưới vốn · null % khi doanh thu 0]; render card + empty "chưa chốt" = Playwright Phase 5)*

## Cụm 50 — Product editor `/san-pham/{id}` + `/san-pham/moi` — core CRUD FE (P3-l Stage 5 slice l-1 · Track D · spec §02/§03 State)

> **Cụm 49 (`ADM-25`) = Stage 4 slice 4d-3** (khép Stage 4) → Cụm 50 mở **Stage 5** (P3-l FE product editor, tiêu thụ stage 1–4; monotonic). Đây là **slice l-1** — **core Product CRUD** lấp 2 dead seam P3-k để lại (card → `/san-pham/{id}`, `+ Thêm` → `/san-pham/moi`). Chỉ trường core `ProductInput` (tên/slug/mô tả/danh mục/giá/kích thước/chất liệu/trạng thái); **gallery ảnh · màu · bộ phận · tuỳ chọn · sub-choice · định-mức · model/asset-job · căn-chỉnh 3D · preview = slice sau** (l-2+, quyết ở gate). Pure FE: **KHÔNG** contract/codegen/migration/dep.
> **`ADM-26` FE** — CỐ Ý để `[ ]` (cùng quy ước Cụm 37/47/48/49): logic thuần (`product-form.ts`) có test `apps/admin/test/product-form.test.ts` nhưng không mang đúng id `ADM-26`; **gate thật** = chính test đó (vitest) + Playwright Phase 5.

- [ ] `ADM-26` — WHEN owner mở `/san-pham/moi` (tạo) HOẶC `/san-pham/{id}` (sửa, RSC `Promise.all` product `GET /admin/products/{id}` + `GET /categories` cookie-forward; unknown id → not-found card; lỗi khác → `(app)/error.tsx`; loading → skeleton) rồi lưu/xoá, the system shall: **tạo** — thu trường core → `validateDraft` (client mirror BE: tên 1–200 · slug `^[a-z0-9]+(?:-[a-z0-9]+)*$` ≤200 · mô tả ≤10k · giá int-VND ≥0 · dims w/d/h int >0 · chất liệu ∈{PLA,PETG,recycled-PLA} · trạng thái ∈{active,draft,archived}) → `POST /admin/products` → **redirect `/san-pham/{id}`** (aggregate-root grain: màu/tuỳ-chọn/model cần id); **sửa** — `PATCH /admin/products/{id}` → `router.refresh()` + feedback "Đã lưu 🧡"; **xoá** — `DELETE` (xác nhận 2 bước, KHÔNG browser dialog) → redirect `/san-pham`, **409 `PRODUCT_IN_USE`** → nudge chuyển Lưu trữ. **Owner-only ở server** (BE `authOwnerOnly`+`assertOwner`; staff → collapse `forbidden`); FE optimistic owner (P3-e ROLE precedent, chưa `/auth/me` tới P3-q). **400 field-map** — `ErrorEnvelope.fields` (slug trùng → mark field `taken`; categoryId lạ → mark; các field khác → `invalid`) vì client validate chạy trước nên slug 400 sống-sót = trùng. **Tiền:** giá nhập số nguyên int-VND thô vào number input (`parseIntField`), **echo `formatVnd` sống** dưới ô = MỘT formatter core, **ZERO** `toLocaleString`/`Intl` rải rác (always-must #2). enum (danh mục/trạng thái/chất liệu) qua `<select>` label-wired (chưa có UI Select primitive → hand-roll token-styled như materials-dialog); `<textarea>` mô tả; slug auto-suggest `slugify` (fold dấu tiếng Việt + đ→d). Danh mục: `/categories` chỉ liệt kê danh mục có SP active → khi sửa giữ `categoryId` hiện tại làm option fallback (plan §6). i18n `products.edit.*` (không hard-code); mỗi route đủ **empty/loading/error** (loading skeleton · error boundary · not-found card). **KHÔNG** contract/codegen/migration/dep · **KHÔNG** money-freeze. *(test: `apps/admin/test/product-form.test.ts` → `validateDraft`/`draftFromProduct`↔`draftToInput` round-trip/`slugify`/`serverFieldErrors`/`emptyDraft`; render + create/edit/delete round-trip = Playwright Phase 5)*

## Cụm 51 — Product editor media — gallery ảnh + model 3D/asset-job FE (P3-l Stage 5 slice l-2 · Track D · spec §02/§03 State)

> **Cụm 50 (`ADM-26`) = Stage 5 slice l-1** (core CRUD) → Cụm 51 nối tiếp (monotonic). Đây là **slice l-2** — **media**: gallery ảnh (`Product.images`, lưu-cùng-SP) + pipeline model 3D (upload `.glb/.stl/.3mf` → asset-job → poll trạng thái). Chỉ hiện ở **mode sửa** (endpoint model-upload/asset-jobs cần product id). **màu · bộ phận · tuỳ chọn · sub-choice · định-mức · căn-chỉnh 3D · live-preview = slice sau** (l-3+). Pure FE: **KHÔNG** contract/codegen/migration/dep (tái dùng `upload-proof` + endpoint P3-j-b/ADR-036 đã có).
> **`ADM-27` FE** — CỐ Ý để `[ ]` (cùng quy ước Cụm 37/47/48/49/50): logic thuần (`upload-model.ts`) có test `apps/admin/test/upload-model.test.ts` nhưng không mang đúng id `ADM-27`; **gate thật** = chính test đó (vitest) + Playwright Phase 5.

- [ ] `ADM-27` — WHEN owner mở `/san-pham/{id}` (sửa) rồi quản lý media, the system shall: **gallery** — tải ảnh (JPG/PNG/WebP) qua presigned-POST dùng chung (`upload-proof` → thẳng Garage), hiện thumbnail lưới; `images[0]` = **ảnh bìa** (ADR-007: worker **KHÔNG** render bìa — bìa là ảnh shop chụp; sprite 360° chỉ dùng hover/fallback); "đặt làm bìa" (đưa lên đầu) + "xoá ảnh" (hit-target ≥44px); gallery là **trường Product** → lưu cùng "Lưu sản phẩm" (PATCH `images[]`, KHÔNG resource riêng); **model 3D** — tải model (`.glb/.stl/.3mf`, ≤100MB) qua model-upload presign (owner-only, ADR-036) → **SHA-256 hex `sourceVersion`** (`crypto.subtle`, ADR-004) → POST thẳng Garage → enqueue **CẢ HAI** job `model_ingest` (→ `model3dUrl`, viewer l-5) + `sprite_render` (→ ảnh xoay 360° hover/fallback no-WebGL) từ cùng nguồn; **poll** `GET .../asset-jobs` (KHÔNG SSE) khi còn job `queued|processing`, cap ~80s (worker slice-3 chưa nối → job đứng `queued`) → dừng + nút "Làm mới"; settle → `router.refresh()` kéo `model3dUrl`/`images` mới. Badge trạng thái `queued|processing|ready|failed`; `failed` hiện `lastError`. **Owner-only ở server** (presign + create-job `authOwnerOnly`; đọc job owner+staff); FE optimistic owner. i18n `products.edit.gallery.*`/`products.edit.model.*` (không hard-code). **KHÔNG** contract/codegen/migration/dep · **KHÔNG** money (media, không đường tiền). *(test: `apps/admin/test/upload-model.test.ts` → `modelMimeFor` [ext→MIME · reject] + `sha256Hex` [known vectors]; upload/enqueue/poll + render = Playwright Phase 5)*

## Cụm 52 — Product editor màu & bộ phận — parts + colours CRUD FE (P3-l Stage 5 slice l-3 · Track D · ADR-037/ADR-039 · spec §02/§03 State)

> **Cụm 51 (`ADM-27`) = Stage 5 slice l-2** (media) → Cụm 52 nối tiếp (monotonic). Đây là **slice l-3** — **màu & bộ phận** (trái tim two-tone ADR-037): per-row CRUD parts + colours, màu nhóm dưới bộ phận qua `Color.partId`, link kho filament qua `Color.filamentMaterialId` (ADR-039). Chỉ hiện ở **mode sửa** (sub-resource cần product id). **tuỳ chọn + sub-choice = l-4 · căn-chỉnh 3D + live-preview = l-5.** Pure FE: **KHÔNG** contract/codegen/migration/dep (endpoint parts/colors đã có từ P3-j-a #73 + ADR-037 Stage 2).
> **`ADM-28` FE** — CỐ Ý để `[ ]` (cùng quy ước Cụm 37/47/48/49/50/51): logic thuần (`product-colors.ts`) có test `apps/admin/test/product-colors.test.ts` nhưng không mang đúng id `ADM-28`; **gate thật** = chính test đó (vitest) + Playwright Phase 5.

- [ ] `ADM-28` — WHEN owner mở `/san-pham/{id}` (sửa) rồi quản lý màu & bộ phận, the system shall: **bộ phận** — per-row CRUD `POST/PATCH/DELETE /admin/products/{id}/parts[/{partId}]` (owner-only, ADR-037); part `{name, displayOrder (tự nối đuôi, không bắt owner nhập), estFilamentQty? (ADR-039 định mức/cái)}`; xoá part **cascade** màu của nó, **409** nếu một màu đã bị đơn ghim → nudge chuyển Lưu trữ; **màu** — per-row CRUD `.../colors[/{colorId}]`; colour `{name, hex (client mirror `^#([0-9a-fA-F]{3}|[0-9a-fA-F]{6})$` + swatch preview), available (Switch), priceDelta int-VND ≥0, partId nullable (nhóm dưới bộ phận — khách chọn 1 màu/bộ phận), filamentMaterialId nullable (link kho ADR-039 → deduct-on-print biết cuộn nào)}`; màu render **nhóm theo bộ phận** (`groupColorsByPart`: sort `displayOrder` → part rỗng vẫn có nhóm · flat group cuối cho partId null/dangling) + badge `+phụ giá`/`Tạm hết`/tên filament; **edit-mode only** (sub-resource cần product id); dialog **native `<dialog>`** (Esc/backdrop/focus-trap), **xoá 2 bước inline** (KHÔNG browser dialog); persist **per-row** → `router.refresh()`. **Owner-only ở server** (BE `authOwnerOnly`; staff → collapse `forbidden`); FE optimistic owner (P3-e ROLE precedent). **Tiền:** priceDelta int-VND thô + echo `formatVnd` sống = MỘT formatter core, **ZERO** `toLocaleString`/`Intl` rải rác (always-must #2). Palette filament `GET /admin/filament-materials` (lenient → [], phụ trợ — miss chỉ mất dropdown link, không chặn trang). i18n `products.edit.colors.*` (không hard-code). **KHÔNG** contract/codegen/migration/dep · **KHÔNG** money-freeze (ghi catalog, không đóng băng đơn). *(test: `apps/admin/test/product-colors.test.ts` → `groupColorsByPart` [order theo displayOrder · flat cuối · dangling partId → flat · empty part-group]; CRUD + dialog + grouping render = Playwright Phase 5)*

## Cụm 53 — Product editor tuỳ chọn — options + choices CRUD FE (P3-l Stage 5 slice l-4 · Track D · ADR-037 · spec §02/§03 State)

> **Cụm 52 (`ADM-28`) = Stage 5 slice l-3** (màu & bộ phận) → Cụm 53 nối tiếp (monotonic). Đây là **slice l-4** — **tuỳ chọn cá nhân hoá**: per-row CRUD options + enumerated `OptionChoice` lồng dưới option kiểu `choice`. Chỉ hiện ở **mode sửa** (sub-resource cần product id). **căn-chỉnh 3D + live-preview = l-5** (khép Stage 5). Pure FE: **KHÔNG** contract/codegen/migration/dep (endpoint options/choices đã có từ P3-j-a #73 + ADR-037 Stage 2).
> **`ADM-29` FE** — CỐ Ý để `[ ]` (cùng quy ước Cụm 37/47/48/49/50/51/52): logic thuần (`product-options.ts`) có test `apps/admin/test/product-options.test.ts` nhưng không mang đúng id `ADM-29`; **gate thật** = chính test đó (vitest) + Playwright Phase 5.

- [ ] `ADM-29` — WHEN owner mở `/san-pham/{id}` (sửa) rồi quản lý tuỳ chọn, the system shall: **option** — per-row CRUD `POST/PATCH/DELETE /admin/products/{id}/options[/{optionId}]` (owner-only, ADR-037); option `{label, description?, type∈{text,choice}, priceDelta int-VND ≥0, maxChars?}`; **`text`** = ô khắc chữ (maxChars: để trống = không giới hạn · nhập = int >0 [client mirror, BE là tường]) → badge "Khắc ≤ N ký tự"; **`choice`** = danh sách lựa chọn khách chọn một → hiện choices lồng; **choice** — per-row CRUD `.../options/{optionId}/choices[/{choiceId}]`; choice `{label, description?, priceDelta int-VND ≥0, displayOrder (tự nối đuôi)}`; choices render **sort theo displayOrder** (`sortChoices`, tiebreak label) dưới option, mỗi choice có sửa/xoá + "thêm lựa chọn"; **edit-mode only** (sub-resource cần product id); dialog **native `<dialog>`** (Esc/backdrop/focus-trap), **xoá 2 bước inline** (KHÔNG browser dialog); persist **per-row** → `router.refresh()`. **Owner-only ở server** (BE `authOwnerOnly`; staff → collapse `forbidden`); FE optimistic owner (P3-e ROLE precedent). **Tiền:** priceDelta int-VND thô + echo `formatVnd` sống = MỘT formatter core, **ZERO** `toLocaleString`/`Intl` rải rác (always-must #2). i18n `products.edit.options.*` (không hard-code). **KHÔNG** contract/codegen/migration/dep · **KHÔNG** money-freeze (ghi catalog, không đóng băng đơn). *(test: `apps/admin/test/product-options.test.ts` → `sortChoices` [order theo displayOrder · tiebreak label · không mutate/empty]; CRUD + dialog + text-vs-choice render = Playwright Phase 5)*

## Cụm 54 — Product editor xem trước & căn chỉnh 3D — Model3dView save FE (P3-l Stage 5 slice l-5 · Track D · ADR-038 · khép Stage 5 & epic)

> **Cụm 53 (`ADM-29`) = Stage 5 slice l-4** (tuỳ chọn) → Cụm 54 nối tiếp (monotonic). Đây là **slice l-5 — KHÉP Stage 5 & epic ADR-037/ADR-039**: xem trước 3D (customer-style) + lưu **góc camera mặc định** (`Model3dView`, ADR-038) qua `PATCH /admin/products/{id}/model-view`. Chỉ hiện ở **mode sửa** + chỉ khi model đã dựng (`model3dUrl` non-empty). **+1 dep admin** `@google/model-viewer@^4.3.1` (đã dùng ở storefront). Endpoint model-view đã có từ ADR-038 Stage 3 #81 → **KHÔNG** contract/codegen/migration.
> **`ADM-30` FE** — CỐ Ý để `[ ]` (cùng quy ước Cụm 37/47/48/49/50/51/52/53): logic thuần (`model-view.ts`) có test `apps/admin/test/model-view.test.ts` nhưng không mang đúng id `ADM-30`; **gate thật** = chính test đó (vitest) + Playwright Phase 5.

- [ ] `ADM-30` — WHEN owner mở `/san-pham/{id}` (sửa) một sản phẩm **đã có `model3dUrl`** (worker dựng xong) rồi xem trước/căn chỉnh 3D, the system shall: **xem trước** — nạp `@google/model-viewer` **on-demand** (dynamic `import()` → ~1MB ngoài initial bundle; SSR-safe vì `<model-viewer>` chỉ render sau khi module load client), hiện mẫu 3D với `camera-controls` (xoay/kéo), **mở ở đúng pose đã lưu** (`product.model3dView` → attr `camera-orbit="{θ}deg {φ}deg {r}%"` + `camera-target="{x}m {y}m {z}m"` qua `model3dViewToAttrs`); **căn chỉnh** — nút "Lưu góc mặc định" đọc camera hiện tại (`getCameraOrbit()` rad+m · `getCameraTarget()` m) → `orbitToModel3dView` chuyển **rad→độ · m→% của bán kính lý tưởng** (bắt `idealRadius` lúc load từ **phần trăm khởi tạo đã biết** — không hằng số ma thuật) + **clamp** theo range ADR-038 (θ [-360,360] chuẩn hoá [-180,180] · φ [0,180] · r (0,1000] · target [-100,100]) → `PATCH /admin/products/{id}/model-view` → 204 → `router.refresh()` + "Đã lưu góc 🧡"; **chưa có model** → note "tải mô hình trước"; **no-WebGL** → note; **lỗi tải/asset** (`onError`) → thông báo. **prefers-reduced-motion tôn trọng by construction** (KHÔNG auto-rotate · `interaction-prompt="none"` → không chuyển động tự thân). **Owner-only ở server** (PATCH model-view `authOwnerOnly`; staff → collapse `forbidden`); FE optimistic owner (P3-e ROLE precedent). **Pose là display-metadata float, KHÔNG tiền** (ADR-038) — không đường tiền, không money-freeze. i18n `products.edit.preview.*` (không hard-code). **+1 dep** `@google/model-viewer` · **KHÔNG** contract/codegen/migration. *(test: `apps/admin/test/model-view.test.ts` → `orbitToModel3dView` [rad→độ · m→% theo ideal · chuẩn hoá θ 270°→-90° · clamp r/target · fallback 105% khi ideal=0] + `model3dViewToAttrs` [attr strings]; viewer render + orbit + save round-trip = Playwright Phase 5)*

## Cụm 55 — Admin categories CRUD (BE) (P3-o slice o-1a · Track F · spec §02/§03 State)

> **Cụm 54 (`ADM-30`) = Stage 5 slice l-5** (khép epic ADR-037/ADR-039) → Cụm 55 nối tiếp (monotonic). Đây là **P3-o slice o-1a** — nửa **BE** của Danh mục admin (`/danh-muc` = seam sidebar chết, chưa có route). **Strict-prefix**: o-1a = admin categories CRUD **KHÔNG migration** (chỉ name/slug trên bảng `categories` sẵn có `{id,slug,name}`); **o-1b** = FE `/danh-muc` (list + dialog create/rename/delete, lấp seam); **o-2** = rich (migration `display_order`+`description`+`thumbnail` → drag-reorder [dnd-kit đã có] + upload ảnh [reuse `upload-proof`]). Tái dùng trọn precedent P3-j-a: handler `admin_products.go` pattern (assertOwner→clean→repo→pg-constraint→DTO), delete-409 (`errProductInUse`→`errCategoryInUse`), classify `authOwnerOnly`, queries `catalog.sql`/`catalog.go`.
> **Go-gated → CỐ Ý để `[ ]`** (cùng quy ước ADM-13/ADM-14/ADM-16 BE): test của `ADM-31` là **Go** (`httpapi.*`), parser ledger `packages/core` chỉ resolve test-id **TS** nên một `[x]` sẽ fail app-gate — **gate thật** = `make verify-go` (services-gates CI).

- [ ] `ADM-31` — WHEN owner gọi category-write (`POST`/`PATCH`/`DELETE /admin/categories[/{id}]` — mọi write **owner-only** qua classify→`authOwnerOnly` **+** re-assert `assertOwner` trong handler [staff→**403**, no-actor→**401**, **trước mọi DB touch**]; danh mục = quyền catalog owner, spec §08) HOẶC admin đọc (`GET /admin/categories` — `authRequired` owner+staff, như product read), the system shall: **đọc** trả **MỌI category** (kể cả rỗng + chỉ-có-hàng-ẩn) kèm `productCount` = số product tham chiếu **qua MỌI status** (`LEFT JOIN products` GROUP BY, count là tín hiệu xoá-được: >0 ⇒ delete chặn 409), sắp **name A→Z** với slug (UNIQUE) làm tiebreak = total-order xác định, **không phân trang** (set nhỏ, admin-curated) — **KHÁC** public `GetCategories` active-only (`EXISTS ... status='active'` non-leak); **create** validate **trước write** (slug `^[a-z0-9]+(?:-[a-z0-9]+)*$` + name non-empty, cả hai ≤ cap → **400** field-map), slug trùng → **400** field `slug` (UNIQUE 23505), trả `Category` (productCount definitionally 0 → không cần list-shape); **update** rename/re-slug, unknown id → **404** (`db.ErrNotFound`), slug trùng category khác → **400** field `slug`, trả `Category` (productCount không đổi bởi rename); **delete** hard-delete chỉ khi **không** product nào tham chiếu, còn hàng → **409** `CATEGORY_IN_USE` (FK `products.category_id` NOT NULL NO ACTION 23503 — steer reassign/archive trước), unknown → **404**. **KHÔNG** tiền (danh mục không mang money — no formatter, no money-freeze); **KHÔNG** migration (name/slug trên bảng sẵn có). *(test: `httpapi.TestAdminCategoryCRUDEndToEnd` [create→list-with-productCount→product-bump-count→rename→delete-empty→unknown-404 real-PG] + `TestDeleteCategoryInUseReturns409` [product tham chiếu → 23503→409] + `TestCategorySlugConflictReturns400` [dup create + dup rename → 400 field slug] + `TestAdminCategoryWritesAreOwnerOnly` [3 write × staff-403/no-actor-401 Docker-free] + `TestCleanCategoryInput` [slug shape · name blank/over-long] + classify 3 write→`authOwnerOnly`, 1 read→`authRequired`)*

## Cụm 56 — Danh mục: thứ tự menu + hiện/ẩn + mô tả/ảnh (BE) (P3-o slice o-2 · Track F · spec §02/§03)

> **Nối tiếp Cụm 55 (o-1a CRUD).** Đây là **P3-o slice o-2** — nửa **BE** của phần "rich": migration `000022` thêm 4 cột ADDITIVE lên `categories` (`display_order`/`description`/`image_url`/`visible`, đều `NOT NULL DEFAULT`, backfill `display_order` theo thứ tự A→Z hiện tại → menu khách **không đổi** tới khi owner kéo). Endpoint reorder mới (`POST /admin/categories/reorder`) + mở rộng `PATCH /admin/categories/{id}` sang `CategoryUpdate` (thêm description/imageUrl/visible; `displayOrder` **KHÔNG** đổi ở đây — chỉ reorder đổi). **Ảnh = reuse** đường `upload-proof` (presigned-POST, cùng host ảnh sản phẩm) → **KHÔNG** hạ tầng upload mới ở BE. Public `Category` DTO **giữ nguyên** `{id,slug,name}` (storefront chỉ render chip name+slug → description/image = admin-only, không mở contract công khai).
> **Go-gated → CỐ Ý để `[ ]`** (cùng quy ước ADM-31/ADM-13): test là **Go** (`httpapi.*`), parser ledger `packages/core` chỉ resolve test-id **TS** nên một `[x]` sẽ fail app-gate — **gate thật** = `make verify-go` (services-gates CI).

- [ ] `ADM-32` — WHEN owner sắp xếp/ẩn/mô tả danh mục (`POST /admin/categories/reorder` + `PATCH /admin/categories/{id}` với `CategoryUpdate` — cả hai **owner-only** qua classify→`authOwnerOnly` **+** re-assert `assertOwner` [staff→**403**, no-actor→**401**, **trước mọi DB touch**]) HOẶC một khách GET `/categories` công khai, the system shall: **reorder** — nhận **toàn bộ** danh sách id theo thứ tự mong muốn → set `display_order` = vị trí 0-based **trong một câu UPDATE nguyên tử** (`unnest … WITH ORDINALITY`), id vắng mặt giữ thứ tự cũ (FE gửi đủ list), id lạ là no-op vô hại, **idempotent** (gửi lại cùng thứ tự = no-op), body nil → **400**; **update** lưu slug/name **+ metadata o-2** (description ≤ cap · imageUrl ≤ cap [tin URL owner tự cấp từ presign, như ảnh gallery] · visible), **KHÔNG** đụng `display_order`, slug trùng → **400** field `slug` (UNIQUE 23505), unknown id → **404**, gom **mọi** field-fault một lần (slug + description cùng lộ); **create** giữ `CategoryInput` = slug/name (metadata mặc định '', '', visible=true; **append cuối menu** `display_order = max+1`); **admin đọc** (`GET /admin/categories`) trả thêm `displayOrder`/`description`/`imageUrl`/`visible`, sắp theo `display_order` rồi name/slug, hiện **cả** category đã ẩn (khác public); **public đọc** (`GET /categories`) áp **hai cổng** thành-viên-menu — `visible AND EXISTS(product active)` — nên category ẩn (dù còn hàng active) **rớt** khỏi menu khách trong khi vẫn ở admin, và trả theo `display_order` (thứ tự khách thấy = thứ tự owner kéo), public `Category` DTO **giữ** `{id,slug,name}` (**không** lộ description/image). **KHÔNG** tiền (danh mục không mang money — no formatter, no money-freeze). *(test: `httpapi.TestCategoryReorderAndVisibility` [reorder → public order đổi + hide → rớt public/còn admin real-PG] + `TestAdminCategoryCRUDEndToEnd` [edit set description/image/visible round-trip trong admin list] + `TestCleanCategoryUpdate` [reuse slug/name + cap description/imageUrl + gom-nhiều-fault Docker-free] + `TestAdminCategoryWritesAreOwnerOnly` [+`ReorderAdminCategories` × staff-403/no-actor-401] + classify `ReorderAdminCategories`→`authOwnerOnly`)*

## Cụm 57 — Đánh giá: kiểm duyệt (BE) (P3-m · Track E · spec §02/§08)

> **Track E (lõi roadmap)** — nửa **BE** của moderation đánh giá (`/danh-gia` = seam sidebar chết). `reviews` table sẵn từ P1 (`{id,product_id,customer_id?,rating,body,images,reply?,status}`; enum `review_status` = **chỉ `published`/`hidden`**, mặc định `published` — KHÔNG có 'pending'). **NO migration.** Seam mới: `ListAllReviews` (JOIN product name + LEFT JOIN customer name; nullable-status narg) + `UpdateReviewModeration` (partial: status coalesce + reply theo `@set_reply`). **RBAC = owner+staff** (`authRequired` mặc định — staff kiểm duyệt đánh giá, spec §08 — KHÁC owner-only catalog). Tab FE (Chờ trả lời / Đã trả lời / Có ảnh) **suy ra client-side** từ (reply, images), KHÔNG phải status.
> **Go-gated → CỐ Ý để `[ ]`** (cùng quy ước ADM-31/ADM-32): test là **Go** (`httpapi.*`), parser ledger `packages/core` chỉ resolve test-id **TS** — gate thật = `make verify-go`.

- [ ] `ADM-33` — WHEN owner HOẶC staff GET `/admin/reviews[?status=]` (kiểm duyệt read) HOẶC PATCH `/admin/reviews/{id}` (kiểm duyệt write — **cả hai `authRequired` owner+staff** qua classify default; staff kiểm duyệt đánh giá, spec §08, KHÁC owner-only catalog), the system shall: **đọc** trả **MỌI đánh giá** qua **MỌI status** (kể cả `hidden` — mặt moderation, KHÁC public `GET /products/{slug}/reviews` published-only tại nguồn SQL) kèm **tên sản phẩm** + **tên khách** (`customerName` = **null** cho guest — customer_id nullable; PII admin-only PDPL, chỉ sau tường auth), newest-first, filter status tuỳ chọn (nullable narg = Tất cả/tab), **không phân trang** (set nhỏ, FE suy ra tab Chờ-trả-lời/Đã-trả-lời/Có-ảnh client-side); **write** áp thay đổi partial — `status` (publish↔hide, coalesce nếu vắng) và/hoặc `reply` (set jsonb `{body, at}` với **`at` server-stamp** — client không backdate; reply chỉ đụng khi có, nên hide/hiện đơn thuần giữ nguyên reply cũ), **bắt buộc ≥1 field** (body rỗng → **400**), reply blank/quá-cap (2000 rune) → **400**, status ngoài enum → **400**, unknown id → **404**, trả **204** (FE đọc lại list); **non-leak**: ẩn một đánh giá → **rớt khỏi public list** nhưng còn ở admin; reply+publish → hiện ở public kèm reply. **KHÔNG** outbox (moderation nội bộ, plan §5); **KHÔNG** tiền; **KHÔNG** migration. *(test: `httpapi.TestAdminReviewModerationEndToEnd` [list-both-status + product/customer names + guest-null-name + `?status=hidden` + hide→rớt-public/còn-admin + reply+publish→hiện-public-`at`-stamped + **staff-moderate-succeeds** + unknown-404 + empty-body-400 real-PG] + `TestBuildReviewModeration` [status-only/reply-only-trim/both/empty-reject/blank-reject/over-cap-reject/bad-status-reject Docker-free] + `TestParseReviewStatusFilter` + classify `GetAdminReviews`/`UpdateAdminReview`→`authRequired`)*
